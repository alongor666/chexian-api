
# Claude Code worktrees（本地临时目录，禁止提交为 gitlink）
.claude/worktrees/

# codex 闸-1/闸-2 prompt + verdict（evidence-loop wrapper 内部产物，按需重生，不入库）
.codex-gates/

# 第三方 Claude Code 插件本地副本（gstack/codex/browse 等，非项目资产，体积 350MB+）
# 项目原创保留：.claude/skills/*.md 文件；第三方 skill（目录+gstack 旧版 symlink）全部忽略
.claude/skills/*
!.claude/skills/*.md

# 本地 plans 目录（一次性规划文件）
.claude/plans/

# Temp files
.tmp/
server/.tmp/

# ETL 异常哨兵运行时状态（ETag + silence fingerprints；workflow cache 持久化跨 run，不入库）
.sentinel-state/

# 业务数据文件（禁止提交，含客户敏感信息）
*.xlsx
*.xls
*.csv
*.tsv
*.tar.gz
*.parquet

# daily.mjs 自动归档目录（旧短窗 xlsx/parquet 被新全量覆盖时归档，含历史快照子目录）
数据管理/.archive/

# 临时数据文件
parquet_data.json

# 巡检报告 JSON（生成数据，通过 rsync 同步到 VPS）
数据管理/patrol_reports/*/latest.json
数据管理/patrol_reports/*/*.json
!数据管理/patrol_reports/*/report.md

# 外部系统同步产物（含客户 VIN/record_id 等敏感映射，禁止提交）
数据管理/outputs/

# 工作流模板
CC工作流复制/
签子流程复制/

# Python缓存
__pycache__/
*.pyc
*.pyo

# 临时/一次性脚本（限定根目录，避免误杀 scripts/ 和 pipelines/ 下的正式脚本）
/compare_data.py
/read_parquet.py
/test_data.parquet

# 系统文件
.DS_Store
Thumbs.db

# 依赖和构建产物
node_modules/
dist/
.vite/
*.tsbuildinfo

# 测试覆盖率
coverage/

# IDE
.vscode/
.idea/
*.swp
*.swo
.trae/

# MCP 工具
.playwright-mcp/
.serena/
2025年复盘/2025fupan/
2025年复盘/dashboard-project/
.obsidian/

# 环境变量（含密钥，禁止提交）
.env
.env.local
.env.production
.env.health-check
server/.env

# 密码文档（禁止提交）
PASSWORDS.txt
PASSWORDS.local.txt

# 本地设置（不提交）
.claude/settings.local.json

# user-only 写入授权哨兵（AGENTS.md §8.3 可授权档，仅用户本人 touch/rm，本地状态不入库）
.claude/.user-only-write-ok

# sync-vps 同步清单（本地状态，不提交）
.last-sync-manifest.json

# 服务端运行时数据（Parquet 大文件 + 用户配置，禁止提交）
server/data/

# 数据管理日志
数据管理/logs/*.log

# 数据管理大文件（staging中间文件）
数据管理/staging/*.xlsx

# 数据管理生成报告（一次性产出，按需重新生成）
数据管理/output/
logs/

# 诊断分析报告产物（AI 生成的 MD/JSON/HTML，按需重新生成，保留 .py 工具脚本）
数据分析报告/
数据管理/数据分析报告/**/*.md
数据管理/数据分析报告/**/*.json
数据管理/数据分析报告/**/*.html
!数据管理/数据分析报告/**/README.md

# 驾意险推介率输出产物（按需重新生成）
数据管理/驾意险推介率/输出/

# 临时开发计划（不提交）
开发文档/plan.md

# 续保临时 SQL/分析（限定 scripts/ 目录）
scripts/*续保*.sql
scripts/*续保*.md
scripts/check_renewal_*.sql

# Playwright/调试产物（禁止提交）
.playwright-cli/
playwright-report/
test-results/
test_output.txt
vitest_log.txt
dev_log.txt
test_err.txt
output/playwright/
.gstack/

# sage-wiki（独立子目录，含 API Key 和运行时数据）
.sage-wiki/
sage-wiki
.planning/

# 编辑器 / 工具本地备份
*.bak.*

# 第三方 MCP 工具本地状态
.smtcmp_json_db/

# ETL 运行时分区元数据（每次 ETL 刷新）
warehouse/**/_partition_meta.json
数据管理/warehouse/**/_partition_meta.json

# Copilot 本地模板工作区（个人资产，不入主仓库）
# 仅匹配根目录 /copilot/，避免误伤 src/features/copilot 等代码目录
/copilot/

# Snapshot 运行时产物（build-snapshots.mjs 每次重建，52MB+ 不入仓库）
数据管理/warehouse/snapshots/
数据管理/raw/full_snapshot/
数据管理/release-manifests/

# daily.mjs 互斥锁（运行时临时文件）
数据管理/.daily.lock

# sx-promote 运行时产物（promote 清单快照 + ready 标记），随 --target-dir 落盘
# （真实 promote→current/，测试→tmp）。非 .parquet，bootstrapper/sync-vps 不加载/不同步；
# 禁入 git（#783 曾因写固定仓库路径污染被追踪文件，引发 spurious 脏状态）。
.sx-promote-manifest.json
.sx-promote-ready

# 赔付率周报对账输出（每周覆盖，每次 6MB；README.md 入 git，子目录 JSON 不入）
数据管理/validation/loss-ratio-weekly/*/
!数据管理/validation/loss-ratio-weekly/README.md

# 集成同步运行时产物（wecom_smartsheet 等含 VIN/保单号敏感数据，禁止提交）
数据管理/integrations/*/state/
数据管理/integrations/*/logs/
数据管理/integrations/*/__pycache__/
数据管理/integrations/*/.pytest_cache/

# 报告 HTML 由 skill 本地生成，不入仓库（VPS 通过 sync-vps.mjs 单独分发）
public/reports/**/*.html
# manifest.json 是 gen-reports-manifest.mjs 的生成产物（owning host 上累积，不入仓库）
public/reports/**/manifest.json
2026-05-23_诊断报告_结构化汇总_完整版.md

# 一次性诊断产物（HTML 报告的 md 导出）
2026-05-*_诊断报告_*.md

# 性能测试产物
artifacts/

# 浏览器/QA 截图（可能含登录表单/token/凭据/敏感业务数据）：本地保留便于复用，永不入库防泄漏
screenshots/
social-card-sx-renewal/
