# ETL & 数据管理脚本依赖（最小集）
# 安装：pip install -r 数据管理/requirements.txt
#
# Python 3.10+ 推荐；生产 VPS 以实际版本为准
duckdb>=1.4.0
pandas>=2.0.0
pyarrow>=15.0.0
openpyxl>=3.1.0
numpy>=1.26.0
# pandas read_excel 引擎（etl_validation.py / transform.py 的 EXCEL_ENGINE='calamine'）
# 2026-07-05 CI 首跑暴露：本机装了但此处漏登记，7 个 ETL 测试在 CI ImportError
python-calamine>=0.2.0
