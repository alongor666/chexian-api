---
phase: 02-sql
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - src/shared/config/org-groups.ts
  - src/features/dashboard/CrossSellOrgTrendChart.tsx
  - src/shared/utils/date.ts
  - src/features/cost/components/CostAnalysisPanel.tsx
  - src/features/coefficient/
  - src/features/pages/CoefficientPage.tsx
  - src/app/App.tsx
  - src/components/layout/SidebarNavigation.tsx
  - src/shared/utils/coefficient-period.ts
  - src/shared/config/coefficient-thresholds.ts
  - src/shared/api/routes.ts
  - src/shared/api/client.ts
  - src/shared/api/query-keys.ts
  - tests/api/client.test.ts
  - 开发文档/00_index/CODE_INDEX.md
autonomous: true
requirements:
  - SQL-02
  - SQL-03
user_setup: []

must_haves:
  truths:
    - "bun run build 零 TypeScript 报错"
    - "CrossSellOrgTrendChart.tsx 正常编译，ORG_GROUPS 从 org-groups.ts 导入"
    - "CostAnalysisPanel.tsx 正常编译，formatDate/getLastDayOfMonth 从 shared/utils/date.ts 导入"
    - "src/features/coefficient/ 目录不存在"
    - "App.tsx 不包含任何 CoefficientPage 或 /coefficient 路由引用"
    - "SidebarNavigation.tsx 不包含 /coefficient 路径或 '系数监控' 文字"
    - "bun run governance 通过"
  artifacts:
    - path: "src/shared/config/org-groups.ts"
      provides: "ORG_GROUPS 业务常量（从 coefficient-thresholds 迁移）"
      exports: ["ORG_GROUPS"]
    - path: "src/shared/utils/date.ts"
      provides: "通用日期工具（formatDate / getLastDayOfMonth）"
      exports: ["formatDate", "getLastDayOfMonth"]
  key_links:
    - from: "src/features/dashboard/CrossSellOrgTrendChart.tsx"
      to: "src/shared/config/org-groups.ts"
      via: "import { ORG_GROUPS } 迁移后的新路径"
      pattern: "org-groups"
    - from: "src/features/cost/components/CostAnalysisPanel.tsx"
      to: "src/shared/utils/date.ts"
      via: "import { formatDate, getLastDayOfMonth } 迁移后的新路径"
      pattern: "shared/utils/date"
---

<objective>
前端系数监控模块全链路删除 + SQL-03 搁置书面结论 — 先迁移被非系数模块依赖的工具常量（ORG_GROUPS / formatDate），再删除所有系数专属代码，最后更新索引文档并记录 SQL-03 搁置决策。

Purpose: 彻底移除前端系数监控功能（组件目录 + 页面 + 路由注册 + 导航项 + API 客户端定义），同时保证交叉销售和成本分析模块的依赖正常工作。SQL-03 的搁置决策在本计划中以书面结论形式存档。
Output: bun run build 零报错，无任何 coefficient 路径可访问，governance 通过

---

## SQL-03 搁置结论（书面存档）

**需求**：SQL-03 要求对 earned-premium-detail.ts 进行 EXPLAIN ANALYZE 验证，判断 12 月滚动查询是否适合 CTE 合并。

**用户决策（D-08）**：用户在 CONTEXT.md 中明确表示"暂时忘了具体上下文，搁置到后续阶段或需求明确后再处理"。

**结论**：SQL-03 在本次 Phase 2 执行中**跳过**，不执行 EXPLAIN ANALYZE 分析，也不修改 earned-premium-detail.ts 任何代码。该需求仍保留在 REQUIREMENTS.md SQL-03 条目中，待后续用户重新明确上下文后在独立阶段处理。

**ROADMAP Success Criteria 更新**：根据 CONTEXT.md deferred 章节说明，ROADMAP Phase 2 的 Success Criteria 第 2、3 条（关于 coefficient 响应时间）已被替换为功能删除验证，第 4 条（earned-premium-detail EXPLAIN ANALYZE）由于 D-08 搁置，Phase 2 的实际完成标准修订为：
1. 黄金快照基线建立完成（SQL-01 完成）
2. /api/query/coefficient 路由不存在（返回 404）
3. 前后端系数监控代码全量清除（build 零报错）
4. SQL-03 满期保费明细决策：书面结论已存档，搁置至后续阶段
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-sql/02-CONTEXT.md
@.planning/phases/02-sql/02-02-SUMMARY.md

<interfaces>
<!-- 关键接口信息，执行器直接使用 -->

**src/shared/config/coefficient-thresholds.ts 中需要迁移的 ORG_GROUPS（完整内容）**：
```typescript
// 迁移到 src/shared/config/org-groups.ts 的内容：
export const ORG_GROUPS = {
  /** 同城机构（成都） */
  SAME_CITY: ['天府', '高新', '新都', '青羊', '武侯', '重客', '本部'],
  /** 异地机构（中支） */
  REMOTE: ['宜宾', '德阳', '资阳', '泸州', '自贡', '乐山', '达州'],
} as const;
```

**src/features/dashboard/CrossSellOrgTrendChart.tsx 第 16 行当前 import（需修改）**：
```typescript
import { ORG_GROUPS } from '../../shared/config/coefficient-thresholds';
```
修改后：
```typescript
import { ORG_GROUPS } from '../../shared/config/org-groups';
```

**src/shared/utils/coefficient-period.ts 中需要迁移的两个函数**（与后端版实现完全相同）：
```typescript
export function formatDate(date: Date): string {
  const year = date.getFullYear();
  const month = String(date.getMonth() + 1).padStart(2, '0');
  const day = String(date.getDate()).padStart(2, '0');
  return `${year}-${month}-${day}`;
}

export function getLastDayOfMonth(year: number, month: number): number {
  return new Date(year, month + 1, 0).getDate();
}
```

**src/features/cost/components/CostAnalysisPanel.tsx 第 29 行当前 import（需修改）**：
```typescript
import { formatDate, getLastDayOfMonth } from '../../../shared/utils/coefficient-period';
```
修改后：
```typescript
import { formatDate, getLastDayOfMonth } from '../../../shared/utils/date';
```

**App.tsx 中需要删除的 coefficient 相关内容（3 处）**：
1. 第 74-75 行的 lazy import：
   ```typescript
   const CoefficientPage = lazy(() =>
     import('../features/pages/CoefficientPage').then((m) => ({ default: m.CoefficientPage }))
   ```
2. 第 237-241 行的路由定义（4 行）：
   ```typescript
   path="coefficient"
   ...
   <RouteAccessGuard routePath="/coefficient">
     ...
     <LazyRoute><CoefficientPage /></LazyRoute>
   ```

**SidebarNavigation.tsx 中需要删除的内容（2 处）**：
1. 第 55 行：`{ path: '/coefficient', icon: Search, label: '系数监控', shortLabel: '系数' },`
2. 第 117-119 行的 case 分支（含 queryKey：coefficient）

**src/shared/api/routes.ts 中需要删除**：
- 第 32 行：`COEFFICIENT: 'coefficient',`

**src/shared/api/client.ts 中需要删除**：
- 第 584 行：`async getCoefficientData(filters?: Record<string, any>): Promise<any> { ... }`

**src/shared/api/query-keys.ts 中需要删除**：
- 第 63-64 行：`coefficient: (params: Record<string, unknown>) => ['coefficient', params] as const,`

**tests/api/client.test.ts 中需要删除**：
- 第 273-290 行的 'should preserve coefficient batch query contract fields' 测试（约 18 行）

**前端删除文件/目录清单（D-05）**：
1. `src/features/coefficient/`（整个目录）
2. `src/features/pages/CoefficientPage.tsx`
3. `src/shared/utils/coefficient-period.ts`（Task 1 迁移完成后）
4. `src/shared/config/coefficient-thresholds.ts`（Task 1 迁移 ORG_GROUPS 完成后）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 迁移前端依赖（ORG_GROUPS 和 formatDate）到通用模块</name>
  <read_first>
    - src/shared/config/coefficient-thresholds.ts（提取 ORG_GROUPS 完整定义，确认第 151-156 行）
    - src/features/dashboard/CrossSellOrgTrendChart.tsx（确认第 16 行 import，第 35/38/39 行使用）
    - src/shared/utils/coefficient-period.ts（提取 formatDate/getLastDayOfMonth 实现）
    - src/features/cost/components/CostAnalysisPanel.tsx（确认第 29 行 import）
    - src/shared/utils/（确认 date.ts 是否已存在）
    - src/shared/config/（确认 org-groups.ts 是否已存在）
  </read_first>
  <files>
    src/shared/config/org-groups.ts,
    src/features/dashboard/CrossSellOrgTrendChart.tsx,
    src/shared/utils/date.ts,
    src/features/cost/components/CostAnalysisPanel.tsx
  </files>
  <action>
**步骤 1：创建 src/shared/config/org-groups.ts**

新建文件，内容：
```typescript
/**
 * 机构分组配置
 *
 * 从 coefficient-thresholds.ts 迁移而来，供交叉销售模块使用。
 * 与系数监控功能无关，是通用业务常量。
 */

export const ORG_GROUPS = {
  /** 同城机构（成都） */
  SAME_CITY: ['天府', '高新', '新都', '青羊', '武侯', '重客', '本部'],
  /** 异地机构（中支） */
  REMOTE: ['宜宾', '德阳', '资阳', '泸州', '自贡', '乐山', '达州'],
} as const;
```

**步骤 2：修改 src/features/dashboard/CrossSellOrgTrendChart.tsx 第 16 行**

将：
```typescript
import { ORG_GROUPS } from '../../shared/config/coefficient-thresholds';
```
改为：
```typescript
import { ORG_GROUPS } from '../../shared/config/org-groups';
```
不修改文件的其他任何内容。

**步骤 3：创建 src/shared/utils/date.ts**

新建文件，内容：
```typescript
/**
 * 通用日期工具函数
 *
 * 从 coefficient-period.ts 迁移而来，供成本分析等模块使用。
 */

/**
 * 格式化日期为 YYYY-MM-DD 字符串
 */
export function formatDate(date: Date): string {
  const year = date.getFullYear();
  const month = String(date.getMonth() + 1).padStart(2, '0');
  const day = String(date.getDate()).padStart(2, '0');
  return `${year}-${month}-${day}`;
}

/**
 * 获取月份的最后一天（0-indexed month）
 */
export function getLastDayOfMonth(year: number, month: number): number {
  return new Date(year, month + 1, 0).getDate();
}
```

**步骤 4：修改 src/features/cost/components/CostAnalysisPanel.tsx 第 29 行**

将：
```typescript
import { formatDate, getLastDayOfMonth } from '../../../shared/utils/coefficient-period';
```
改为：
```typescript
import { formatDate, getLastDayOfMonth } from '../../../shared/utils/date';
```
不修改文件的其他任何内容。

**步骤 5：验证**
执行 `bun run build`，确认无 TS 报错，CrossSellOrgTrendChart 和 CostAnalysisPanel 均正常编译。
  </action>
  <verify>
    <automated>bun run build 2>&1 | grep -E "error|Error" | grep -v "node_modules" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `cat src/shared/config/org-groups.ts` 包含 `export const ORG_GROUPS`
    - `grep "coefficient-thresholds" src/features/dashboard/CrossSellOrgTrendChart.tsx` 无输出
    - `grep "org-groups" src/features/dashboard/CrossSellOrgTrendChart.tsx` 有输出
    - `cat src/shared/utils/date.ts` 包含 `export function formatDate` 和 `export function getLastDayOfMonth`
    - `grep "coefficient-period" src/features/cost/components/CostAnalysisPanel.tsx` 无输出
    - `grep "shared/utils/date" src/features/cost/components/CostAnalysisPanel.tsx` 有输出
    - `bun run build` 退出码为 0，输出不含 "error TS" 字样
  </acceptance_criteria>
  <done>ORG_GROUPS 迁移到 org-groups.ts；formatDate/getLastDayOfMonth 迁移到 shared/utils/date.ts；两处 import 已更新；build 通过</done>
</task>

<task type="auto">
  <name>Task 2: 删除前端系数模块并清理路由/导航/API 注册</name>
  <read_first>
    - src/app/App.tsx（确认第 74-75 行 lazy import 和第 237-241 行路由定义）
    - src/components/layout/SidebarNavigation.tsx（确认第 55 行导航项和第 117-119 行 case）
    - src/shared/api/routes.ts（确认第 32 行 COEFFICIENT 常量）
    - src/shared/api/client.ts（确认第 584 行 getCoefficientData 方法）
    - src/shared/api/query-keys.ts（确认第 63-64 行 coefficient key）
    - tests/api/client.test.ts（确认第 273-290 行 coefficient 测试块边界）
    - 开发文档/00_index/CODE_INDEX.md（找到 coefficient 相关条目，准备更新）
  </read_first>
  <files>
    src/features/coefficient/,
    src/features/pages/CoefficientPage.tsx,
    src/shared/utils/coefficient-period.ts,
    src/shared/config/coefficient-thresholds.ts,
    src/app/App.tsx,
    src/components/layout/SidebarNavigation.tsx,
    src/shared/api/routes.ts,
    src/shared/api/client.ts,
    src/shared/api/query-keys.ts,
    tests/api/client.test.ts,
    开发文档/00_index/CODE_INDEX.md
  </files>
  <action>
按以下顺序执行：

**步骤 1：删除系数专属文件/目录（Task 1 已完成依赖迁移，现在安全删除）**
- 删除整个目录 `src/features/coefficient/`（含 components × 5 + hooks × 1 + utils × 3 + types × 1）
- 删除文件 `src/features/pages/CoefficientPage.tsx`
- 删除文件 `src/shared/utils/coefficient-period.ts`（已无消费者）
- 删除文件 `src/shared/config/coefficient-thresholds.ts`（ORG_GROUPS 已迁移，此文件已无非系数引用）

**步骤 2：编辑 src/app/App.tsx**

删除以下内容（精确定位后删除，不修改其他行）：
```typescript
// 删除 lazy import（第 74-75 行附近，2 行）：
const CoefficientPage = lazy(() =>
  import('../features/pages/CoefficientPage').then((m) => ({ default: m.CoefficientPage }))
);

// 删除路由定义块（第 237-241 行附近，约 5 行）：
path="coefficient"
...
<RouteAccessGuard routePath="/coefficient">
  ...
  <LazyRoute><CoefficientPage /></LazyRoute>
</RouteAccessGuard>
```

**步骤 3：编辑 src/components/layout/SidebarNavigation.tsx**

删除以下内容：
- 导航数组中的 `{ path: '/coefficient', icon: Search, label: '系数监控', shortLabel: '系数' }` 整行
- switch/case 中的 `case '/coefficient':` 分支（含 queryKey coefficient 的整个 case 块）

**步骤 4：编辑 src/shared/api/routes.ts**

删除：`COEFFICIENT: 'coefficient',` 整行（约第 32 行）

**步骤 5：编辑 src/shared/api/client.ts**

删除：`async getCoefficientData(...)` 整行（约第 584 行）

**步骤 6：编辑 src/shared/api/query-keys.ts**

删除：`coefficient: (params: Record<string, unknown>) => ['coefficient', params] as const,` 两行（约第 63-64 行）

**步骤 7：编辑 tests/api/client.test.ts**

删除：`it('should preserve coefficient batch query contract fields', ...)` 整个测试块（第 273-290 行约 18 行，从 `it(` 到对应的 `});` 结束）

**步骤 8：更新 开发文档/00_index/CODE_INDEX.md**

搜索 CODE_INDEX.md 中所有含 "coefficient" 的行，将其删除或更新为已删除备注。此步骤依赖文件实际内容，需 read_first 后精确 Edit。

**步骤 9：最终验证序列**
1. `bun run build` — 零 TS 报错
2. `bun run test -- --run` — 全部 PASS
3. `bun run governance` — 治理通过
  </action>
  <verify>
    <automated>bun run build 2>&1 | grep -c "error TS" && bun run governance 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/features/coefficient/` 报错 "No such file or directory"
    - `ls src/features/pages/CoefficientPage.tsx` 报错 "No such file"
    - `ls src/shared/utils/coefficient-period.ts` 报错 "No such file"
    - `ls src/shared/config/coefficient-thresholds.ts` 报错 "No such file"
    - `grep "CoefficientPage\|/coefficient" src/app/App.tsx` 无输出
    - `grep "系数监控\|/coefficient" src/components/layout/SidebarNavigation.tsx` 无输出
    - `grep "COEFFICIENT\|coefficient" src/shared/api/routes.ts` 无输出
    - `grep "getCoefficientData\|coefficient" src/shared/api/client.ts` 无输出
    - `grep "coefficient" src/shared/api/query-keys.ts` 无输出
    - `grep "coefficient" tests/api/client.test.ts` 无输出
    - `bun run build 2>&1 | grep "error TS"` 无输出（退出码 0）
    - `bun run test -- --run` 退出码 0
    - `bun run governance` 退出码 0
  </acceptance_criteria>
  <done>前端系数模块全链路清除：4 个专属文件/目录已删除；App.tsx/SidebarNavigation/routes/client/query-keys 中所有引用已清理；construct governance 通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 删除操作 → 前端构建 | 删除前需确认 Task 1 的依赖迁移已通过 build 验证 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | src/shared/config/coefficient-thresholds.ts 前端版 | mitigate | Task 1 先迁移 ORG_GROUPS，执行 bun run build 验证，Task 2 才删除 coefficient-thresholds.ts |
| T-02-07 | Tampering | App.tsx 路由删除范围 | mitigate | read_first 确认行号后精确 Edit，只删除 coefficient 相关的 lazy import + route block |
| T-02-08 | Denial of Service | coefficient-period.ts 残余引用 | mitigate | Task 1 完成后 `grep -rn coefficient-period src/` 扫描确认零残余，再删除文件 |
</threat_model>

<verification>
全量 grep 扫描（Phase 2 完成标志）：
- `grep -rn "coefficient" src/ --include="*.ts" --include="*.tsx" 2>/dev/null | grep -v "node_modules"` — 零输出（所有前端 coefficient 引用清除）
- `grep -rn "coefficient" server/src/ --include="*.ts" 2>/dev/null` — 零输出（所有后端 coefficient 引用清除）
- `bun run build` — 退出码 0
- `bun run test -- --run` — 退出码 0
- `bun run governance` — 退出码 0
- server 运行时：`curl -s -o /dev/null -w '%{http_code}' localhost:3000/api/query/coefficient` — 返回 404
</verification>

<success_criteria>
Phase 2 完成（SQL-01 + SQL-02）：
1. 黄金快照基线已建立（02-01）
2. 前后端系数监控全链路清除 — build/test/governance 全部通过
3. /api/query/coefficient 路由不存在（404）
4. SQL-03 搁置书面结论已存档（本计划 objective 章节）
5. ROADMAP Phase 2 Success Criteria 已更新（第 2/3/4 条替换）
</success_criteria>

<output>
After completion, create `.planning/phases/02-sql/02-03-SUMMARY.md`
</output>
