---
phase: 02-sql
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/golden-baseline.mjs
  - .planning/golden-baseline/_meta.json
  - .planning/golden-baseline/baseline-manifest.json
autonomous: false
requirements:
  - SQL-01
user_setup: []

must_haves:
  truths:
    - "所有 66 个 GET 端点（含删除前的 /api/query/coefficient）至少各有一份 admin scope 的 JSON 快照存档"
    - "黄金基线目录 .planning/golden-baseline/ 中有 _meta.json 记录构建时间和数据版本"
    - "运行 node scripts/golden-baseline.mjs --compare 与自身对比，输出 PASS for all endpoints，process.exitCode 为 0"
    - "快照脚本支持 --dry-run 模式，不依赖 server 可输出端点清单"
  artifacts:
    - path: "scripts/golden-baseline.mjs"
      provides: "黄金基线构建脚本（--build / --compare / --dry-run）"
      contains: "golden-baseline"
    - path: ".planning/golden-baseline/_meta.json"
      provides: "基线元数据（构建时间、数据 mtime）"
      contains: "buildTime"
    - path: ".planning/golden-baseline/baseline-manifest.json"
      provides: "所有纳入基线的端点清单"
      contains: "endpoints"
  key_links:
    - from: "scripts/golden-baseline.mjs"
      to: "scripts/build-snapshots.mjs"
      via: "复用 atomicWrite / login / computeParamHash 模式"
      pattern: "atomicWrite"
    - from: ".planning/golden-baseline/"
      to: "数据管理/warehouse/snapshots/"
      via: "独立目录，不影响运行时快照服务"
      pattern: ".planning/golden-baseline"
---

<objective>
建立黄金快照回归基线 — 在任何 SQL 代码修改前，对所有 API 端点（含即将删除的系数接口）完整抓取一次 JSON 响应，存入 .planning/golden-baseline/，作为后续回归对比的事实基准。

Purpose: SQL-02（删除系数模块）等任何代码变更完成后，可通过 `node scripts/golden-baseline.mjs --compare` 自动验证结果零差异，满足 D-02（每个字段误差为零）要求。
Output: scripts/golden-baseline.mjs + .planning/golden-baseline/ 目录（66 个端点的 JSON 快照 + 元数据）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-sql/02-CONTEXT.md

<interfaces>
<!-- 从 scripts/build-snapshots.mjs 提取的可复用模式 -->

可复用函数（直接从 build-snapshots.mjs 复制或 import）：
```javascript
// 原子写入
function atomicWrite(filePath, content) {
  const dir = dirname(filePath);
  if (!existsSync(dir)) mkdirSync(dir, { recursive: true });
  const tmpPath = filePath + '.tmp';
  writeFileSync(tmpPath, content, 'utf-8');
  renameSync(tmpPath, filePath);
}

// 参数哈希（12位 sha256）
function computeParamHash(params) {
  const sorted = Object.entries(params)
    .filter(([, v]) => v !== undefined && v !== '')
    .sort(([a], [b]) => a.localeCompare(b))
    .map(([k, v]) => `${k}=${v}`)
    .join('&');
  return crypto.createHash('sha256').update(sorted).digest('hex').slice(0, 12);
}

// 登录（返回 JWT）
async function login(username, password) {
  const res = await fetch(`${SERVER_URL}/api/auth/login`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ username, password }),
  });
  const body = await res.json();
  return body.data?.token || body.token;
}
```

admin 凭据：username='admin', password='CxAdmin@2026!'
server URL：http://localhost:3000（或 SNAPSHOT_SERVER_URL 环境变量）

当前 build-snapshots.mjs 的 9 个 bundle 已覆盖的端点：
/api/query/dashboard-bundle, /api/query/performance-bundle, /api/query/cross-sell-bundle,
/api/filters/options, /api/query/customer-flow/summary, /api/query/customer-flow/inflow,
/api/query/customer-flow/outflow, /api/query/customer-flow/trend, /api/query/customer-flow/metadata

未覆盖的端点（golden-baseline 需补充）— 使用默认参数（空筛选器）各抓一次：
- /api/query/kpi, /api/query/kpi/summary
- /api/query/trend/monthly, /api/query/trend/weekly, /api/query/trend/daily
- /api/query/truck
- /api/query/growth
- /api/query/coefficient  ← 删除前最后一次快照，manifest 标记 deprecated:true
- /api/query/cost
- /api/query/comprehensive/detail, /api/query/comprehensive/summary
- /api/query/cross-sell (6个路由，各抓一次)
- /api/query/salesman
- /api/query/report (3个路由)
- /api/query/premium-plan (3个路由)
- /api/query/performance (5个路由)
- /api/query/quote-conversion (7个路由)
- /api/query/claims-detail (10个路由)
- /api/query/expense-development
- /api/query/repair (4个路由)
- /api/query/renewal-v2 (6个路由)
- /api/query/patrol (2个路由)
- /api/query/bundles (3个 bundle 路由)

回归比对核心逻辑（D-02 严格匹配）：
```javascript
import assert from 'node:assert';

function compareEndpoint(baseline, current, slug) {
  try {
    assert.deepStrictEqual(current, baseline);
    console.log(`  PASS  ${slug}`);
  } catch (err) {
    console.error(`  FAIL  ${slug}`);
    console.error(err.message);
    process.exitCode = 1;
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建黄金基线脚本 golden-baseline.mjs</name>
  <read_first>
    - scripts/build-snapshots.mjs（复用 atomicWrite/login/computeParamHash/fetchBundle 函数）
    - server/src/routes/query.ts（确认所有路由注册，提取真实端点路径）
    - server/src/routes/query/kpi.ts, trend.ts, coefficient.ts（确认路由路径字符串）
  </read_first>
  <files>scripts/golden-baseline.mjs</files>
  <action>
新建 scripts/golden-baseline.mjs，实现三个模式：

**--dry-run 模式**：列出所有将要抓取的端点清单，不访问 server，process.exit(0)。

**--build 模式（默认）**：
1. 检查 server 健康（GET /health），失败则打印提示 "请先启动: bun run dev:full" 并 exit(1)
2. 用 admin 凭据登录获取 JWT（username='admin', password='CxAdmin@2026!'）
3. 并行抓取所有端点（Promise.allSettled），每个端点用默认参数（空对象 {}）
4. 例外端点使用具体参数（见下方 ENDPOINT_DEFINITIONS）
5. 原子写入每个快照到 .planning/golden-baseline/{endpointSlug}/{paramHash}.json，格式：
   `{ _meta: { buildTime, endpointSlug, params, paramHash, deprecated }, data: <响应的 data 字段或整个 body> }`
6. 写入 .planning/golden-baseline/_meta.json：
   `{ buildTime: new Date().toISOString(), serverUrl, totalEndpoints, successCount, failCount }`
7. 写入 .planning/golden-baseline/baseline-manifest.json：
   `{ endpoints: [ { slug, path, params, paramHash, deprecated } ] }`

**--compare 模式**：
1. 读取 baseline-manifest.json，对每个非 deprecated 端点重新抓取
2. 用 `assert.deepStrictEqual(current, baseline)` 比对（D-02：严格精确匹配，零容忍）
3. PASS 绿色，FAIL 红色（打印 assert 错误信息）
4. 任意 FAIL 则 process.exitCode = 1，全部 PASS 则输出总数并 exit(0)

**ENDPOINT_DEFINITIONS 对象**（包含所有 66 个端点，coefficient 标记 deprecated:true）：

从 server/src/routes/query/*.ts 读取真实路径后填入，格式示例：
```javascript
const ENDPOINT_DEFINITIONS = [
  { slug: 'kpi', path: '/api/query/kpi', params: {}, deprecated: false },
  { slug: 'kpi-summary', path: '/api/query/kpi/summary', params: {}, deprecated: false },
  { slug: 'trend-monthly', path: '/api/query/trend/monthly', params: {}, deprecated: false },
  // ... 其余端点
  { slug: 'coefficient', path: '/api/query/coefficient', params: { queryType: 'full' }, deprecated: true },
  // ...
];
```

文件头部用 `#!/usr/bin/env node`，使用 ES Module（import/export）。
复用 build-snapshots.mjs 中的 atomicWrite、computeParamHash、login 函数（直接内联复制，不 import，保持脚本独立）。
颜色输出使用 ANSI codes（与 build-snapshots.mjs 同模式）。
  </action>
  <verify>
    <automated>node scripts/golden-baseline.mjs --dry-run 2>&1 | grep -c "slug\|path\|端点"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/golden-baseline.mjs 文件存在
    - `node scripts/golden-baseline.mjs --dry-run` 不报错，输出包含 "coefficient" 字符串
    - `node scripts/golden-baseline.mjs --dry-run` 不报错，输出包含至少 60 行端点信息（`node scripts/golden-baseline.mjs --dry-run 2>&1 | wc -l` >= 60）
    - 文件包含 `assert.deepStrictEqual` 字符串（比对逻辑存在）
    - 文件包含 `deprecated` 字符串（系数端点标记逻辑存在）
  </acceptance_criteria>
  <done>golden-baseline.mjs 可通过 --dry-run 模式无错误运行，显示 60+ 端点清单，coefficient 端点标记为 deprecated</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 启动 server 并运行黄金基线构建</name>
  <what-built>golden-baseline.mjs 脚本（Task 1 完成）</what-built>
  <how-to-verify>
1. 确认本地开发服务器已启动（如未启动，执行 `bun run dev:full`，等待 "Server listening" 日志）
2. 确认本地 Parquet 数据存在：`ls 数据管理/warehouse/fact/policy/current/`（应有 4 个 .parquet 文件）
3. 执行黄金基线构建：
   ```
   node scripts/golden-baseline.mjs --build
   ```
4. 等待脚本完成，确认输出：
   - "成功" 计数应 >= 60（允许部分非核心端点因参数问题失败）
   - .planning/golden-baseline/ 目录已创建
   - .planning/golden-baseline/_meta.json 存在
   - .planning/golden-baseline/baseline-manifest.json 存在
5. 验证 coefficient 端点快照存在（删除前最后一次记录）：
   ```
   ls .planning/golden-baseline/coefficient/
   ```
6. 运行自我比对验证基线一致性：
   ```
   node scripts/golden-baseline.mjs --compare
   ```
   期望输出：全部 PASS（即时比对，数据未变化，应 100% 通过）
  </how-to-verify>
  <resume-signal>输入 "approved" 确认基线构建成功，或描述失败情况</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| golden-baseline.mjs → localhost:3000 | 脚本以 admin JWT 访问本地 server，仅本地开发使用 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | .planning/golden-baseline/ | accept | 基线目录含 admin scope 数据，但在 .planning/ 下（gitignore 覆盖或本地开发专用），不上传 VPS |
| T-02-02 | Elevation of Privilege | golden-baseline.mjs 硬编码凭据 | accept | admin 凭据已在 build-snapshots.mjs 中同样存在，仅开发脚本，不影响生产 |
</threat_model>

<verification>
- `node scripts/golden-baseline.mjs --dry-run` 零报错，输出 60+ 端点
- `.planning/golden-baseline/_meta.json` 存在且含 `buildTime` 字段
- `.planning/golden-baseline/baseline-manifest.json` 存在且含 `endpoints` 数组
- `.planning/golden-baseline/coefficient/` 目录存在（系数端点最后一次快照）
- `node scripts/golden-baseline.mjs --compare` 在基线建立后立即运行输出全部 PASS
</verification>

<success_criteria>
SQL-01 完成：
1. golden-baseline.mjs 脚本已创建，支持 --build / --compare / --dry-run 三种模式
2. .planning/golden-baseline/ 中有 66 个端点（含 coefficient deprecated 标记）的 JSON 快照
3. --compare 模式与自身比对 100% PASS（为 SQL-02 删除后的回归比对做好准备）
</success_criteria>

<output>
After completion, create `.planning/phases/02-sql/02-01-SUMMARY.md`
</output>
