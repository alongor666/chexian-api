---
phase: 02-sql
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - server/src/utils/date.ts
  - server/src/sql/cost/earned-premium.ts
  - server/src/sql/__tests__/coefficient.test.ts
  - server/src/sql/coefficient.ts
  - server/src/routes/query/coefficient.ts
  - server/src/config/coefficient-thresholds.ts
  - server/src/utils/coefficient-period.ts
  - server/src/routes/query.ts
autonomous: true
requirements:
  - SQL-02
user_setup: []

must_haves:
  truths:
    - "bun run test 全部通过，coefficient.test.ts 文件不存在"
    - "bun run build 零 TypeScript 报错"
    - "server/src/routes/query.ts 不再包含 coefficientRoutes 任何引用"
    - "server/src/utils/date.ts 导出 formatDate 函数"
    - "server/src/sql/cost/earned-premium.ts 从 ../../utils/date.js 导入 formatDate"
  artifacts:
    - path: "server/src/utils/date.ts"
      provides: "通用日期工具函数（formatDate / getLastDayOfMonth）"
      exports: ["formatDate", "getLastDayOfMonth"]
    - path: "server/src/routes/query.ts"
      provides: "不含 coefficient 路由的路由聚合器"
      contains: "kpiRoutes"
  key_links:
    - from: "server/src/sql/cost/earned-premium.ts"
      to: "server/src/utils/date.ts"
      via: "import { formatDate } 迁移后的新路径"
      pattern: "utils/date"
---

<objective>
后端系数监控模块全链路删除 — 按安全顺序：先迁移被其他模块依赖的工具函数（formatDate），再删除系数专属文件，最后清理路由注册。

Purpose: 彻底移除 6 个后端文件（SQL 生成器 + 路由 + 配置 + 测试 + 工具函数），使后端不再存在任何系数监控逻辑，同时保证 earned-premium.ts 的 formatDate 依赖正常工作。
Output: 后端构建零错误，单元测试全部通过，/api/query/coefficient 端点不再存在（curl 返回 404）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-sql/02-CONTEXT.md
@.planning/phases/02-sql/02-01-SUMMARY.md

<interfaces>
<!-- 关键接口信息，执行器直接使用，无需自行探索 -->

**server/src/utils/coefficient-period.ts** 中需要迁移的两个函数（完整实现）：
```typescript
// 格式化日期为 YYYY-MM-DD
export function formatDate(date: Date): string {
  const year = date.getFullYear();
  const month = String(date.getMonth() + 1).padStart(2, '0');
  const day = String(date.getDate()).padStart(2, '0');
  return `${year}-${month}-${day}`;
}

// 获取月份的最后一天
export function getLastDayOfMonth(year: number, month: number): number {
  return new Date(year, month + 1, 0).getDate();
}
```

**server/src/sql/cost/earned-premium.ts** 第 11 行当前 import（需要修改）：
```typescript
import { formatDate } from '../../utils/coefficient-period.js';
```
修改后应为：
```typescript
import { formatDate } from '../../utils/date.js';
```

**server/src/routes/query.ts** 中需要删除的两行：
```typescript
// 第 18 行 - 删除此 import：
import coefficientRoutes from './query/coefficient.js';
// 第 50 行 - 删除此 use：
router.use(coefficientRoutes);
```

**删除文件清单（D-04）**：
1. `server/src/sql/__tests__/coefficient.test.ts`
2. `server/src/sql/coefficient.ts`
3. `server/src/routes/query/coefficient.ts`
4. `server/src/config/coefficient-thresholds.ts`（后端版，与前端版路径不同）
5. `server/src/utils/coefficient-period.ts`

**注意**：`server/src/config/coefficient-thresholds.ts`（后端版）与 `src/shared/config/coefficient-thresholds.ts`（前端版）是两个不同的文件，本计划只删除后端版，前端版在 02-03-PLAN.md 处理。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 迁移 formatDate/getLastDayOfMonth 到 server/src/utils/date.ts</name>
  <read_first>
    - server/src/utils/coefficient-period.ts（提取 formatDate 和 getLastDayOfMonth 的完整实现）
    - server/src/sql/cost/earned-premium.ts（确认第 11 行 import 路径，更新为新路径）
    - server/src/utils/（确认 date.ts 是否已存在，避免覆盖）
  </read_first>
  <files>
    server/src/utils/date.ts,
    server/src/sql/cost/earned-premium.ts
  </files>
  <action>
**步骤 1：创建 server/src/utils/date.ts**

新建文件，内容如下（精确复制函数实现，不引入任何 coefficient 相关依赖）：
```typescript
/**
 * 通用日期工具函数
 *
 * 从 coefficient-period.ts 迁移而来，供 earned-premium.ts 等模块使用。
 */

/**
 * 格式化日期为 YYYY-MM-DD 字符串
 */
export function formatDate(date: Date): string {
  const year = date.getFullYear();
  const month = String(date.getMonth() + 1).padStart(2, '0');
  const day = String(date.getDate()).padStart(2, '0');
  return `${year}-${month}-${day}`;
}

/**
 * 获取月份的最后一天（0-indexed month）
 */
export function getLastDayOfMonth(year: number, month: number): number {
  return new Date(year, month + 1, 0).getDate();
}
```

**步骤 2：修改 server/src/sql/cost/earned-premium.ts 第 11 行**

将：
```typescript
import { formatDate } from '../../utils/coefficient-period.js';
```
改为：
```typescript
import { formatDate } from '../../utils/date.js';
```

不修改文件的任何其他内容。
  </action>
  <verify>
    <automated>bun run test -- --run --reporter=verbose 2>&1 | grep -E "PASS|FAIL|cost|earned" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `cat server/src/utils/date.ts` 输出包含 `export function formatDate` 和 `export function getLastDayOfMonth`
    - `grep "coefficient-period" server/src/sql/cost/earned-premium.ts` 无输出（import 已更新）
    - `grep "utils/date" server/src/sql/cost/earned-premium.ts` 输出包含 `../../utils/date.js`
    - `bun run test -- --run` 中与 cost/earned-premium 相关的测试全部 PASS（无 import 错误）
  </acceptance_criteria>
  <done>formatDate 已从 coefficient-period.ts 迁移到 date.ts；earned-premium.ts 指向新路径；测试通过</done>
</task>

<task type="auto">
  <name>Task 2: 删除后端 5 个系数专属文件并清理路由注册</name>
  <read_first>
    - server/src/routes/query.ts（确认 coefficientRoutes import 在第 18 行，router.use 在第 50 行）
    - server/src/sql/__tests__/coefficient.test.ts（确认文件存在后再删除）
    - server/src/config/coefficient-thresholds.ts（确认是后端版，路径前缀是 server/）
  </read_first>
  <files>
    server/src/routes/query.ts,
    server/src/sql/__tests__/coefficient.test.ts,
    server/src/sql/coefficient.ts,
    server/src/routes/query/coefficient.ts,
    server/src/config/coefficient-thresholds.ts,
    server/src/utils/coefficient-period.ts
  </files>
  <action>
按以下顺序执行（先删测试，再删实现，最后清路由）：

**步骤 1：删除 5 个文件**
依次删除以下文件：
- `server/src/sql/__tests__/coefficient.test.ts`
- `server/src/sql/coefficient.ts`
- `server/src/routes/query/coefficient.ts`
- `server/src/config/coefficient-thresholds.ts`（后端版，路径 `server/src/config/`，非 `src/shared/config/`）
- `server/src/utils/coefficient-period.ts`（Task 1 已完成 formatDate 迁移，此文件现可安全删除）

**步骤 2：编辑 server/src/routes/query.ts**

删除以下两行（不修改其他任何内容）：
```typescript
// 删除此行（第 18 行附近）：
import coefficientRoutes from './query/coefficient.js';

// 删除此行（router.use 部分）：
router.use(coefficientRoutes);
```

删除后 query.ts 应保持其余所有 import 和 router.use 不变，仅这两处被移除。

**步骤 3：验证序列**
按以下顺序执行验证命令：
1. `bun run test -- --run` — 确认 coefficient.test.ts 消失，其余全过
2. `bun run build` — 确认 TypeScript 零报错
3. `bun run governance` — 确认治理检查通过（注意 Pitfall 4：governance 检查 #10 对删除路由的处理，若报错需在 tests/api/client.test.ts 中清理 coefficient 测试）
  </action>
  <verify>
    <automated>bun run test -- --run 2>&1 | tail -5 && bun run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `ls server/src/sql/coefficient.ts` 报 "No such file"（文件已删除）
    - `ls server/src/routes/query/coefficient.ts` 报 "No such file"
    - `ls server/src/config/coefficient-thresholds.ts` 报 "No such file"（后端版）
    - `ls server/src/utils/coefficient-period.ts` 报 "No such file"
    - `ls server/src/sql/__tests__/coefficient.test.ts` 报 "No such file"
    - `grep "coefficientRoutes" server/src/routes/query.ts` 无输出
    - `bun run test -- --run` 退出码为 0（全部通过）
    - `bun run build` 退出码为 0，输出无 "error TS" 字样
    - `bun run governance` 退出码为 0
  </acceptance_criteria>
  <done>后端系数模块 5 个文件已删除；query.ts 中 coefficientRoutes 注册已清除；构建和测试全部通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 删除操作 → 文件系统 | 永久删除，不可恢复，需确认 02-01 黄金基线已建立 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | server/src/routes/query.ts | mitigate | 删除前 read_first 确认行号，Edit 精确删除两行，不影响其他路由注册 |
| T-02-04 | Denial of Service | earned-premium.ts import 断链 | mitigate | Task 1 先迁移 formatDate 并验证测试通过，Task 2 才删除 coefficient-period.ts |
| T-02-05 | Information Disclosure | 后端 coefficient-thresholds.ts 与前端同名 | mitigate | 明确限定删除路径 server/src/config/，不触碰 src/shared/config/ |
</threat_model>

<verification>
- `grep "coefficientRoutes" server/src/routes/query.ts` 零输出
- `ls server/src/sql/coefficient.ts server/src/routes/query/coefficient.ts server/src/config/coefficient-thresholds.ts server/src/utils/coefficient-period.ts server/src/sql/__tests__/coefficient.test.ts` 全部报 "No such file"
- `grep "import.*from.*utils/date" server/src/sql/cost/earned-premium.ts` 有输出（date.ts 引用正常）
- `bun run test -- --run` 退出码 0
- `bun run build` 退出码 0，零 TS 报错
- `bun run governance` 退出码 0
</verification>

<success_criteria>
SQL-02 后端部分完成：
1. 后端系数监控模块 5 个文件已删除（SQL 生成器 + 路由 + 配置 + 测试 + 工具函数）
2. query.ts 中 coefficientRoutes 注册已清除
3. earned-premium.ts 的 formatDate 依赖通过 date.ts 正常工作
4. bun run test + build + governance 三项全部通过
</success_criteria>

<output>
After completion, create `.planning/phases/02-sql/02-02-SUMMARY.md`
</output>
