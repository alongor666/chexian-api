---
phase: 01-security-baseline
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - tests/e2e/verify-org-permissions.spec.ts
autonomous: false
requirements:
  - SEC-02
user_setup: []

must_haves:
  truths:
    - "admin 和 leshan 请求同一 API 端点，各自命中独立快照（X-Snapshot: hit）"
    - "admin 返回的数据量大于 leshan（全量 vs 乐山子集）"
    - "未认证请求不命中任何快照"
    - "E2E 测试自动验证上述三项行为"
  artifacts:
    - path: "tests/e2e/verify-org-permissions.spec.ts"
      provides: "多角色快照隔离 E2E 测试"
      contains: "X-Snapshot"
  key_links:
    - from: "tests/e2e/verify-org-permissions.spec.ts"
      to: "/api/query/dashboard-bundle"
      via: "page.request.get 发送带不同 JWT cookie 的 API 请求"
      pattern: "dashboard-bundle"
    - from: "tests/e2e/verify-org-permissions.spec.ts"
      to: "/api/auth/login"
      via: "loginAs 辅助函数获取 JWT"
      pattern: "loginAs"
---

<objective>
构建权限隔离 E2E 自动化验证（SEC-02）。扩展现有 verify-org-permissions.spec.ts，新增测试场景验证 admin（scope=all）和 leshan（scope=乐山）访问同一 API 端点时命中各自独立的快照文件，且响应数据内容不同。

Purpose: 建立自动化安全回归测试，确保快照层权限隔离在后续重构中不被破坏。
Output: E2E 测试扩展 + checkpoint 验证
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-baseline/01-CONTEXT.md
@.planning/phases/01-security-baseline/01-RESEARCH.md
@.planning/phases/01-security-baseline/01-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From tests/e2e/verify-org-permissions.spec.ts:
```typescript
// L2 — 凭据辅助
import { DEFAULT_E2E_PASSWORD, DEFAULT_E2E_USERNAME } from './helpers/credentials';

// L5 — 独立 session
test.use({ storageState: { cookies: [], origins: [] } });

// L13-L48 — 已有 loginAsUser 辅助函数（admin 角色登录）
async function loginAsUser(page: import('@playwright/test').Page) {
  // 使用 username/password 变量（L10-L11），POST /api/auth/login
  // 成功后注入 cx_access_token cookie
}
```

From tests/e2e/helpers/credentials.ts:
```typescript
export const DEFAULT_E2E_USERNAME = process.env.E2E_USERNAME ?? 'admin';
export const DEFAULT_E2E_PASSWORD = process.env.E2E_PASSWORD ?? (() => {
  throw new Error('E2E_PASSWORD environment variable is required.');
})();
```

From server/src/middleware/snapshot-serve.ts (Plan 01 修复后):
```typescript
// permissionToScope 现在返回 string | null
// snapshotServe 在 scope===null 时直接 next()
// X-Snapshot 响应头值: 'hit' | 'miss' | 'stale' | 'error'
```

From scripts/build-snapshots.mjs:
```javascript
// SCOPE_CREDENTIALS 映射（L29-43）
const SCOPE_CREDENTIALS = {
  all: { username: 'admin', password: 'CxAdmin@2026!' },
  '乐山': { username: 'leshan', password: 'leshan123' },
  // ...其他机构
};
// 使用方法: node scripts/build-snapshots.mjs --scope 乐山
```

凭据参考（来自 CONTEXT.md specifics）：
- admin: username='admin', password='CxAdmin@2026!' (branch_admin, scope=all)
- leshan: username='leshan', password='leshan123' (org_user, scope=乐山)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 E2E 测试 — 多角色快照隔离验证</name>
  <files>tests/e2e/verify-org-permissions.spec.ts</files>
  <read_first>
    - tests/e2e/verify-org-permissions.spec.ts（完整文件，理解 loginAsUser 辅助函数结构、test.skip 模式、现有测试布局）
    - tests/e2e/helpers/credentials.ts（凭据来源）
    - server/src/middleware/snapshot-serve.ts（确认 X-Snapshot 响应头格式 — hit|miss|stale|error，L174-L180）
    - scripts/build-snapshots.mjs（确认 SCOPE_CREDENTIALS 映射 L29-43，理解如何为 org scope 构建快照）
    - .planning/phases/01-security-baseline/01-01-SUMMARY.md（确认 Plan 01 修复已完成）
  </read_first>
  <action>
    **Per D-03 + D-04 + D-05 + D-06**。在现有 verify-org-permissions.spec.ts 中扩展，不新建测试文件。

    **Step 1: 提取通用登录辅助函数**

    当前 `loginAsUser` 函数硬编码使用 `username`/`password` 变量（L10-L11，默认 admin）。需要支持以不同用户登录。

    在文件中新增一个通用辅助函数（放在现有 `loginAsUser` 之后，仍在 `test.describe('Permission Verification')` 块内部）：
    ```typescript
    async function loginAs(page: import('@playwright/test').Page, user: string, pass: string) {
      for (let attempt = 0; attempt < 5; attempt++) {
        if (attempt > 0) await page.waitForTimeout(2000 * attempt);
        const loginResponse = await page.request.post('http://localhost:3000/api/auth/login', {
          data: { username: user, password: pass },
          timeout: 30000,
        });
        if (loginResponse.status() === 200) {
          const loginPayload = await loginResponse.json();
          const accessToken = loginPayload?.data?.token;
          expect(accessToken).toBeTruthy();
          await page.context().addCookies([{
            name: 'cx_access_token',
            value: accessToken,
            url: 'http://localhost',
            httpOnly: true,
            sameSite: 'Lax',
          }]);
          return;
        }
        if (loginResponse.status() !== 429) {
          expect(loginResponse.status()).toBe(200);
        }
      }
      throw new Error(`Login failed for user ${user} after 5 attempts`);
    }
    ```

    **Step 2: 新增快照隔离测试场景**

    在现有 `test.describe('Permission Verification')` 块末尾（现有两个 test 之后），新增以下测试：

    ```typescript
    test('Snapshot isolation: admin and leshan hit different snapshot scopes', async ({ page }) => {
      test.skip(!process.env.CI, '快照隔离 E2E 仅 CI 执行（需 snapshot 预构建）');

      const endpoint = 'http://localhost:3000/api/query/dashboard-bundle?timeView=daily&perspective=premium&rankingLimit=10';

      // 1. admin 登录 -> 请求 dashboard-bundle
      await loginAs(page, 'admin', 'CxAdmin@2026!');
      const adminResponse = await page.request.get(endpoint);
      expect(adminResponse.status()).toBe(200);
      const adminSnapshot = adminResponse.headers()['x-snapshot'];
      const adminData = await adminResponse.json();

      // 清除 session，切换到 leshan
      await page.context().clearCookies();

      // 2. leshan 登录 -> 请求同一端点
      await loginAs(page, 'leshan', 'leshan123');
      const leshanResponse = await page.request.get(endpoint);
      expect(leshanResponse.status()).toBe(200);
      const leshanSnapshot = leshanResponse.headers()['x-snapshot'];
      const leshanData = await leshanResponse.json();

      // 3. 验证隔离
      expect(adminSnapshot).toBe('hit');
      expect(leshanSnapshot).toBe('hit');

      expect(adminData.success).toBe(true);
      expect(leshanData.success).toBe(true);

      // 关键隔离断言：admin 数据不等于 leshan 数据
      expect(JSON.stringify(adminData.data)).not.toBe(JSON.stringify(leshanData.data));
    });

    test('Snapshot isolation: unauthenticated request does not hit snapshot', async ({ page }) => {
      test.skip(!process.env.CI, '快照隔离 E2E 仅 CI 执行');

      const endpoint = 'http://localhost:3000/api/query/dashboard-bundle?timeView=daily&perspective=premium&rankingLimit=10';
      const response = await page.request.get(endpoint);

      // 未认证应返回 401
      expect(response.status()).toBe(401);
    });
    ```

    **不要修改的内容**：
    - 现有的 `loginAsUser` 函数保持不变（已有测试依赖它）
    - 现有的两个测试用例保持不变
    - 不新建测试文件（per D-04）
  </action>
  <verify>
    <automated>cd /Users/alongor666/Downloads/底层数据湖DUD/chexian-api && bun run test:e2e -- tests/e2e/verify-org-permissions.spec.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/verify-org-permissions.spec.ts 包含 `loginAs` 函数（接受 user, pass 参数）
    - tests/e2e/verify-org-permissions.spec.ts 包含 `Snapshot isolation: admin and leshan` 测试描述
    - tests/e2e/verify-org-permissions.spec.ts 包含 `x-snapshot` 响应头检查
    - tests/e2e/verify-org-permissions.spec.ts 包含 `expect(adminSnapshot).toBe('hit')` 断言
    - tests/e2e/verify-org-permissions.spec.ts 包含 `expect(leshanSnapshot).toBe('hit')` 断言
    - tests/e2e/verify-org-permissions.spec.ts 包含 `not.toBe(JSON.stringify(leshanData.data))` 数据差异断言
    - tests/e2e/verify-org-permissions.spec.ts 包含 `unauthenticated request` 测试
    - tests/e2e/verify-org-permissions.spec.ts 包含 `expect(response.status()).toBe(401)` 未认证断言
    - 新增测试使用 `test.skip(!process.env.CI, ...)` 模式（与现有测试一致）
    - `bun run build` 零 TypeScript 错误
  </acceptance_criteria>
  <done>
    E2E 测试文件扩展完成，包含：admin/leshan 快照隔离验证 + 未认证请求 401 验证。CI 环境下可自动运行。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 人工验证 — Phase 1 安全基线端到端确认</name>
  <files>N/A</files>
  <action>
    完成 Phase 1 安全基线全部修复后的人工端到端验证。验证前需要确保：
    - 本地 server 已启动（`bun run dev:full`）
    - 已为乐山 scope 构建快照（`node scripts/build-snapshots.mjs --scope 乐山`）

    验证步骤：
    1. 启动本地 server: `bun run dev:full`
    2. 构建乐山快照: `node scripts/build-snapshots.mjs --scope 乐山`
    3. 验证 admin 快照命中:
       ```bash
       TOKEN=$(curl -s -X POST http://localhost:3000/api/auth/login \
         -H 'Content-Type: application/json' \
         -d '{"username":"admin","password":"CxAdmin@2026!"}' | jq -r '.data.token')
       curl -s -I "http://localhost:3000/api/query/dashboard-bundle?timeView=daily&perspective=premium&rankingLimit=10" \
         -H "Cookie: cx_access_token=$TOKEN" | grep -i x-snapshot
       ```
       期望: `X-Snapshot: hit`

    4. 验证 leshan 快照命中:
       ```bash
       TOKEN=$(curl -s -X POST http://localhost:3000/api/auth/login \
         -H 'Content-Type: application/json' \
         -d '{"username":"leshan","password":"leshan123"}' | jq -r '.data.token')
       curl -s -I "http://localhost:3000/api/query/dashboard-bundle?timeView=daily&perspective=premium&rankingLimit=10" \
         -H "Cookie: cx_access_token=$TOKEN" | grep -i x-snapshot
       ```
       期望: `X-Snapshot: hit`

    5. 验证数据隔离: 用 admin 和 leshan 分别请求同一端点，比较 `data` 字段 — admin 数据应包含所有机构，leshan 仅包含乐山。

    6. 验证未认证请求:
       ```bash
       curl -s -o /dev/null -w '%{http_code}' "http://localhost:3000/api/query/dashboard-bundle"
       ```
       期望: `401`
  </action>
  <verify>人工验证 — 按上述步骤执行 curl 命令，确认输出符合期望</verify>
  <done>
    用户确认以下全部通过：admin X-Snapshot:hit + leshan X-Snapshot:hit（不同 scope）+ 数据内容不同 + 未认证 401。输入 "approved" 确认或描述问题。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> auth middleware | JWT cookie 验证，未认证请求被 401 拦截 |
| auth -> permission -> snapshot | 不同角色生成不同 permissionFilter，映射到不同 scope 目录 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-02 | Information Disclosure | 跨角色快照数据泄漏 | mitigate | E2E 测试自动验证 admin/leshan 返回不同数据集（本 plan 核心验证） |
| T-1-04 | Information Disclosure | 未认证用户绕过 auth 访问快照 | mitigate | E2E 测试验证无 JWT 请求返回 401（auth 中间件在 snapshot 之前执行） |
| T-1-05 | Elevation of Privilege | leshan 用户篡改 cookie 获取 admin scope | accept | JWT 签名验证在 auth 中间件完成，篡改 token 无法通过验证；不在本 phase 范围 |
</threat_model>

<verification>
1. `bun run build` — TypeScript 编译零错误
2. `bun run test` — 全量单元测试通过
3. E2E 测试（CI 环境或本地手动验证）：admin 和 leshan 各自命中独立快照 + 数据内容不同
4. 未认证请求返回 401
</verification>

<success_criteria>
- verify-org-permissions.spec.ts 包含 2 个新增测试场景（快照隔离 + 未认证 401）
- admin 请求 dashboard-bundle -> X-Snapshot: hit
- leshan 请求同一端点 -> X-Snapshot: hit + 数据内容与 admin 不同
- 未认证请求 -> 401
- 全量单元测试 + E2E 测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-baseline/01-02-SUMMARY.md`
</output>
