---
phase: 04-materialization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/services/duckdb.ts
  - server/src/services/duckdb-infra.ts
  - server/src/services/duckdb-parquet-loader.ts
  - server/src/services/duckdb-type-converter.ts
  - server/src/services/duckdb-init-tables.ts
  - server/src/services/data-bootstrapper.ts
  - server/src/services/__tests__/duckdb-derived-tables.test.ts
  - server/src/services/__tests__/duckdb-materialize-batches.test.ts
autonomous: true
requirements:
  - MAT-02

must_haves:
  truths:
    - "duckdb.ts 文件不超过 110 行（wc -l 可验证）"
    - "bun run build 零 TypeScript 报错"
    - "bun run test 原有测试仍然通过"
    - "duckdb.ts 中不存在任何 load*/materialize* 代理方法"
    - "duckdb-parquet-loader.ts、duckdb-type-converter.ts、duckdb-init-tables.ts 三个新文件存在且导出正确符号"
  artifacts:
    - path: "server/src/services/duckdb.ts"
      provides: "DuckDBService 主类 + 单例，≤110 行"
      contains: "export class DuckDBService"
    - path: "server/src/services/duckdb-parquet-loader.ts"
      provides: "Parquet 指纹缓存 + loadMultipleParquet 增量逻辑"
      exports: ["computeParquetFingerprint", "loadMultipleParquet"]
    - path: "server/src/services/duckdb-type-converter.ts"
      provides: "BigInt/DATE/TIMESTAMP 类型转换"
      exports: ["convertBigIntToNumber", "SLOW_QUERY_THRESHOLD_MS"]
    - path: "server/src/services/duckdb-init-tables.ts"
      provides: "init 阶段建表逻辑（KpiPlanConfig/UserAccount/RoleConfig）"
      exports: ["initDuckDBTables"]
    - path: "server/src/services/duckdb-infra.ts"
      provides: "ConnectionPool + QueryCache + 表工具方法"
      exports: ["ConnectionPool", "QueryCache", "dropRelationIfExists", "hasRelation", "getTableSchema"]
  key_links:
    - from: "server/src/services/duckdb.ts"
      to: "server/src/services/duckdb-parquet-loader.ts"
      via: "import { loadMultipleParquet } from './duckdb-parquet-loader.js'"
      pattern: "loadMultipleParquet.*duckdb-parquet-loader"
    - from: "server/src/services/duckdb.ts"
      to: "server/src/services/duckdb-init-tables.ts"
      via: "initDuckDBTables(this) 委托调用"
      pattern: "initDuckDBTables"
    - from: "server/src/services/__tests__/duckdb-materialize-batches.test.ts"
      to: "server/src/services/duckdb-materialization.ts"
      via: "直接 import { materializeInBatches }"
      pattern: "import.*materializeInBatches.*duckdb-materialization"
---

<objective>
MAT-02：将 666 行的 duckdb.ts God File 拆分为 4 个独立职责模块，删除所有 ~17 个代理方法，duckdb.ts 瘦身到 ≤110 行。

Purpose: 消除 God File 反模式，为 Phase 04-02 的惰性加载改造提供干净的接口基础。调用方（data-bootstrapper.ts、测试文件、routes/data.ts）直接 import 子模块，不再通过代理。
Output: 4 个新/改文件（duckdb-parquet-loader.ts、duckdb-type-converter.ts、duckdb-init-tables.ts、duckdb-infra.ts 扩展）+ 主类瘦身 + 2 个测试文件修复。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-materialization/04-CONTEXT.md
@.planning/phases/04-materialization/04-RESEARCH.md

<!-- 核心源文件 -->
@server/src/services/duckdb.ts
@server/src/services/duckdb-infra.ts
@server/src/services/duckdb-types.ts
@server/src/services/duckdb-materialization.ts
@server/src/services/data-bootstrapper.ts
@server/src/routes/data.ts

<interfaces>
<!-- 从 duckdb.ts 提取的关键接口，执行时无需重新读取 -->

From server/src/services/duckdb-types.ts (完整文件):
```typescript
export interface DuckDBQueryable {
  query<T = any>(sql: string, cacheTtlMs?: number): Promise<T[]>;
  getTableSchema(tableName: string): Promise<any[]>;
  hasRelation(relationName: string): Promise<boolean>;
  dropRelationIfExists(relationName: string): Promise<void>;
  invalidateCache(options?: { silent?: boolean }): void;
}
```

From server/src/services/duckdb.ts — 接口声明（DuckDBServiceConfig, L75-84）:
```typescript
export interface DuckDBServiceConfig {
  path?: string;
  maxConnections?: number;
  maxMemory?: string;
  threads?: number;
}
```

From server/src/services/duckdb.ts — 代理方法列表（L543-625，全部待删除）:
- materialization 代理: createPolicyFactView, materializeInBatches, dropAllDerivedTables, ensureAggregatesReady
- domainLoaders 代理: loadDimParquet, loadPlateRegionDim, loadTeamMapping, buildAchievementView,
  loadQuoteConversion, loadClaimsDetail, loadClaimsBulk, loadClaimsAgg, createClaimsAggFromDetail,
  loadCrossSell, loadRepairDim, loadBrandDim, loadCustomerFlow, loadRenewalUniverse

From server/src/services/data-bootstrapper.ts — BootstrapDuckDB 接口（L55-75），拆分后需缩减:
```typescript
export interface BootstrapDuckDB {
  loadParquet(filePath: string, tableName: string): Promise<void>;
  loadMultipleParquet(filePaths: string[]): Promise<{ totalRows: number }>;
  createPolicyFactView(sourceTable?: string): Promise<void>;  // 移除，直接 import
  query<T = any>(sql: string, cacheTtlMs?: number): Promise<T[]>;
  loadDimParquet(salesmanPath: string, planPath: string): Promise<void>;  // 移除
  loadTeamMapping(jsonFilePath: string): Promise<void>;  // 移除
  // ... 其余 load* 方法全部移除，bootstrapper 直接 import 子模块
}
```

From server/src/routes/data.ts — 直接调用 duckdbService 的代理方法（L404, L412, L586, L680）:
- duckdbService.loadMultipleParquet(...)  → 保留（loadMultipleParquet 委托到 parquet-loader 后，接口不变）
- duckdbService.createPolicyFactView(...)  → 改为 import createPolicyFactView from duckdb-materialization
- duckdbService.dropAllDerivedTables()    → 改为 import dropAllDerivedTables from duckdb-materialization
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建三个新拆分文件（duckdb-parquet-loader.ts、duckdb-type-converter.ts、duckdb-init-tables.ts）</name>
  <read_first>
    - server/src/services/duckdb.ts（从中提取 L30-67: 指纹缓存类型+computeParquetFingerprint；L330-366: convertBigIntToNumber；L143-222: init 建表逻辑）
    - server/src/services/duckdb-types.ts（DuckDBQueryable 接口，新文件的 db 参数类型）
    - server/src/config/paths.ts（getKpiPlanConfigPath，init-tables 需要）
    - server/src/utils/security.ts（escapeSqlValue，两个文件都需要）
  </read_first>
  <behavior>
    - duckdb-parquet-loader.ts: computeParquetFingerprint 接受 string[] 返回 FingerprintResult|null；loadMultipleParquet 接受 (db: DuckDBQueryable, filePaths: string[])，返回 Promise&lt;{totalRows: number}&gt;
    - duckdb-type-converter.ts: convertBigIntToNumber 是纯函数接受 any 返回 any（不再是 class 方法，改为独立 export function）；导出 SLOW_QUERY_THRESHOLD_MS = 3000
    - duckdb-init-tables.ts: initDuckDBTables 接受 (db: DuckDBQueryable) 返回 Promise&lt;void&gt;，内部包含 KpiPlanConfig + UserAccount + RoleConfig 建表 + KpiPlanConfig 数据加载
  </behavior>
  <files>
    server/src/services/duckdb-parquet-loader.ts,
    server/src/services/duckdb-type-converter.ts,
    server/src/services/duckdb-init-tables.ts
  </files>
  <action>
  **duckdb-parquet-loader.ts（新建，约 140 行）：**
  从 duckdb.ts 迁移 L30-67（指纹缓存相关）和 L436-537（loadMultipleParquet 完整实现）。
  - 保留 `interface ParquetCacheEntry`、`interface FingerprintResult`、`const parquetFingerprintCache`
  - `computeParquetFingerprint(filePaths: string[]): FingerprintResult | null` — 直接迁移，保持原逻辑
  - `export async function loadMultipleParquet(db: DuckDBQueryable, filePaths: string[]): Promise<{ totalRows: number }>` — 将原方法中所有 `this.hasRelation`/`this.query`/`this.dropRelationIfExists`/`this.invalidateCache` 替换为 `db.hasRelation`/`db.query`/`db.dropRelationIfExists`/`db.invalidateCache`
  - 导入头：`import { createHash } from 'crypto'; import { statSync } from 'fs'; import type { DuckDBQueryable } from './duckdb-types.js'; import { escapeSqlValue } from '../utils/security.js'; import { AppError } from '../middleware/error.js';`

  **duckdb-type-converter.ts（新建，约 45 行）：**
  从 duckdb.ts 迁移 L67（SLOW_QUERY_THRESHOLD_MS 常量）和 L330-366（convertBigIntToNumber 方法体）。
  - `export const SLOW_QUERY_THRESHOLD_MS = 3000;`
  - `export function convertBigIntToNumber(data: any): any` — 完整迁移逻辑，改为独立函数（去掉 `private` 关键字，`this.convertBigIntToNumber` 递归调用改为直接调用 `convertBigIntToNumber`）

  **duckdb-init-tables.ts（新建，约 90 行）：**
  从 duckdb.ts 迁移 L143-222（init 中的三张表建表逻辑 + KpiPlanConfig 数据加载）。
  - `export async function initDuckDBTables(db: DuckDBQueryable): Promise<void>` — 迁移完整建表逻辑，将所有 `this.query` 替换为 `db.query`
  - 导入头：`import type { DuckDBQueryable } from './duckdb-types.js'; import { escapeSqlValue } from '../utils/security.js'; import { getKpiPlanConfigPath } from '../config/paths.js';`

  **验证后处理（D-01 per CONTEXT.md）：** 三个文件创建后运行 `bun run build`，TypeScript 必须零报错。
  </action>
  <verify>
    <automated>bun run build 2>&1 | grep -E "error TS|found 0 errors" | head -5</automated>
  </verify>
  <done>
    - server/src/services/duckdb-parquet-loader.ts 存在，包含 `export async function loadMultipleParquet`
    - server/src/services/duckdb-type-converter.ts 存在，包含 `export function convertBigIntToNumber` 和 `export const SLOW_QUERY_THRESHOLD_MS`
    - server/src/services/duckdb-init-tables.ts 存在，包含 `export async function initDuckDBTables`
    - bun run build 零 TypeScript 报错
  </done>
</task>

<task type="auto">
  <name>Task 2: 瘦身 duckdb.ts — 删除代理方法，委托到子模块，扩展 duckdb-infra.ts</name>
  <read_first>
    - server/src/services/duckdb.ts（完整文件，当前 666 行）
    - server/src/services/duckdb-infra.ts（当前 129 行，将迁移入表工具方法）
    - server/src/services/duckdb-parquet-loader.ts（Task 1 创建，确认导出）
    - server/src/services/duckdb-type-converter.ts（Task 1 创建，确认导出）
    - server/src/services/duckdb-init-tables.ts（Task 1 创建，确认导出）
  </read_first>
  <files>
    server/src/services/duckdb.ts,
    server/src/services/duckdb-infra.ts
  </files>
  <action>
  **步骤 1：扩展 duckdb-infra.ts（迁移表工具方法，+~50 行，目标约 180 行）：**
  将 duckdb.ts 中的表/视图工具方法（L375-414）迁移到 duckdb-infra.ts 末尾：
  - `dropRelationIfExists(db: DuckDBQueryable, relationName: string): Promise<void>` — 迁移逻辑，将 `this.query`/`this.sanitize` 替换为 `db.query` + 直接引用 `sanitizeTableName`/`escapeSqlValue`
  - `hasRelation(db: DuckDBQueryable, relationName: string): Promise<boolean>` — 同上迁移
  - `getTableSchema(db: DuckDBQueryable, tableName: string): Promise<any[]>` — 同上迁移
  在 duckdb-infra.ts 顶部添加：`import { sanitizeTableName, escapeSqlValue } from '../utils/security.js';` 和 `import type { DuckDBQueryable } from './duckdb-types.js';`

  **步骤 2：重写 duckdb.ts（目标 ≤110 行，per D-01/D-02/D-03）：**
  完整重写 duckdb.ts 为精简版本。保留方法（per D-03）：
  - `constructor(config?: DuckDBServiceConfig)`
  - `async init(): Promise<void>` — 委托 `initDuckDBTables(this)` 处理建表逻辑；仅保留 DuckDBInstance.create + ConnectionPool 初始化 + SET 内存/线程
  - `async query<T = any>(sql: string, cacheTtlMs = 0): Promise<T[]>` — 保留完整逻辑（慢查询监控用 SLOW_QUERY_THRESHOLD_MS from duckdb-type-converter；类型转换用 convertBigIntToNumber from duckdb-type-converter）；将 `getConnection/releaseConnection` 私有方法内联（直接使用 `this.connectionPool.acquire()` / `this.connectionPool.release(conn)`）
  - `invalidateCache(options?): void` — 保留原逻辑
  - `get cacheSize(): number`
  - `async loadParquet(filePath, tableName): Promise<void>` — 保留（单文件加载，per D-03）；内部调用 `dropRelationIfExists(this, tableName)` from duckdb-infra（或直接写 SQL）
  - `loadMultipleParquet(filePaths): Promise<{totalRows: number}>` — 1 行委托：`return loadMultipleParquet(this, filePaths);`
  - `async hasRelation(name): Promise<boolean>` — 1 行委托：`return hasRelation(this, name);`（注：DuckDBQueryable 接口要求此方法存在）
  - `async dropRelationIfExists(name): Promise<void>` — 1 行委托：`return dropRelationIfExists(this, name);`
  - `async getTableSchema(name): Promise<any[]>` — 1 行委托：`return getTableSchema(this, name);`
  - `async close(): Promise<void>`

  **删除（per D-02）：** 所有 load*/materialize* 代理方法（14 + 3 = 17 个），`DuckDBServiceConfig` interface（移到 duckdb-types.ts，重新 export），`getConnection`/`releaseConnection` 私有方法（内联到 query），`convertBigIntToNumber` 私有方法（用 import），Parquet 指纹缓存类型和变量（已在 duckdb-parquet-loader.ts）

  **导入（新 duckdb.ts 顶部）：**
  ```typescript
  import { DuckDBInstance } from '@duckdb/node-api';
  import type { DuckDBConnection } from '@duckdb/node-api';
  import { databaseConfig, DUCKDB_INIT_OPTIONS } from '../config/database.js';
  import { AppError } from '../middleware/error.js';
  import { sanitizeTableName, escapeSqlValue } from '../utils/security.js';
  import { recordQueryMetric } from '../utils/request-context.js';
  import { QueryCache, ConnectionPool, dropRelationIfExists, hasRelation, getTableSchema } from './duckdb-infra.js';
  import type { DuckDBQueryable } from './duckdb-types.js';
  import { initDuckDBTables } from './duckdb-init-tables.js';
  import { convertBigIntToNumber, SLOW_QUERY_THRESHOLD_MS } from './duckdb-type-converter.js';
  import { loadMultipleParquet } from './duckdb-parquet-loader.js';
  import { clearRouteCache } from './route-cache.js';
  import { invalidateSnapshotPathCache } from '../middleware/snapshot-serve.js';
  export type { DuckDBServiceConfig } from './duckdb-types.js';  // 若移至 duckdb-types.ts
  ```

  注意：`DuckDBServiceConfig` 可留在 duckdb.ts 末尾（节省 re-export 行数），重点是行数达标。

  **步骤 3：修改 data-bootstrapper.ts 中的 BootstrapDuckDB 接口（per D-02）：**
  将 BootstrapDuckDB 接口缩减为 5 个核心方法（仅保留 DuckDBService 主类实际有的方法）：
  ```typescript
  export interface BootstrapDuckDB {
    loadParquet(filePath: string, tableName: string): Promise<void>;
    loadMultipleParquet(filePaths: string[]): Promise<{ totalRows: number }>;
    query<T = any>(sql: string, cacheTtlMs?: number): Promise<T[]>;
    hasRelation(relationName: string): Promise<boolean>;
    dropRelationIfExists(relationName: string): Promise<void>;
  }
  ```
  在 data-bootstrapper.ts 文件顶部添加 imports：
  ```typescript
  import * as materialization from './duckdb-materialization.js';
  import * as domainLoaders from './duckdb-domain-loaders.js';
  ```
  将 bootstrapper 内部所有 `this.db.createPolicyFactView(...)` 改为 `materialization.createPolicyFactView(this.db, ...)`
  将 `this.db.loadDimParquet(...)` 改为 `domainLoaders.loadDimParquet(this.db, ...)`
  将 `this.db.loadTeamMapping(...)` 改为 `domainLoaders.loadTeamMapping(this.db, ...)`
  将 Stage 11（loadAuxiliaryDomains）中所有 `this.db.load*`/`this.db.create*` 改为对应 domainLoaders 函数调用

  验证顺序（per Pitfall 4 in RESEARCH.md）：先改 bootstrapper 内部调用，再删接口方法声明，最后删主类代理方法。
  </action>
  <verify>
    <automated>wc -l /Users/alongor666/Downloads/底层数据湖DUD/chexian-api/server/src/services/duckdb.ts && bun run build 2>&1 | grep -c "error TS"</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l server/src/services/duckdb.ts` 输出数字 ≤ 110
    - `bun run build` 输出中 "error TS" 出现次数为 0（即 `grep -c "error TS"` 返回 0）
    - `grep -n "async load\|async materialize\|async createPolicy\|async createCross\|async buildAchieve" server/src/services/duckdb.ts` 无匹配（代理方法已删除）
    - `grep -n "dropRelationIfExists\|hasRelation\|getTableSchema" server/src/services/duckdb-infra.ts` 有匹配（表工具方法已迁移）
    - `grep -n "initDuckDBTables" server/src/services/duckdb.ts` 有匹配（委托调用存在）
  </acceptance_criteria>
  <done>
    duckdb.ts ≤110 行，零代理方法，bun run build 零报错，data-bootstrapper.ts BootstrapDuckDB 接口缩减为 5 个方法
  </done>
</task>

<task type="auto">
  <name>Task 3: 修复测试文件 + 修复 routes/data.ts 直接调用代理方法的位置</name>
  <read_first>
    - server/src/services/__tests__/duckdb-derived-tables.test.ts（调用 duckdbService.dropAllDerivedTables，需修改）
    - server/src/services/__tests__/duckdb-materialize-batches.test.ts（调用 duckdbService.materializeInBatches，需修改）
    - server/src/routes/data.ts（L404 loadMultipleParquet / L412 createPolicyFactView / L586 dropAllDerivedTables / L680 createPolicyFactView，需修改最后 3 处）
    - server/src/services/duckdb-materialization.ts（确认 materializeInBatches 和 dropAllDerivedTables 的函数签名）
  </read_first>
  <files>
    server/src/services/__tests__/duckdb-derived-tables.test.ts,
    server/src/services/__tests__/duckdb-materialize-batches.test.ts,
    server/src/routes/data.ts
  </files>
  <action>
  **duckdb-derived-tables.test.ts（per Pitfall 3 in RESEARCH.md）：**
  在文件顶部添加导入：`import { dropAllDerivedTables } from '../duckdb-materialization.js';`
  将所有 `duckdbService.dropAllDerivedTables()` 替换为 `dropAllDerivedTables(duckdbService)`
  共 6 处（L28, L34, L46, L59, L73 附近）。

  **duckdb-materialize-batches.test.ts（per Pitfall 3 in RESEARCH.md）：**
  在文件顶部添加导入：`import { materializeInBatches } from '../duckdb-materialization.js';`
  将所有 `await duckdbService.materializeInBatches(...)` 替换为 `await materializeInBatches(duckdbService, ...)`（第一个参数改为 duckdbService）。
  共约 9 处（L47, L59, L73, L87, L100, L111, L122, L134, L144 附近）。

  **routes/data.ts（3 处非 loadMultipleParquet 的调用）：**
  在 data.ts 顶部添加导入：`import { createPolicyFactView, dropAllDerivedTables } from '../services/duckdb-materialization.js';`
  - L412 `duckdbService.createPolicyFactView('raw_parquet')` → `createPolicyFactView(duckdbService, 'raw_parquet')`
  - L586 `duckdbService.dropAllDerivedTables()` → `dropAllDerivedTables(duckdbService)`
  - L680 `duckdbService.createPolicyFactView('raw_parquet')` → `createPolicyFactView(duckdbService, 'raw_parquet')`
  注意：L404 `duckdbService.loadMultipleParquet(...)` 保留不变（loadMultipleParquet 仍是 DuckDBService 的公共方法，1 行委托）。

  运行完整测试：`bun run test` 确认所有单元测试通过。
  </action>
  <verify>
    <automated>bun run test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "duckdbService\.dropAllDerivedTables\|duckdbService\.materializeInBatches\|duckdbService\.createPolicyFactView" server/src/services/__tests__/duckdb-derived-tables.test.ts server/src/services/__tests__/duckdb-materialize-batches.test.ts server/src/routes/data.ts` 无匹配（所有代理调用已替换）
    - `grep -n "import.*materializeInBatches.*duckdb-materialization" server/src/services/__tests__/duckdb-materialize-batches.test.ts` 有匹配（直接导入子模块）
    - `grep -n "import.*dropAllDerivedTables.*duckdb-materialization" server/src/services/__tests__/duckdb-derived-tables.test.ts` 有匹配
    - `bun run test` 所有测试通过，输出包含 "passed" 且不含 "failed"
    - `bun run build` 零报错
  </acceptance_criteria>
  <done>
    两个测试文件直接调用 duckdb-materialization 子模块，routes/data.ts 的 createPolicyFactView/dropAllDerivedTables 直接调用，bun run test 全绿
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 模块边界 | 代理方法删除后，调用方直接引用子模块——import 路径错误会在编译期静态捕获 |
| 测试隔离 | 集成测试文件调用被删方法——编译/运行时 "is not a function" 会立即暴露 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | data-bootstrapper.ts BootstrapDuckDB 接口 | mitigate | 删除接口方法前先修改 bootstrapper 内部调用（per Pitfall 4）；bun run build 验证 TS 零报错 |
| T-04-02 | Denial of Service | convertBigIntToNumber 从实例方法改为独立函数 | mitigate | 函数签名保持一致（接受 any 返回 any），递归调用从 `this.convertBigIntToNumber` 改为直接调用自身，有单元测试覆盖 |
| T-04-03 | Information Disclosure | routes/data.ts 直接调用代理方法 | mitigate | 替换为直接 import 子模块，行为不变，接口更清晰 |
| T-04-04 | Elevation of Privilege | N/A（纯结构重构，无权限逻辑变更） | accept | 本计划不修改任何 auth/permission 相关代码 |
</threat_model>

<verification>
运行顺序：

1. `wc -l server/src/services/duckdb.ts` — 必须 ≤ 110
2. `bun run build` — 必须零 TypeScript 报错
3. `bun run test` — 单元测试全绿
4. `grep -n "async load\|async materialize\|async createPolicy\|async createCross\|async buildAchieve" server/src/services/duckdb.ts` — 无输出（代理方法已删除）
5. `grep "export" server/src/services/duckdb-parquet-loader.ts server/src/services/duckdb-type-converter.ts server/src/services/duckdb-init-tables.ts` — 必须包含 loadMultipleParquet、convertBigIntToNumber、SLOW_QUERY_THRESHOLD_MS、initDuckDBTables
</verification>

<success_criteria>
- duckdb.ts 文件行数 ≤ 110（wc -l 可验证）
- bun run build 零 TypeScript 报错
- bun run test 所有单元测试通过
- duckdb.ts 中不存在任何 load*/materialize*/createPolicy*/createCross*/buildAchieve* 方法（代理全删除）
- 三个新文件存在，各自导出正确符号
- duckdb-infra.ts 包含 dropRelationIfExists/hasRelation/getTableSchema 函数
- data-bootstrapper.ts BootstrapDuckDB 接口缩减为 5 个方法
- routes/data.ts 中 createPolicyFactView 和 dropAllDerivedTables 直接调用 duckdb-materialization 子模块
</success_criteria>

<output>
完成后创建 `.planning/phases/04-materialization/04-01-SUMMARY.md`，记录：
- 实际行数（wc -l 输出）
- 拆分的 4 个文件（各自大小）
- 修改的 3 个调用方文件
- 测试通过状态
- 任何偏离计划的情况
</output>
