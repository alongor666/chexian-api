---
phase: 03-code-structure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - vite.config.ts
  - package.json
  - .planning/phases/03-code-structure/bundle-baseline.html
autonomous: true
requirements:
  - FE-01
  - FE-02
  - FE-03
user_setup: []

must_haves:
  truths:
    - "rollup-plugin-visualizer 基线报告存档，各 chunk 大小可查"
    - "vite-plugin-compression2 替换成功，bun run build 零警告"
    - "brotli/gzip 压缩产物正常生成"
    - "ECharts chunk 与主 bundle 分离状态已确认并记录"
  artifacts:
    - path: "vite.config.ts"
      provides: "Updated Vite config with visualizer + compression2"
      contains: "vite-plugin-compression2"
    - path: ".planning/phases/03-code-structure/bundle-baseline.html"
      provides: "Visualizer baseline report"
      min_lines: 10
    - path: "package.json"
      provides: "Updated dependencies"
      contains: "rollup-plugin-visualizer"
  key_links:
    - from: "vite.config.ts"
      to: "package.json"
      via: "plugin imports"
      pattern: "vite-plugin-compression2"
---

<objective>
前端构建优化：安装 bundle 分析工具、替换过时压缩插件、验证 ECharts 懒加载状态。

Purpose: 建立包体基线可观测性，替换停更 4 年的压缩插件为活跃维护版本，确认图表库不在首屏关键路径。
Output: bundle-baseline.html 基线报告 + vite-plugin-compression2 替换完成 + ECharts 加载状态结论
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-code-structure/03-CONTEXT.md

<interfaces>
<!-- Current vite.config.ts plugin section -->
From vite.config.ts:
```typescript
import viteCompression from 'vite-plugin-compression'

plugins: [
  react(),
  viteCompression({ algorithm: 'gzip', ext: '.gz', threshold: 1024 }),
  viteCompression({ algorithm: 'brotliCompress', ext: '.br', threshold: 1024 }),
],
```

<!-- Current manualChunks config -->
```typescript
manualChunks: {
  'vendor-react': ['react', 'react-dom', 'react-router-dom'],
  'vendor-echarts': ['echarts', 'echarts-for-react'],
  'vendor-data': ['date-fns', 'exceljs'],
  'vendor-export': ['jspdf', 'html2canvas'],
  'vendor-ui': ['lucide-react'],
},
```

<!-- Current dependency -->
package.json: "vite-plugin-compression": "^0.5.1"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 安装 visualizer + 替换压缩插件 + 生成基线报告 (per D-04, D-05)</name>
  <read_first>
    - vite.config.ts
    - package.json
  </read_first>
  <files>
    vite.config.ts,
    package.json
  </files>
  <action>
    1. 安装新依赖、卸载旧依赖：
       ```bash
       bun add -D rollup-plugin-visualizer vite-plugin-compression2
       bun remove vite-plugin-compression
       ```

    2. 更新 `vite.config.ts`：

       a) 替换 import 语句：
          - 删除：`import viteCompression from 'vite-plugin-compression'`
          - 添加：`import { compression } from 'vite-plugin-compression2'`
          - 添加：`import { visualizer } from 'rollup-plugin-visualizer'`

       b) 替换 plugins 数组中的压缩插件配置：
          - 删除两行旧 `viteCompression(...)` 调用
          - 添加 `vite-plugin-compression2` 的 gzip 和 brotli 配置：
            ```typescript
            compression({ algorithm: 'gzip', ext: '.gz', threshold: 1024 }),
            compression({ algorithm: 'brotliCompress', ext: '.br', threshold: 1024 }),
            ```

       c) 添加 visualizer 插件（仅在 build 时激活，输出到 .planning 目录）：
          ```typescript
          visualizer({
            filename: '.planning/phases/03-code-structure/bundle-baseline.html',
            open: false,
            gzipSize: true,
            brotliSize: true,
          }),
          ```
          注意：visualizer 应放在 plugins 数组最后，因为它是分析型插件。

    3. 运行 `bun run build`，确认：
       - 零 TS 错误
       - 零警告（特别是无 vite-plugin-compression 相关废弃警告）
       - dist/ 目录下生成 .gz 和 .br 文件

    4. 确认 `.planning/phases/03-code-structure/bundle-baseline.html` 已生成。

    5. 验证 ECharts chunk 分离状态 (per D-06)：
       - 在 build 输出中检查 `vendor-echarts` chunk 是否存在且独立
       - 在 bundle-baseline.html 中确认 ECharts 相关模块都打包在 `vendor-echarts` chunk
       - 当前 `manualChunks` 已配置 `'vendor-echarts': ['echarts', 'echarts-for-react']`，理论上 ECharts 已按需加载（作为独立 chunk，仅在使用图表的页面加载）
       - 记录结论：ECharts 是否已与主 bundle 分离、chunk 大小

    6. 在 SUMMARY 中记录 ECharts 懒加载验证结论。若 visualizer 报告显示 ECharts chunk 确实独立（不在 index.js 主入口中），则 FE-03 已满足，无需额外 React.lazy 改造。
  </action>
  <verify>
    <automated>cd /Users/alongor666/Downloads/底层数据湖DUD/chexian-api && bun run build && ls dist/assets/*.gz | head -3 && ls dist/assets/*.br | head -3 && test -f .planning/phases/03-code-structure/bundle-baseline.html && echo "BASELINE_EXISTS"</automated>
  </verify>
  <acceptance_criteria>
    - vite.config.ts 包含 `from 'vite-plugin-compression2'`
    - vite.config.ts 包含 `from 'rollup-plugin-visualizer'`
    - vite.config.ts 不包含 `from 'vite-plugin-compression'`（旧插件已移除）
    - package.json 包含 `rollup-plugin-visualizer`
    - package.json 包含 `vite-plugin-compression2`
    - package.json 不包含 `"vite-plugin-compression":` （旧依赖已移除）
    - `bun run build` 零错误零警告
    - dist/assets/ 下存在 .gz 和 .br 文件
    - .planning/phases/03-code-structure/bundle-baseline.html 存在且大于 1KB
    - build 输出中存在 `vendor-echarts` chunk
  </acceptance_criteria>
  <done>
    - rollup-plugin-visualizer 已安装，bundle-baseline.html 基线报告存档
    - vite-plugin-compression2 替换完成，gzip/brotli 产物正常
    - ECharts 懒加载状态已验证并记录
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | 构建工具链变更，不涉及运行时安全边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | T (Tampering) | 第三方 npm 包 | accept | rollup-plugin-visualizer (5M+ weekly downloads) 和 vite-plugin-compression2 (30K+ weekly downloads) 均为活跃维护的主流构建工具，仅在 devDependencies 中，不影响运行时代码。`bun run build` 验证产物完整性。 |
</threat_model>

<verification>
1. `bun run build` — 零错误零警告
2. `ls dist/assets/*.gz | wc -l` — 至少 5 个 gzip 文件
3. `ls dist/assets/*.br | wc -l` — 至少 5 个 brotli 文件
4. `test -f .planning/phases/03-code-structure/bundle-baseline.html` — 基线报告存在
5. `grep "vendor-echarts" dist/assets/*.js` 或 build 输出确认 ECharts chunk 独立
</verification>

<success_criteria>
- Bundle 基线报告已存档，各 chunk 大小可查
- 压缩插件替换为 vite-plugin-compression2，零构建警告
- ECharts chunk 分离状态已确认（独立 chunk = 按需加载生效）
</success_criteria>

<output>
After completion, create `.planning/phases/03-code-structure/03-02-SUMMARY.md`
</output>
