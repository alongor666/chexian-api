# chexian-api 架构增量巡检 · 2026-07-08

> 本次为**首次运行**——`开发文档/reviews/架构巡检/` 目录此前不存在，无历史报告可比对，本文档同时作为后续巡检的基线。

## 结论先行

三项确定性证据（治理校验、类型检查、单元测试）**全部通过**，代码库处于健康状态。对六个候选架构风险方向做独立复核后，**未发现任何未被现有 BACKLOG 覆盖的新增架构债务**——本仓库 2026-07-04 已做过一轮全量"架构价值审计"，把活跃任务按五分类（净简化/根治性简化/必要复杂/低回报复杂化/纯装饰）逐一裁定，本次巡检验证了那轮裁定至今仍然成立。因此本次**不新增 B1xx 条目**，仅对两个证据有更新的既有条目追加了复核记录（见"BACKLOG 变更"节）。

Top 3 优先方向因此从"新发现问题"改为"证据支持、值得下阶段关注的既有条目检查点"——三者均已在 BACKLOG 登记，本报告的价值是给出独立验证证据与时间窗提醒。

---

## 一、确定性证据

| 检查 | 命令 | 结果 |
|------|------|------|
| 治理校验 | `bun run governance` | **52/52 通过**（初次运行 4 项失败，见下方环境说明；补装依赖后复跑全绿）|
| 类型检查 | `bun run typecheck` | **通过，0 错误** |
| 单元测试 | `bun run test --run` | **327 文件 / 4744 用例通过，25 跳过，0 失败**，耗时 79.55s |

### 环境说明（非代码问题）

本次远程执行环境是全新容器，`node_modules` 未预装，首次跑 `bun run governance` 时 4 项检查因 `typescript`/`@duckdb/node-api` 缺失而报错（AST 扫描类检查 3 项 + RouteCatalog 参数契约 1 项）。执行 `bun install`（根目录）与 `cd server && bun install` 补齐依赖后复跑，52 项全部通过。**这是环境初始化缺口，不是代码或架构问题**，记录在此仅为可复现说明，无需登记 BACKLOG。

### 巡检范围局限

本仓库为浅克隆，`git log` 仅可见 56 个提交，时间跨度 2026-07-05 ~ 2026-07-08（今日）。无法用 `git log` 精确核实"自上次巡检以来"的变更范围（因无上次巡检），也无法对"新增目录是否补齐 INDEX.md"做基于提交历史的精确判定（改用目录快照核实，见下文六）。

---

## 二、六个候选方向的独立复核结果

逐项列出复核结论；标注"已覆盖"的均已在 BACKLOG 有对应条目，不重复登记。

### 1. 多省（SC/SX）隔离架构成熟度 —— 已覆盖，健康推进中

**证据**：`server/src/config/sql-federation-policy.ts`（302 行）已建立完整 fail-closed 省份解析层：`BRANCH_CODE_RE` 白名单校验、`resolveBranchCode()`（非法/缺省告警回退）、`assertBranchCodeSet()`（强身份场景抛错）、`FEDERATED_REGISTRY`（逐关系声明实测权限列）。全仓扫描确认生产代码中 `?? 'SC'` 静默默认已收敛为 **5 处结构化 `MAP[x ?? 'SC'] ?? MAP.SC` 形态**（`server/src/sql/kpi-detail.ts:53`、`src/components/layout/SidebarUserPanel.tsx:56`、`src/features/auth/UserLoginPanel.tsx:41`、`src/features/dashboard/CrossSellOrgTrendChart.tsx:200`、`src/features/filters/AdvancedFilterPanel.tsx:216`），非散落裸硬编码。

**对应 BACKLOG**：P1 `2026-06-23-claude-801409`（Phase B 隔离层根治）+ P1 `2026-06-29-claude-a5aa03`（分省隔离四道纵深防线根治）+ P3 `2026-07-07-claude-e80304`（前后端 BRANCH 映射镜像无自动化闸）。本次已对 e80304 追加复核 note（5 处清单已落笔，供下次实施时直接引用）。

### 2. 立方体双轨切流成熟度 —— 已覆盖，时间盒临近

**证据**：`server/ecosystem.config.cjs:48` 仅设 `CUBE_SHADOW_COMPARE: 'true'`，全文无 `CUBE_ROUTING_ENABLED`——趋势/增长/业务员立方体仍停在影子对账阶段，未切流。

**对应 BACKLOG**：P1 `2026-06-20-claude-f1c991`（趋势/增长/业务员立方体首批切流），2026-07-04 审计已精确诊断根因（PM2 reload 打断影子样本累积，样本量未达 1000 门槛）并设定**时间盒 2026-08-03**——届时样本仍不达标则整套灰度机制（shadow/promote/burnin/sentinel）进入投入产出重估。距今不足一个月，本次已对该条追加复核 note，提醒下次巡检前先跑 `scripts/release/cube-promote.mjs` 核对 `canAdvance`，避免时间盒被静默滑过。**这是本轮巡检中证据最支持"近期需要一次主动检查"的既有条目**，非新增债务。

### 3. SQL 生成器目录体量 —— 不紧急

**证据**：`server/src/sql/` 下最大文件 `claims-detail.ts` 770 行，其后依次 `performance-analysis/shared.ts` 595 行、`claims-heatmap.ts` 581 行、`repair.ts` 520 行。仅 1 个文件越过 500 行门槛，无生成器巨石文件危机。与已登记 B306（DuckDB 性能高危三件套）属不同维度——B306 是执行计划问题，本项是体量问题，暂不构成独立登记理由。

### 4. 前端 `features/` 模块体量与耦合 —— 已覆盖，owner 已明确冻结

**证据**：`bun run governance` 的"前端分层依赖边界"检查报 **0 违规**（扫描 440 文件），跨层耦合干净。单文件体量方面，超 800 行的组件有 4 个：`GeoRiskPanel.tsx` 978 行、`CrossSellAnalysisPanel.tsx` 903 行、`NewEarnedPremiumTable.tsx` 891 行、`PerformanceAnalysisPanel.tsx` 877 行。

**对应 BACKLOG**：P2 `2026-07-03-claude-05dff4`（前端审查中危债打包）第 ⑦ 项已列出这批文件，2026-07-04 审计已明确裁定**冻结**："无故障纯整洁，动了反有回归险，触发条件=该文件因其他需求被改时顺手做"。本次复核确认该判断依据（0 层级违规、纯体量非架构问题）至今仍然成立，维持冻结，不新增登记。

### 5. 测试套件架构健康度 —— 健康，不紧急

**证据**：`vite.config.ts` 的 `test.exclude` 共 8 条，全部对应原生模块（`duckdb-*`/`state-db`/`better-sqlite3`/`parquet-*`），每条均有明确 CI 限制原因；`vitest.integration.config.ts` 的 `include` 仅 4 个模式。排除清单增长受控，符合 CLAUDE.md §5 分层协议，无失控膨胀迹象。

### 6. 新增核心层目录是否补齐 INDEX.md —— 无法精确判定，快照未见违规

**证据**：受浅克隆限制无法精确执行 `git log --diff-filter=A` 判定"近期新增"目录。基于当前目录快照核查：`server/src/` 下仅 `sql/INDEX.md` 一份本地索引，`services/`/`routes/`/`config/`/`utils/`/`middleware/`/`agent/`/`normalize/` 等均无本地 INDEX.md，但均由集中式 `开发文档/00_index/CODE_INDEX.md`（2026-07-08 当日刚做过事实核对大修，见其"变更记录"）承载登记，非孤儿目录。**结论**：快照层面未见违规，但本项需要一次有完整提交历史的环境才能彻底排除，留作后续巡检的方法论缺口记录，不构成本次登记理由。

---

## 三、增量对比（新增/已恶化/已修复）

首次运行，无历史报告可比对，此节从下次巡检起生效。

---

## 四、Top 3（既有条目复核结论，非新登记）

### 方向 1（优先级最高）｜立方体切流时间盒即将到期

- **证据**：`server/ecosystem.config.cjs:43-48` 注释明确记录切流判据与操作路径；BACKLOG `2026-06-20-claude-f1c991` 2026-07-04 审计已设定 2026-08-03 时间盒。
- **影响面**：若时间盒到期前无人主动检查，可能静默滑过既定的"投入产出重估"决策点，导致影子对账双跑开销无限期延续。
- **预估工作量**：小（一次 `cube-promote.mjs` 运行 + 判读结果，约 10 分钟）。
- **验收标准**：`node scripts/release/cube-promote.mjs --history-issue <哨兵issue号>`，读取 `canAdvance` 布尔结果；若 `true` 则按 f1c991 执行链推进切流 PR，若 `false` 且已过 2026-08-03 则触发既定重估流程。

### 方向 2｜多省 BRANCH 映射镜像自动化闸（低风险速赢，工作量最小）

- **证据**：全仓扫描确认仅 5 处结构化 `_BY_BRANCH` 映射（清单见"二、1"），风险面已收敛到可枚举范围。
- **影响面**：新增第三个省份时，5 处映射需人工同步，遗漏会导致该省用户看到错误的机构分组/登录用户/图表标签，但不会造成跨省数据泄漏（非安全红线，是体验一致性问题）。
- **预估工作量**：小（4 文件加锚点注释 + 1 个 governance 检查函数，参照既有"能力矩阵镜像"闸模式）。
- **验收标准**：`bun run governance` 新增一项 "BRANCH映射镜像" 检查并通过；故意改动其中一侧后复跑，检查应报失败（验证闸生效）。

### 方向 3｜claims-detail.ts 体量观察（暂不行动，仅记录门槛）

- **证据**：`server/src/sql/claims-detail.ts` 770 行，为当前最大 SQL 生成器文件，尚未越过任何既定治理门槛。
- **影响面**：暂无——单文件体量未构成可维护性事故，仅作为后续若继续增长需关注的观察点。
- **预估工作量**：暂不安排。
- **验收标准**：暂不适用；建议下次巡检复查行数增量，若突破 1000 行再评估拆分。

---

## 五、BACKLOG 变更

未新增 B1xx 条目。对以下两个既有条目追加了复核 note（均带独立验证证据，不改变原状态/优先级）：

- `2026-06-20-claude-f1c991`（P1）—— 追加：确认切流开关现状与时间盒证据、提醒下次巡检前跑 `cube-promote.mjs`。
- `2026-07-07-claude-e80304`（P3）—— 追加：全仓扫描确认的 5 处 `_BY_BRANCH` 映射清单，供后续实施直接引用。

---

*巡检人：Claude（架构增量巡检自动化任务）· 生成时间：2026-07-08*
