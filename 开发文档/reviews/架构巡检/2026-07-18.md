# 架构增量巡检（2026-07-18）

> 本次为「架构巡检」系列**首次运行**（`开发文档/reviews/架构巡检/` 此前不存在），故不做"与上次报告对比"的增量呈现，而是建立基线；后续巡检从本报告起做真正的三分类对比（新增/已恶化/已修复）。
> 采集范围：`git log --oneline` 覆盖 2026-07-12 ~ 2026-07-18（近 6 天，50 个提交）。

## 结论先行

代码库的**结构性护栏本身健康**：`bun run governance` 61/61 全绿，`bun run typecheck`（前端 + server 两工程）零报错，`bun run test --run` 395 个测试文件 5558/5558 通过（25 项按契约显式 skip，非漏跑）。本次巡检未发现新的护栏破防或回归。

真正值得关注的不是"哪里坏了"，而是**近 6 天的开发精力高度集中在权限/认证子系统**（auth.ts / access-control.ts / permission.ts / preset-users.ts 四文件合计 `+3860 / -83` 行，`git log --since=2026-07-01 --numstat` 实测），且该子系统本身已经产出了 3 次同类型正确性缺陷（"后端权威判定函数被前端/另一端另写一份等价逻辑"模式）。项目自身的"进化铁律"机制已经在 07-15 识别并登记了通用防线需求（`2026-07-15-claude-cfac9b`），但截至本次巡检，07-17 落地的 PR #1132 仍是针对单一场景（RESTRICTED_MODULES 清单）的点状契约测试，通用静态闸尚未建立——这是本次巡检认定的 Top 1 方向。

## Top 3 优先方向

### 方向一：权限判定权威源分裂，同类正确性缺陷已发作 3 次，通用防线仍未建立

**证据**：
- `.claude/workflow/pr-evolution.md` 与 backlog `2026-07-15-claude-cfac9b`（P2·PROPOSED）已明确记录两次实例：PR #1104（前端 `ORG_USER_DEFAULT_ALLOWED_ROUTES` vs 后端 `ORG_ROLE_ALLOWED_ROUTES` 两份清单漂移 → org_user 误报 403）、PR #1108（`/api/auth/me` 的 `hasPassword` 与 `changePassword` 校验用不同函数 → 账号永久锁死在强制设密页）。
- 第 3 次实例：PR #1132（07-17，commit `d312a15`）新增 `RESTRICTED_MODULE_BACKEND_GUARDS` + `restricted-module-backend-coverage.test.ts`，仍是**逐场景手工登记**的契约测试，不是 cfac9b 期望的"扫描 auth 响应字段是否派生自权威函数"通用静态闸。
- 权限白名单本身存在一个尚未收口的具体缺口，且被**重复登记两次**：`2026-07-12-claude-20d3db`（H2，来自账号管理安全审查）与 `2026-07-14-claude-a69509`（来自 codex 对抗评审）描述的是同一件事——`permission.ts:216 getAllowedRoutesForRole()` 与 `preset-users.ts` 的 `resolveAllowedRoutes()` 都只读源码静态 `PRESET_ROLES`，不查运行时 `RoleConfig` store，导致管理面对 `allowedRoutes` 的用户级/角色级覆盖对后端鉴权无效。两条重复条目已于本次巡检加 note 提示合并（`bun scripts/backlog.mjs note 2026-07-12-claude-20d3db ...`）。
- 规模信号：`git log --since=2026-07-01 --format="" --numstat -- server/src/routes/auth.ts server/src/services/access-control.ts server/src/services/permission.ts server/src/config/preset-users.ts` 汇总 `+3860 / -83` 行，是本审计窗口内变更量最集中的子系统。

**影响面**：认证/授权是全站请求路径的强制关口，此类缺陷的失败模式是"看似通过评审却在生产误判 403/放行过宽"，且不会被 `bun run governance` 现有 61 项检查捕获（cfac9b 要求的正是新增一项）。已确认 3 次发作、2 次用户可见故障（误 403 / 锁死设密页），风险等级高于一般代码质量问题。

**预估工作量**：中（1-2 天）。核心是设计一条静态扫描规则：识别 `auth.ts`/`permission.ts` 等文件中对"是否有权限/是否需要改密"类字段的独立计算（而非调用已声明的权威函数），可复用治理体系已有的 `pattern-engine.mjs` + `pattern-rules.mjs` 声明式规则框架（`开发文档/reviews/2026-07-05-治理体系奥卡姆审计与重构.md` §2.3 已建好该引擎，新增一条规则理论上是"1 行配置 + 一对 fixture"量级）。

**验收标准**：
1. `bun scripts/backlog.mjs status 2026-07-15-claude-cfac9b DONE --evidence "<PR号>"` 可执行，且 evidence 指向新增的 governance 静态闸（非又一次 point-fix）。
2. `bun run governance` 检查项数从 61 增至 62，新增项对"人为去掉权威函数调用、手写等价逻辑"的变异用例能精确检出（仿 PR #1132 契约测试的变异负向验证方法论）。
3. `2026-07-12-claude-20d3db` 与 `2026-07-14-claude-a69509` 完成合并或标记 duplicate，避免后续会话重复触碰同一缺口两次。

### 方向二：access-control.ts 与 auth.ts 同期同幅度膨胀，拆分范围未覆盖前者

**证据**：
- `wc -l server/src/routes/auth.ts server/src/services/access-control.ts` → 1014 行 / 1040 行，均进入全仓 TS/TSX 文件行数 Top 3（`server/src/services/duckdb-domain-loaders.ts` 1151 行第一）。
- `git log --since=2026-07-01 --numstat` 实测：`auth.ts +1025/-11`，`access-control.ts +1071/-31` —— 两文件近 6 天几乎是同幅度重写级别的churn，且发生在同一权限重构窗口。
- 现有拆分计划 `2026-07-12-claude-1a97fd`（M7-struct，P3·PROPOSED）范围仅覆盖 `auth.ts`（"拆公开 router / 受保护 router"），未提及 `access-control.ts`；后者承载 `RoleConfig` 存取、`AccessUser` 存取、PAT 关联逻辑等多重职责，同样具备拆分收益。
- 本次巡检已就此登记新条目 `2026-07-18-claude-86ec9f`（P3），避免与 1a97fd 重复但扩展覆盖面。

**影响面**：中等。两个文件均是权限判定的直接载体，体积和职责密度越高，方向一描述的"权威函数被绕过"类缺陷越容易在评审中被漏看（改动分散在 1000+ 行文件的不同角落，diff 可读性下降）。这是方向一风险的放大因子，而非独立新风险。

**预估工作量**：中（1-2 天，与 M7-struct 1a97fd 可合并规划一次性做）。

**验收标准**：
1. `bun run typecheck` 与 `bun run test --run` 全绿（拆分不改变行为，纯结构重排）。
2. 拆分后单文件行数回落到 CODE_INDEX 全景表 Top 20 之外（参照阈值：本次 Top 20 最低点 660 行）。
3. `bun scripts/backlog.mjs status 2026-07-18-claude-86ec9f DONE --evidence "<PR号>"`。

### 方向三：07-12 生产 502 事故的两个部署链根治项已停滞 6 天

**证据**：
- `开发文档/reviews/2026-07-12-生产502事故RCA.md`、`开发文档/reviews/2026-07-12-pm2开机自启修复.md` 已详细记录事故与部分止血。
- 三个 follow-up backlog 条目自 07-12 创建后状态零推进（`grep -l <uid> backlog-events/2026-07/*.json` 折叠事件时间线实测）：
  - `2026-07-12-claude-7b1a72`（P2）：`pm2 resurrect` 绕过依赖完整性自愈闸，事故复现路径未堵——仍 PROPOSED，无任何后续事件。
  - `2026-07-12-claude-4b95fa`（P2·部署链 RED LINE）：`npm ci` 先删后装制造 `node_modules` 空窗，事故根因之一——仍 PROPOSED，无任何后续事件。
  - `2026-07-12-claude-10e467`（P1）：VPS 未装 PM2 开机自启，云厂商维护重启即全站宕机——仍 PARTIAL（07-12 当天最后一次动作），后续 6 天无推进。
- 期间团队精力（如上两个方向所示）集中投入到权限/认证功能与安全评审收口，部署链根治被搁置。

**影响面**：高但低频——一旦触发（VPS 重启、resurrect 路径撞依赖缺失）即全站不可用，且 07-12 已发作过一次。三项均已有明确技术方案（详见对应 backlog desc），非设计缺口，是纯粹的排期滞后。

**预估工作量**：小-中。10e467（PM2 systemd 开机自启）预估工作量最小（配置级）；7b1a72（resurrect 前置校验闸）中等；4b95fa（node_modules 原子替换）标注"部署链 RED LINE，禁 auto-merge，须人工监控窗"，工作量含发布窗口协调。

**验收标准**：
1. `2026-07-12-claude-10e467` 状态推进到 DONE，证据含 VPS 实测 `systemctl status pm2-root` 生效 + `pm2 save` dump 内容核对。
2. `2026-07-12-claude-7b1a72` 的 `verify-natives canary` 纳入 express，`resurrect-guarded` 子命令在模拟依赖缺失场景下验证拦截生效。
3. `2026-07-12-claude-4b95fa` 按 `.claude/rules/deploy-chain-sop.md` 走人工监控窗合并，合并后一次真实部署观测无 `node_modules` 空窗期 502。

## 已确认健康、无需行动的信号（记录以便下次对比）

- `bun run governance` 61/61（含 CLAUDE.md 体积 17.0KB/20KB，接近但未超预算；rules eager-load 20.5KB/40KB 有余量）。
- `bun run typecheck` 前端 + server 两工程零报错。
- `bun run test --run` 395 文件 5558/5558（25 skip 有据可查，非静默跳过）。
- 14 个存量项目技能迁移至外部 skills 仓（PR #1126/#1125，commit `81121ed`）已由 backlog `2026-06-xx` 系列条目 DONE 收口，governance「项目本地 skill 契约」检查确认 `.claude/skills/` 不存在残留实体目录。
- `bun scripts/backlog/check-merged-drift.mjs` 因浅克隆跳过，未产出可用信号（非本次巡检可修复项，环境限制）。

## 本次登记 / 追加的 backlog 事件

| 操作 | uid | 内容 |
|------|-----|------|
| note | `2026-07-15-claude-cfac9b` | 补充第 3 次发作证据（PR #1132）+ 四文件 churn 统计，建议提优先级 |
| note | `2026-07-12-claude-20d3db` | 标记与 `2026-07-14-claude-a69509` 重复登记同一缺口，建议合并 |
| add | `2026-07-18-claude-86ec9f`（P3） | access-control.ts 拆分评估，扩展 `1a97fd` 覆盖范围 |

---

维护者：架构巡检自动化（scheduled routine）· 下次巡检请先读本报告，只报与本报告的增量。
