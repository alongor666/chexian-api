# 前缀防线退役清单（801409 收尾项 · 2026-07-10 评估）

> **backlog uid**：`2026-06-23-claude-801409`（本清单为其最后剩余范围）· 联动 `2026-06-29-claude-a5aa03` P0/P2/P3 重估（§5）
> **上位文档**：[子目录布局cutover_SOP_2026-07-06.md](./子目录布局cutover_SOP_2026-07-06.md) §2-5
> **一句话结论**：**本次不删任何代码**——SOP 要求「cutover 稳定运行 ≥1 周后」评估退役，而 cutover（2026-07-07）后 3 天内发生 3 起布局相关事故（§1），「稳定」判据未满足；本文交付**带时间盒的逐项退役清单**（§2/§3），实际退役动作由独立执行卡在时间盒期满后按 §4 oracle 执行。

---

## 0. 评估结论：现在删是「无害冗余」还是「有成本死代码」？

逐项亲测（grep + 主仓 `ls` + governance 源码阅读，2026-07-10）后的判定：

1. **前缀专属防线当前全部处于「休眠看守」态，零误报零拦截成本**。`checkPolicyGlobPrefixIsolation` 只扫顶层扁平 `current/*.parquet`——主仓已是 `SC/`+`SX/` 子目录布局（亲验 `ls`，2026-07-10 凌晨仍在正常更新），命中 0 文件即 `data_present: false` → skip=success，不产生误报也不拖慢 governance。`flatPrefixBranch`（装载去重兜底）在子目录下 `f.branch` 恒有值、代码路径休眠。
2. **它们不是孤立死代码，而是「双布局自适应」的扁平半边**。`branch_paths.py` 扁平分支（`[!S]*` glob + `flat_prefix_branch`）被 35 个 Python 读侧站点经 SSOT 间接依赖；`parquet-overlap-check.mjs` 前缀解析被 sync-vps / daily.mjs / governance / policy-current-shards 四方消费。单独拆散删任何一点都可能破坏「扁平布局回滚」这条 SOP §4 保留的逃生通道——正确的退役单元是**「扁平布局支持」整体**，一次专项 PR、带 §4 oracle。
3. **时间盒**：SOP 观察期锚点应从**最后一次布局相关生产修复**起算，而非 cutover 日。最后一起为 PR #1024（sx-promote sha256 非确定性误报，2026-07-10 合并）→ **退役执行 PR 不早于 2026-07-17**，且期间无新增布局相关事故；届时若扁平回滚通道确认永久放弃（owner 确认），按 §2 清单执行。

## 1. 「稳定 ≥1 周」判据核查（评估依据，全部可回溯）

| 日期 | 事件 | 来源 |
|------|------|------|
| 2026-07-07 | 生产 cutover 实跑完成（SOP T-0~T-7 全勾，5 parquet 4,464,114 行零差异） | BACKLOG_LOG eid `9a625f01` |
| 2026-07-08 | `release:daily` 实测暴露 4 处消费者残留扁平 glob（daily.mjs 前置闸 / diagnose-period-trend / checkDataDrift / 邮政双 YAML），当日修复 | uid `2026-07-08-claude-170419`（已 DONE，排查报告 [2026-07-08-B5子目录cutover残留glob排查.md](../reviews/2026-07-08-B5子目录cutover残留glob排查.md) 确认此后零 live 残留） |
| 2026-07-10 | 生产全站 403（dist.bak 套娃部署 bug，与布局无关但同窗口） | PR #1022 |
| 2026-07-10 | sx-promote sha256 非确定性误报冲突 → 中止 SX 晋升连坐 SC 发布链 | PR #1024（**直接在子目录晋升链路上**） |

结论：距 cutover 仅 3 天且布局相关修复墨迹未干，删防线的前提「稳定运行 ≥1 周」不成立。

## 2. 退役清单（时间盒期满后一个专项 PR 同批执行）

> 处置口径：**修补不拆除**——每项动手前须重新确认「消费方=0 或全部同批迁移」，任一不成立即中止该项。

| # | 项 | 位置（2026-07-10 亲验） | 处置 | 说明 |
|---|----|------|------|------|
| R1 | `checkPolicyGlobPrefixIsolation`（`[!S]*` 前缀隔离闸） | `scripts/check-governance.mjs:1101`（注册于 :2731，已挂 `retireWhen` 指向本清单） | **可退役** | 只扫顶层扁平 glob，子目录布局下恒 skip 空转；其存在意义=守护 R3 扁平 `[!S]*` 语义前提，必须与 R3 同批退役，先删 R1 后删 R3 会留裸奔窗口（反向顺序禁止） |
| R2 | `flatPrefixBranch` + 去重前缀兜底 | `server/src/services/data-bootstrapper.ts:307-360` | **可退役** | B5 修复的扁平前缀去重盲视兜底；子目录下休眠。退役=装载层放弃扁平兼容，须同步收紧 `enforceProvinceSubdirGate` 语义（并存互斥保留、扁平合法态移除） |
| R3 | `branch_paths.py` 扁平分支（`flat_prefix_branch` / `[!S]*.parquet` / 裸名→SC 回退） | `数据管理/pipelines/branch_paths.py:43-205` | **可退役** | Python 读侧 SSOT 的扁平半边，35 站点间接依赖——退役只动 SSOT 一处，站点无感；须保留「零文件/意外布局 fail-closed」 |
| R4 | `parquet-overlap-check.mjs` 扁平文件名前缀解析（`parseBranchFromFilename`：`<BRANCH>_` → 省、裸名回退 SC） | `scripts/lib/parquet-overlap-check.mjs:106` | **可退役** | 直接消费者 3 处（check-governance / sync-vps / daily.mjs）全部已双布局化；`policy-current-shards.mjs` 是其**被 import 的依赖方**而非消费方。生产 parquet 均含 branch_code，前缀解析仅剩边缘兜底（sonnet 对抗评审 2026-07-10 核实） |
| R5 | sx-promote `SX_` 前缀识别防呆 | `scripts/release/sx-promote.mjs:142,418,486` | **待观察 → 倾向保留至 R1-R4 落地** | 现职责已不是命名产物，而是**检测旧扁平残留、防「扁平+子目录并存」**——在扁平支持整体退役、生产确认无残留前它是最后一道迁移态防呆 |
| R6 | 主仓 `current/` 顶层旧产物 `.sx-promote-manifest.json` / `.sx-promote-ready`（2026-06-29 扁平时代） | 主仓数据目录（非 git） | **可清理（运维操作，不入代码 PR）** | manifest/marker 现随 targetDir 落 `current/SX/` 内（sx-promote.mjs:1047-1052 亲验）；清理前 `ls -la` 亲验 + 确认无脚本读顶层路径 |
| R7 | 文档层 `SX_` 命名约定（`.claude/rules/data-pipeline.md`、multi-branch 各文档） | grep `SX_` 命中 11 个文档 | **退役=追加「已退役」标注** | rules 文件 append-only（AGENTS.md §8.2），只加墓碑标注不删正文；multi-branch 历史文档保留原文作沿革 |

## 3. 保留清单（与路径方案正交，禁止随退役 PR 顺手删）

| 项 | 位置 | 为何与布局正交 |
|----|------|------|
| `checkProvincePrefixMapConsistency` | `check-governance.mjs:926` | 名字带 Prefix 但管的是**上游 BI 导出文件名拼音前缀路由**（`sichuan_`/`shanxi_` → branch_code，`source-file-routing.mjs` ⟷ `fields.json` 双源一致）——ETL **入口**约定，与 current/ **出口**布局无关。亲验（2026-07-10）：拼音路由被 `daily.mjs` / `bi-export-pull.mjs` / `shard-classify.mjs` / `branch-publish.mjs` / `sync-and-reload.mjs` 5 处 live 消费。⚠️ 该检查注册处（:3476）被奥卡姆批次误归「前缀族」挂了 `retireWhen` 指向本清单——**判定为误标**，退役执行 PR 须移除该 retireWhen 标注并保留检查本体 |
| `checkSingleProvincePerFile` | `check-governance.mjs:954` | 双布局都扫（顶层+子目录两条 domain），单文件不混省 pre-sync 数据质量闸；扁平退役后仅删顶层 domain 行 |
| `checkWecomEngineBranchIsolation`（闸 #52） | `check-governance.mjs:3314` | WHERE branch_code / 出口断言静态扫描（a5aa03-P3 落地件），管 SQL 过滤不管文件布局 |
| 出口零信任断言 `assert_single_branch` / `branch-assert.mjs` | PR #858 产物 | a5aa03-P1，纵深第二层，2026-07-04 架构审计已明确「子目录化后仍有价值」 |
| `enforceProvinceSubdirGate` + 并存互斥闸 + `policy-current-shards.mjs` 迁移态冲突检测 | `data-bootstrapper.ts` / `policy-current-shards.mjs:144-147` | 子目录方案自身的 fail-closed 核心，是退役后**唯一**的布局防线 |
| `checkBranchMappingMirror` / `SX_ORGANIZATIONS` | `dual-lock-and-branch-mirror-checks.mjs` / `permission.ts:40` | 机构常量前后端镜像，业务语义层 |
| pattern-rules `branch-code-fallback`（禁 `?? 'SC'` 静默默认） | `scripts/governance/pattern-rules.mjs:206` | 省份轴反模式闸，与布局轴无关 |

## 4. 退役执行 PR 的 oracle 要求（届时逐项打勾）

1. 逐项消费方复核：R1-R4 每处删除点 `grep -rn` 确认无残余引用（含测试）；
2. governance 红绿对照：被删检查的 fixture 一并删除，剩余检查数与 `bun run governance` 输出一致；
3. 双布局回归收窄声明：PR 描述显式声明「扁平布局自此不再支持，回滚路径改为『子目录整目录还原』（SOP §4 备份 `current.bak-*` 本身已是布局无关的目录级恢复）」，owner 确认；
4. `bun run verify:full` 全绿 + 主仓真实数据 smoke：`branch_paths` 双省 glob、装载 `Data ready` 行数与基线一致；
5. `retireWhen` 生命周期标注对账：移除 `check-governance.mjs:2732`（随 R1 退役）与 `:3476`（误标，检查保留、仅摘标注，见 §3）两处；
6. 本清单 §2 各行回填「已退役（PR #）/保留」终态（本文档 append-only 追加，不改写原表）。

## 5. a5aa03 联动重估（子目录落地后 P0/P2/P3 哪些已被结构性覆盖）

| 子项 | 重估结论（2026-07-10） | 证据 |
|------|------|------|
| P0 邮政表止血 | **已完成（结构性四层覆盖）** | 邮政双实例 YAML 亲验：子目录 glob（`current/SC|SX/*.parquet`，2026-07-08 随 170419 修复）+ `extra_where: branch_code='SC'/'SX'` + 引擎出口 `assert_single_branch` + 闸 #52 实例层静态盯 |
| P1 出口零信任断言 | 已完成（PR #858，2026-06-30） | BACKLOG eid `b173c21a` |
| P2 统一取数入口 SSOT | **已被结构性覆盖** | Python 35 站点统一 `branch_paths` SSOT（PR #957）+ Node `policy-current-shards.mjs` + 企微双引擎收编（170419 DONE 复核「全仓保单直查点 100% 收编共享 helper」）。剩余=非 daily 手动工具 `branch=None` 省份轴收窄，已独立追踪 uid `2026-07-09-claude-50d62e` + `project_multiprovince_hardcode_debt` |
| P3 闸固化全量范围 | **主体已落地，剩余子项按拍板废弃** | 已落地：闸 #52 + 省份静默默认反模式（162 文件）+ 单文件不混省（双布局）+ 省份前缀映射一致。原计划「扩 `checkPolicyGlobPrefixIsolation` 到企微 instance/技能仓」子项随前缀方案退役作废（本清单 R1）；「裸读 current 即 CI 红」的布局轴由物理子目录 fail-loud（扁平 glob 读 0 行即 `IO Error`）结构性承担 |
| P4 物理子目录化 | 已完成（并入 801409，2026-07-07 生产 cutover） | BACKLOG eid `9a625f01` |
| 遗留盲区（不阻断关单） | 多省扩展时 quotes/salesman/customer_flow 出口断言只看 policy_no 派生（eid `d5facc8b` follow-up）——第三省接入前置项，归 `project_multiprovince_hardcode_debt` 轴 | — |

→ a5aa03 五子项全部「完成/结构性覆盖/废弃有据」，可置 DONE；剩余细项均有独立 uid 或 memory 轴承接。

## 6. 关联

- 退役范围原始定义：cutover SOP §2-5；owner 拍板（2026-07-04）：B3 子目录终局、前缀方案转过渡态待退役（BACKLOG eid `7c5fed11`）
- 残留 glob 排查（零 live 残留背书）：`开发文档/reviews/2026-07-08-B5子目录cutover残留glob排查.md`
- 红线：CLAUDE.md §0「修补不拆除」；`.claude/rules/data-pipeline.md` 省份隔离；deploy 链文件（sync-vps.mjs / sx-promote.mjs）届时按 `.claude/rules/deploy-chain-sop.md` 禁 auto-merge
