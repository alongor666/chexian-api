# 全国多省架构决策（ADR）— 山西接入触发的架构选型复评

> **状态**：✅ 架构方向已与用户拍板（2026-06-19）｜🟡 0a 实施方案经对抗式评审校准（见 §11）｜⏳ 实施计划见 `.claude/plans/`
>
> **决策人**：项目主人（用户）｜**起草**：Claude（基于全程代码实证 + 双模型对抗式评审）
>
> **背景一句话**：原 ETL 只有四川（SC）单省数据，2026-06-19 拿到山西（SX）全套源数据（签单 485MB / 报价 113MB / 厂牌 43MB / 理赔 / 维修，共 ≈645MB），终局确认为**全国铺开多省的统一经营分析平台**。本文复评"该如何改造最好"，并固化结论与风险面。
>
> **维护协议**：§1-§10 经一次对抗式评审校准（2026-06-19，见 §11 日志）。后续新决策/新实证追加章节或追加 §11 日志条目，不静默改写已校准结论。

---

## 1. 决策（TL;DR）

| # | 决策 | 一句话理由 |
|---|------|-----------|
| D1 | **维持"单数据湖 + `branch_code` 省份列 + 行级安全（RLS）"架构，不换范式** | 终局是全国汇总 + 持续加省 + 一套运维，这正是单数据湖的主场；且本项目该架构脚手架已建成约 90%（双轮对抗评审未动摇此决策） |
| D2 | 否决"各省独立部署/独立库" | 全国终局下没有跨省汇总能力、运维成本 ×N，仅适用于"某省法规要求物理彻底隔离"的特例 |
| D3 | 把"第 0 层"拆成 **0a（省份前缀文件名 + 扁平目录 + branch-aware 管道，现在做）** 与 **0b（内存随省份收缩，性能工程，归第 2 层）** 两块分开定价　🔴 **【0a 隔离层「前缀扁平 + 否决子目录」部分已被 D6 超越（2026-06-24，见 §11）；0a/0b 分层定价本身仍有效】** | 0a 不采用 Hive 子目录（评审证实代码库多处假设扁平目录，子目录 blast radius 超 0a 预算）；二者收益与代价不同，混为一谈会低估风险（见 §6）　↳ **D6 校准**：该 blast radius 已由 Phase B B1 装载层零物理迁移 + 双闸填平，隔离层改子目录终局 |
| D4 | ClickHouse-分布式 / 存算分离作为**写进文档的扩容逃生口**，设明确触发阈值，现在不做（YAGNI） | 过早上 MPP 引擎是过度工程；但它是"物理隔离 + 跨省汇总"兼得的真正进化形态，须预留 |
| **D5** | **【评审校准】0a 不把 SX 数据载入共享 runtime；口径签字（G5）+ RLS-on 是 SX 进共享 runtime 的硬前置（对齐 Day-1 SOP）** | 装载器把 `current/*.parquet` 全量物化进同一内存表；RLS 默认关时 SX 行会让四川用户看到混省数据。详见 §6.2、§11 |
| **D6** | **【2026-06-24 收敛·超越 D3】0a 隔离层终局 = 分省子目录 `current/<省>/`（每省独立 sync），退役 #753 前缀 + rsync filter；检测层 `branch_code` 派生已 Phase A 根治、方案无关** | D3 否决子目录的核心理由（装载层 blast radius）已由 Phase B B1 零物理迁移 + 双闸填平；用户 2026-06-22 已批准《省份派生化+分省子目录方案》、§11 2026-06-23 已定性前缀为过渡。详见 §11 2026-06-24 + [D3-vs-PhaseB 分叉复盘](./D3-vs-PhaseB分叉复盘_2026-06-24.md) |

> **行级安全（RLS, Row-Level Security）**：在查询 SQL 自动注入 `WHERE branch_code='<用户所属省>'`，让每个省的用户只能看到本省数据的机制。本项目由 `BRANCH_RLS_ENABLED` 开关控制，**默认关闭**（`server/src/config/env.ts:127`）。

---

## 2. 问题与终局

- **现状**：所有数据 `branch_code` 默认贴 `SC`（四川）；14 个三级机构均为四川分公司机构。
- **终局（用户 2026-06-19 确认）**：**未来全国铺开多省**，山西只是第二个省，目标是一个统一的多省经营分析平台，总部/用户需要看跨省合并视图。
- **核心矛盾**：现实现是按"四川 + 再加一个省"的体量做的，**不是按"全国 30 省平台"做的**；直接照现状铺全国，会在第 3、第 5 个省时撞上结构性扩展债（内存、源摄入、机构唯一性、派生域、扁平目录假设）。

---

## 3. 备选方案对比（评判标准＝全国多省终局）

| 维度 | A. 单数据湖 + branch_code + RLS（现方案） | B. 各省独立部署/独立库 | C. 分库存储 + 联邦查询层 | C+. ClickHouse-分布式（C 的进化形态） |
|---|---|---|---|---|
| 跨省合并视图 | ✅ 一条 `GROUP BY branch_code` | ❌ 跨 N 库拉数应用层拼，几乎做不动 | 🟡 联邦层能做但复杂 | ✅ 分布式表原生合并 |
| 新增一省成本 | ✅ ETL 贴标签 + 发账号 | ❌ 整套部署+运维 ×N | 🟡 加库 + 注册联邦 | ✅ 加一个分片 |
| 运维负担 | ✅ 一套代码一套部署 | ❌ N 套发版同步 | 🟡 1 套代码 + N 库连接 | 🟡 跑 DB 集群 |
| 数据隔离强度 | 🟡 逻辑隔离（RLS 列过滤，代码 bug 可能串读） | ✅ 物理隔离最强 | ✅ 物理隔离 | ✅ 库级物理隔离 |
| 小 VPS 扩展性 | 🟡 内存峰值随省线性涨（见 §6 0b） | ✅ 各省各机器天然分摊 | 🟡 看联邦层落点 | ✅ MPP 水平扩展 |
| 本项目现状 | **已建成 ~90%**（字段/RLS/缓存/SQL/SOP 全就位） | 从零另起 + 拆现有单仓逻辑 | 联邦雏形已有（`SQL_FEDERATION_ENABLED`）但面向派生域，非跨省 | 须迁移引擎 |

**结论**：选 **A**。B 的"物理隔离"优势在"需要全国汇总"面前直接变致命缺陷；C+ 是 A 撞到规模天花板后的升级路径（见 §7 第 2 层），不是现在的选项。

---

## 4. 现状盘点 — 多分公司脚手架已建成 ~90%（实证）

| 层 | 现状 | 实证 |
|---|------|------|
| 字段层 | `branch_code` 已定义为派生常量字段（`SC`/`SX`），ETL 按 `BRANCH_CODE` 环境变量贴标签 | `server/src/config/field-registry/fields.json:550` |
| ETL 引擎 | `transform.py` 已实现派生字段物化引擎（读 `derivation.type=constant` + `envVar`），`BRANCH_CODE=SX node 数据管理/daily.mjs all` 即注入 | `数据管理/pipelines/transform.py:1054-1089` |
| 历史回填 | `backfill_derived_fields.py` 已存在（给旧 Parquet 补 branch_code 常量列） | `数据管理/pipelines/backfill_derived_fields.py` |
| 权限层 RLS | `permission.ts` 注入 `branch_code='<省>'`，无 branchCode 时 fail-closed 401 强制重登；**flag 默认关** | `server/src/middleware/permission.ts:88`、`env.ts:127` |
| 缓存隔离 | route-cache key 已按 branch 隔离（PR #501），压测脚本自动断言 SC/SX 各自命中独立 cache | `scripts/multi-branch-stress-test.mjs` |
| SQL 层 | 已拔"四川"硬编码，按 branchCode 派生中文名（SC 已填，SX 注释待启用） | `server/src/config/branch-names.ts` |
| 预置用户 | 全部 20 用户标 `SC`，`branchCode` 字段就位，`getUniqueBranchCodes()` 加 SX 用户后自动含 SX | `server/src/config/preset-users.ts` |
| 操作手册 | Day-1 上线 SOP（10 项验证清单）+ 回滚 SOP + 口径对齐框架全部写好 | `.claude/rules/multi-branch-day1-sop.md`、`.claude/rules/multi-branch-rollback-sop.md`、`开发文档/multi-branch/口径对齐_山西.md` |

---

## 5. 缺口清单 — 真正要做的"改造"

> 这些是"全国可用"的前置，不是"换架构"。按性质分类。

### 5.1 ETL / 数据层
- **G1｜源文件省份化摄入**：`daily.mjs` 只认 `01_签单清单_*.xlsx` 等固定命名、从单目录 `数据管理/` 摄入；山西文件名为 `山西_签单清单_2021-2026.xlsx`，命名/路径对不上。需 `数据管理/staging/<省>/` 分目录 + daily.mjs 按省编排。
- **G2｜省份物理隔离**：见 §7 0a（省份前缀文件名 + branch-aware 管道）。
- **G3｜维度/派生域省份限定**：业务员/厂牌/维修/计划维度表 + `RepairDim`/`QuoteConversion`/`RenewalTrackerFact` 等派生域均为单 `latest.parquet`/全局表，无省份维度（`paths.ts`、`duckdb-domain-loaders.ts:408-560`）。**多省共存不只是 policy 分片问题**——这些表不处理则 SX 用户拿到混省/全局数据。
- **G4｜派生域补 branch_code**：cross-sell / repair / customer_flow / new_energy_claims 等派生视图未携带 branch_code（BACKLOG `2026-06-19-claude-00bac8` / PR #676 follow-up）→ 省用户查这些一律 fail-closed 拒绝（安全无泄漏，但功能不可用）。

### 5.2 业务口径层
- **G5｜口径业务签字**：`口径对齐_山西.md` 中 11 类客户分类 / NCD 表 / 风险评级 / 续保窗口 / 满期系数 6 大口径全为 `⏳ 未签字`。**这是业务方的活**，山西口径未必与四川相同。**RLS flag 启用前必须签字（Day-1 SOP §8 + D5）。**

### 5.3 应用 / 治理层
- **G6｜机构白名单硬编码**：`kpi-detail.ts:85` 硬编码 14 个机构同城/异地白名单，新增/山西机构两边都不计入 → region 环形图分母静默缺失（BACKLOG `2026-06-11-claude-fdbba5`）。**到第 2 个省就破**。
- **G7｜山西账号**：`preset-users.ts` 尚未加 SX 超管 + 山西机构用户（Day-1 SOP §4）。
- **G8｜前端空态保护**：山西数据装载中的提示文案未做，禁止静默展示空 KPI（Day-1 SOP §5）。

---

## 6. 风险面 — 0a 的改动面（实证 + 评审校准）

> **校准说明**：本节原结论"风险小而集中，71 端点零改动，最尖雷=governance #13"经双模型对抗评审**部分被证伪**，已重写。校准日志见 §11。

### 6.1 哪些真不动 / 哪些会动

| 层 | 评审后结论 | 实证 |
|---|------|------|
| **71/78 端点 SQL 文本** | ✅ 文本零改动 | `server/src/sql` 内联 `read_parquet`＝0；全走 `FROM PolicyFact`(129) 等命名关系 |
| 端点**结果** | ⚠️ **会变**（措辞校准） | 端点结果取决于"装载的文件集合 + RLS 开关"——0a 一旦载入 SX 行且 RLS 关，四川结果即变（见 §6.2）。"SQL 不改 ≠ 结果不变" |
| 装载/物化层 | ⚠️ 与文件布局耦合 | `data-bootstrapper.ts:147/321` 非递归读 `current/*.parquet` → `loadMultipleParquet` → `duckdb-parquet-loader.ts:139` `read_parquet` → `materialization.ts:176` 全量物化进 `PolicyFactRealtime` |
| **扁平目录假设** | ❌ **远不止 2 处**（证伪） | 见下表 ~6 处 |
| RLS / cache key | ✅ 已就位 | `permission.ts:88`、PR #501 |

**扁平目录假设站点（0a 必须逐一 branch-aware，否则误报/误删/CI 卡死）：**

| 站点 | 问题 | 后果 |
|---|---|---|
| `scripts/lib/parquet-overlap-check.mjs:43` | 非递归 readdir + 不分省两两比对 | SC/SX 同期分片误报"数据翻倍" |
| `数据管理/pipelines/quick_reference.mjs:96` | `shardCount`＝目录文件总数 | governance #15 报"分片 4 vs 8"→ **CI exit 1** |
| `数据管理/daily.mjs:1449` | 范围互斥守卫 `RANGE_RE` 不分省 + 归档正则只匹配 `^YYYYMMDD-` | SX 前缀分片不受保护 / 归档误判 |
| `数据管理/daily.mjs:~1651` | `data-sources.json` 行数汇总所有 current 分片 | 跨省行数混计 |
| `scripts/sync-vps.mjs:368/459/595` | `--delete` + 新鲜度闸按全目录总行数 + 整目录同步粒度 | 单省同步风险 / 新鲜度误判 |
| `server/src/routes/data.ts:367` | web 上传归档正则把 `SX_` 吃进 prefix 段 | 仅 web 上传路径触发（0a 走 ETL+rsync 不触发，记为已知限制） |

### 6.2 最关键发现 — 0a 不能把 SX 载入共享 runtime（D5 来源）

装载器把 `current/*.parquet` **全量物化进同一张内存表** `PolicyFactRealtime`（`materialization.ts:176`）。0a 若把 SX 放进 `current/`、而 RLS **默认关**（`env.ts:127`），则 admin（四川）查询无 branch 过滤 → **看到 SC+SX 合并数据**。这不仅让 golden-baseline"四川零差异"必然失败，更是**生产安全隐患**：SX 数据一进共享 runtime + RLS 没开，四川线上用户立刻看到混省数据。

**纠正**：本项目 Day-1 SOP 早已在 T-3（载 SX 到生产、发账号之前）就 `BRANCH_RLS_ENABLED=true`。故正确序列 = **口径签字（G5）+ RLS-on 是 SX 数据进共享 runtime 的硬前置**；0a 期 SX 数据只在隔离环境（独立目录/duckdb 直查）验证，绝不进服务端载入的 `current/`。

### 6.3 0a 拆分

- **0a｜省份前缀文件名 + 扁平目录 + branch-aware 管道（现在做）**
  - 收益：**ETL/运维隔离 + governance 不误报**——单省刷新只动自己的前缀文件、单省 sync、`branch_code` 作数据列。
  - 改动：§6.1 表中 ~6 处扁平假设逐一 branch-aware（含测试）。
  - 不变：71/78 端点 SQL 文本、RLS、cache。
  - 局限：**不省内存**（全省物化进 `PolicyFactRealtime`）；**不把 SX 载入共享 runtime**（见 6.2）。
- **0b｜内存随被查省份收缩 + 真 Hive 子目录分区（不便宜，归第 2 层）**
  - 要省内存须放弃"全量物化进内存"，改 `read_parquet(hive)` 视图，**丢掉 4 个索引 + zonemap**，须 `golden-baseline` + `bench-universal-cube` 全量回归。
  - branch_code 双源风险（同名数据列 + hive 路径分区键）在此层才触发（评审论点⑤，0b 的雷，非 0a）。

---

## 7. 扩展性分层与触发阈值

| 层 | 内容 | 何时做 | 触发阈值 |
|---|------|--------|---------|
| **0a** | 省份前缀文件名 + 源摄入省份化（G1）+ ~6 处扁平假设 branch-aware + SX 隔离验证 | **现在**（山西接入前置，纯管道，不进共享 runtime） | 接第 2 个省即做 |
| **0b** | 物化层改造 / 内存随省份收缩 / 真 Hive 子目录分区 | 内存吃紧才做 | 单机 RSS / DuckDB peak 接近上限 |
| **第 2 层 · 存算分离** | parquet 上腾讯云 COS，查询节点独立于 web 层 | 数据撑爆单 2核4G web 机时 | 单机内存/磁盘扛不住 |
| **第 2 层 · ClickHouse / Doris** | MPP 列存 + 原生行策略 + 分布式表（每省一分片 + 总部合并视图）；物理隔离 + 跨省汇总兼得 | 真到全国规模 | 行数千万-亿级 / 高并发 / 监管要求物理隔离 |

---

## 8. 验证策略（现成 harness，证据闭环）

任何改动按 `.claude/rules/evidence-loop.md` §4 执行，本主题对应 harness：

| 验证目标 | 现成 harness | 通过标准（校准后） |
|---------|-------------|---------|
| 78 端点行为零回退（管道改动必跑） | `scripts/golden-baseline.mjs --build`（**改动前 commit 抓**）→ `--compare` | 零差异（脚本实测 78 端点，非 71；仅 admin=SC 视角） |
| **四川隔离证明** | golden-baseline 须在 **`BRANCH_RLS_ENABLED=true`** 下跑（否则 SX 行混入必然失败，见 6.2） | SC 端点零差异 |
| SX 视角 | `multi-branch-stress-test.mjs --simulate-sx`（RLS on） | SC/SX 各自 cache，SX token 不读 SC 行 |
| 立方体口径不变 | `cube-shadow.ts` 影子对账 + `bench-universal-cube.mjs` | 容差内 |
| 源数据口径 | `duckdb` 直查 SX 隔离 parquet vs 山西原始 Excel | 误差 ≤ 万分之一 |
| 回归门禁 | `bun run governance` + `bun run verify:full` | 全绿 |

---

## 9. 实施路线（校准后序列）

1. **本文档（架构决策）** — 已落地 + 经评审校准。
2. **0a 实施计划** — 见 `.claude/plans/2026-06-19-multi-province-0a-branch-partition.md`（重写版）。
3. **0a｜纯管道**：~6 处扁平假设 branch-aware + 源摄入省份化 → governance/verify 全绿 + golden-baseline 零差异（**此时不载入 SX，四川零差异自然成立**）。
4. **0a｜SX 隔离验证**：`BRANCH_CODE=SX` ETL 到独立验证目录 → duckdb 直查口径对账（**不进 `current/`**）。
5. **G5 口径业务签字**（业务方）——**SX 进共享 runtime 的硬前置**。
6. **G3/G4/G6/G7/G8** 补齐 → 按 Day-1 SOP：**先 `BRANCH_RLS_ENABLED=true` + RLS 隔离验证**，再载 SX 到共享/生产、发山西账号。
7. 监控期（Day-1 SOP §6），触发条件按回滚 SOP。

> **顺序铁律（D5）**：G5 业务签字 + RLS-on 完成前，**禁止**把 SX 数据载入服务端 `current/` 或 sync 到 VPS（否则四川线上用户看到混省数据）。

---

## 10. 关联

- **口径对齐**：[口径对齐_山西.md](./口径对齐_山西.md)
- **Day-1 SOP**：[../../.claude/rules/multi-branch-day1-sop.md](../../.claude/rules/multi-branch-day1-sop.md)
- **回滚 SOP**：[../../.claude/rules/multi-branch-rollback-sop.md](../../.claude/rules/multi-branch-rollback-sop.md)
- **证据闭环 §4 harness**：[../../.claude/rules/evidence-loop.md](../../.claude/rules/evidence-loop.md)
- **字段注册表**：`server/src/config/field-registry/fields.json`（branch_code 定义）
- **装载/物化层**：`server/src/services/duckdb-parquet-loader.ts`、`duckdb-materialization.ts`、`data-bootstrapper.ts`
- **相关 BACKLOG**：`2026-06-19-claude-00bac8`（派生域 org_level_3 NULL）、`2026-06-11-claude-fdbba5`（机构白名单硬编码）、`2026-06-14-claude-4641ef`（派生域数据架构治理）

---

## 11. 对抗式评审校准日志

### 2026-06-19 · 双模型独立对抗式评审（evidence-verifier + codex gpt-5.5）

对 §6 原结论的 5 条承重论点逐条证伪，两模型独立撞同一结论（高置信）：

| 论点 | 裁决 | 校准动作 |
|---|---|---|
| ①71 端点 SQL 零改动 | ⚠️ 措辞误导 | §6.1 区分"SQL 文本零改动"vs"端点结果会变" |
| ②前缀避开所有扁平假设 | ❌ 证伪 | §6.1 扁平假设站点从 2 处订正为 ~6 处 |
| ③golden-baseline 证四川零差异 | ❌ 证伪 | §6.2 + §8：RLS 关 + SX 入共享表 → 混省必然失败；校正为 RLS-on 下证 + 78 端点（非 71）+ 基线须改动前 commit 抓（实测 `.planning/golden-baseline/` 不存在） |
| ④组内孤立限摩仍被检出 | ❌ 证伪 | 该保护本不存在（`parquet-overlap-check.mjs:7-8` 注释与实现不符），计划改为登记 BACKLOG，不声称保留 |
| ⑤branch_code 双源是 0b 的雷 | ✅ 维持 | §6.3 0b 段保留；确认非 0a 的雷 |

**新增（原稿未列）**：
- ADR↔计划"Hive vs 前缀"矛盾 → 本次统一为"0a 用前缀扁平、Hive 子目录归 0b"（D3、§6.3、§7）。
- golden-baseline 实测 **78** 端点（非 71），仅 admin=SC 视角。
- 派生域/维度表（RepairDim/QuoteConversion/RenewalTracker + 各 dim）无省份维度 → G3 升格强调。
- **最重要**：D5 序列纠正（口径签字 + RLS-on 前置）—— 源于 §6.2，对齐本项目 Day-1 SOP。

**未动摇**：D1（单数据湖 + RLS 架构选型）两轮评审均未被反驳。被校准的是 0a 执行方案与论点措辞，非范式选型。

### 2026-06-21 · G3 维度表省份化 — loader 层落地（实证追加，非校准）

backlog `2026-06-21-claude-6ae4d7`（PARTIAL）· 分支 `claude/sx-standard-domains` · commit `5405cd8c`。

**已落地（loader 数据层）**：`salesman`/`plan`/`repair` 维度 loader 支持多省加载并注入 `branch_code`，沿用派生域联邦 RLS 范式（数据携带 `branch_code`，查询期由 RLS 过滤）。
- 纯函数 `buildBranchDimSelect`（`duckdb-domain-loaders.ts`）：**单源短路 = 历史 SQL 逐字节一致**（不加 `branch_code`、不 UNION）→ SC-only 默认部署字节安全（**按构造证明**——golden-baseline 当前 BLOCKED on `E2E_PASSWORD`，§8 已注明，故不依赖 API 层回归而靠 SQL 形态恒等 + 集成回归 + 全量 CI 直证）；多源 `UNION ALL BY NAME` + 缺列补 `'<省>' AS branch_code`。
- `data-bootstrapper.resolveBranchDimExtras` 探测 `warehouse/validation/<省>/dim/<域>/latest.parquet`：0a 期无副本 → 空 → 单源字节安全；GATED 多省副本就位 → 自动 UNION。
- **`brand`/`plate_region` 刻意保持全局**（省份无关查找表：车型库厂牌映射 / 车牌前缀→地区，各省都需查全表），federation 维持 exempt。校正 §5.1 G3 把"厂牌"列入待处理的措辞——运行期 brand 不需分省隔离（G1 的 validation/SX/brand 副本仅 ETL 完整性校验所需，非运行期隔离）。

**仍为后续（配 G4 派生域 branch_code）**：`SalesmanTeamMapping`/`SalesmanPlanFact`/`achievement_cache` 的 `branch_code` 传播 + typed 路由（`premium-plan`/`repair` 等）分省过滤；`RepairDim` 仍**不纳入** cx sql 联邦（其 `org_level_3` 编码格式安全决策不动，见 `sql-federation-policy.ts` 注释 + `sql-federation.test.ts` 越权回归）。

**降级兜底（临时·GATED 前补齐，决策 2026-06-21）**：SX 无业务员/计划源时不传 SX extra 源 → SX 保单业务员经 `buildAchievementView` Part B 落「未归属团队/未归属机构」、计划缺省 0；`plate_region` 用全局表。

**验证**：CI 单测 6（`buildBranchDimSelect`）+ 集成 4（多省加载 / 单省字节回归 / 分省过滤）+ `duckdb-dim-dedup` 回归 7 + 全量 CI 3423 全绿；typecheck 通过；`governance` 42/42。

### 2026-06-21 · G4 派生域 branch_code — loader 多省层落地（实证追加，非校准）

backlog `2026-06-21-claude-8571a6`（DONE）· 分支 `claude/sx-standard-domains` · commit `c6f09fba`。

**澄清 G4 范围**：派生视图的 branch_code **常量列**早由 P0.5（原 `selectWithBranchCode`）覆盖（4 个 federation direct 视图 permissionColumns 含 branch_code，见 `sql-federation.test.ts`）。既有 backlog `00bac8`/`c21667`/`e2240c` 实为**非 branch_code** 子问题（new_energy `org_level_3` 全 NULL / `is_telemarketing` RLS / renewal tonnage 字段）。本轮 G4 = 把单省**部署常量** branch_code 升级为多省**真实 per-row** branch_code（与 G3 维度层平行）。

**已落地（loader 多省层）**：`quote`/`cross_sell`/`new_energy`/`renewal` 4 个派生域 loader 支持多省加载。
- `selectUnionWithBranchCode`（取代原单源 `selectWithBranchCode`，**移除死代码**）：单源 = P0.5 历史补列行为逐字节一致（派生域恒含 branch_code，federation RLS 视图必备）；多源 `UNION ALL BY NAME` + 缺列补对应省份常量、已含者原样（SX validation 副本由 G1 ETL 注入真实 branch_code）。
- `data-bootstrapper.resolveBranchFactExtras` 探测 `validation/<省>/<域>/latest.parquet`（G1 已落 SX 的 quotes_conversion/renewal_tracker）；0a 期缺者 → 空 → 单源字节安全。`paths.getBranchValidationFactPath`。
- **`customer_flow` 派生自 PolicyFact 已含 branch_code**，无需改（GATED 时 PolicyFact 含 SX → CustomerFlow 自动继承）。

**仍为后续**：与 G3 后续合并 —— `SalesmanTeamMapping`/`achievement_cache` branch_code 传播 + typed 路由分省过滤；派生域 `is_telemarketing` RLS（`c21667`）、new_energy `org_level_3`（`00bac8`）、renewal tonnage（`e2240c`）为独立子问题。

**验证**：集成 4（`duckdb-branch-fact`：单省 P0.5 字节回归 / 多源补常量 / SX 携真实省份 / 分省过滤）；typecheck 通过；`governance` 42/42；全量 CI 3423 全绿。`domain-testcases` 5 失败为既有（`endorsement_no`/`coverage_combination` fixture 缺列，clean main 同样失败，非本变更）。

### 2026-06-21 · G3/G4 查询期收口 — typed 路由分省 RLS 落地（实证追加，非校准）

backlog `2026-06-21-claude-6ae4d7`（note 追加）· 分支 `claude/sx-rls-closeout`。承接 §11 上两条（G3/G4 loader 多省层）的「仍为后续」：把 loader 注入的 `branch_code` **在查询期真正下推**到 typed 路由，使 GATED 多省时 SX 用户被 branch RLS 隔离。

**① branch_code 传播（loader 层补完）**：`duckdb-domain-loaders.ts` 对 `SalesmanTeamMapping`/`SalesmanPlanFact`/`achievement_cache` 三表 **gated multiProvince** 注入 branch_code（单省=逐字节历史行为，字节安全；`achievement_cache` 为 `CREATE TABLE`，加列必 gated）。
- multiProvince 判定 = **DESCRIBE SalesmanDim 零假设实测**（含 branch_code ⟺ extraSalesmanSources 非空 ⟺ G3 多源 UNION 已激活），复用 G3 信号。
- `achievement_cache` 分省码来源：A1/A2 = `m.branch_code`（SalesmanTeamMapping 业务员归属省）；Part B（有保单无映射业务员）= `ytd_actual` 的 `MAX(branch_code)`（聚合列，1 行/业务员**不 fan-out** A1）。二次守卫 `branchAware` = multiProvince ∧ PolicyFact 实测含 branch_code 列（防 Part B 的 `MAX(branch_code)` 在 PolicyFact 缺列时 Binder Error）。

**② typed 路由分省过滤（双门控）**：新增 `routes/query/shared.ts:resolveBranchRlsCode(req, relation)`：
- **gate a** = `req.permissionFilter` 含 `branch_code='XX'`（⟺ `BRANCH_RLS_ENABLED=true` 且用户有 branchCode；middleware/permission.ts 注入）。flag=false（默认/生产）→ undefined → 不注入 → 字节安全。
- **gate b** = `information_schema` 实测目标关系含 branch_code 列（⟺ GATED 多省加载已激活）。**🔴 免疫 T-3 中间态**（Day-1 SOP 先 RLS-on 再载 SX：此刻 SC-only 单源关系无 branch_code 列）→ undefined → 不注入 → 不 Binder Error、SC 全量=零差异（与 §6.2 / golden-baseline 隔离证明的「SC 端点零差异」一致）。
- 接入：`premium-plan`/`plan-achievement`（premiumPlan.ts buildCacheWhere）、`kpi`（buildAchievementCacheWhere）、`comprehensive-bundle`、`performance-bundle`（top-salesman/drilldown via buildPlanScopeConds + org-heatmap 的 plan_by_dim CTE 直查 SalesmanTeamMapping）、`repair/*` 12 端点（buildRepairWhere = org 段 + branch 段）。`RepairDim` 仍**不纳入** cx sql 联邦（编码 org_level_3 安全决策不动）。

**仍为后续（非本 PR）**：`routes/filters.ts` `/filters/options` 直查 SalesmanTeamMapping（routes/ 非 routes/query/* 文件作用域外，越权泄漏跨省业务员-团队映射，留独立项）；`marketing-report`/`premium-report` 的 SalesmanTeamMapping/SalesmanPlanFact 标签子查询（仅 team_name 命名泄漏，非数据行泄漏，低优）。

**验证**：CI 单测 **3435 全绿**（+12 `branch-rls-injection` 注入纯函数：传 branchCode→含 `branch_code='SX'` / 不传→零注入字节安全）；集成 `duckdb-branch-dim` **7/7**（+3：三表单省无列字节回归 / 多省含列分省隔离 SX≠SC）+ `duckdb-branch-rls-resolve` **5/5**（双门控 + T-3 gate b 免疫 + 关系不存在安全降级）；typecheck；`governance` 42/42。`domain-testcases` 5 失败为既有（同上，非本变更）。`BRANCH_RLS_ENABLED=false` 默认逐字节不变（按构造单源恒等 + 集成回归证；golden-baseline 仍 BLOCKED on E2E_PASSWORD）。

### 2026-06-23 · Phase A 检测层收口 — 0a 前缀/常量标签**过渡定性**（实证追加，非校准）

backlog `2026-06-23-claude-bc36e8`（Phase A 全 DONE）· 分支 `claude/province-p5` · 规划唯一事实源 [省份派生化与子目录方案_2026-06-23.md](./省份派生化与子目录方案_2026-06-23.md)（P1/P3-A~E/P4/P5）。

**🔴 0a 过渡定性（本 ADR 此前未明示，Phase 5 补）**：D3 定的 **0a「省份前缀文件名 + 扁平目录 + rsync filter + ETL 常量标签 branch_code」是过渡方案，非终局**。Phase A（检测层）已用 **`branch_code` 从 `policy_no` 前缀派生（610→SC/618→SX，`fields.json` derivation.mapping 唯一事实源）+ ETL 自校验 fail-fast + governance 单文件不混省闸（PR #769 / #762~#767）** **替代 0a 的「ETL 常量标签 branch_code」检测层**——常量标签「文件喂错省 → 整列静默贴错、测不出混省」的脆弱点已被派生 + 自校验根治。

**过渡债务（诚实标注）**：
- **检测层已根治**：branch_code 是 `policy_no` 派生的省份真相，混省/贴错被治理闸检出（exit 1）。
- **隔离层仍是 0a 现状**：「扁平目录 + `SX_` 前缀 + rsync filter」（#753，commit `6858ee8d`）**未退役**——物理「删不错省」能力待 **Phase B（`current/<省>/` 子目录，高爆炸半径，动工前问用户）** 交付。即本期 = **检测层根治、隔离层维持 #753**。
- `SX_` 前缀现仅作 sync 文件区分用途，**不再是 branch_code 的事实来源**（事实来源已是 parquet 内派生省份）。

**RLS/loader 适配核对（Phase 5 ① · 零代码改动确认 · codex 双闸修正 overclaim）**：`permission.ts:88-99` RLS 注入语义不变（消费查询期 `branch_code` 等值过滤）。**branch_code 列供给分四类机制（非统一兜底，codex gpt-5.5 闸-1+闸-2 实读代码推翻原"全域双保险/三类"草案）**：① **PolicyFact** / ② **ClaimsDetail** 走 `read_parquet(union_by_name=true)` **无 loader 补列、必须 parquet 物理含列**（`duckdb-parquet-loader.ts:139` / `duckdb-domain-loaders.ts:687`）；③ **4 federation 派生视图** `selectUnionWithBranchCode:486-507` DESCRIBE 自适应**缺列补常量列兜底**；④ **维度/计划/达成缓存**（SalesmanDim/SalesmanTeamMapping/SalesmanPlanFact/achievement_cache）**gated 多源注入** branch_code（multiProvince+branchAware 时，单省零注入字节安全）。**RLS-on 硬前置 = Phase 4 backfill 全量物理补列**（`env.ts:127` 明示，非靠 loader 兜底）。typed 路由 RLS 闭合**分四档**：PolicyFact 直接 branch / **repair 普通端点 + premium-plan/kpi/performance/report 经 `resolveBranchRlsCode` gated branch 下推** / **renewal-tracker·cube 靠 `org_level_3` 降级**（branch_code 下推为 follow-up）/ **repair/orphan-shops RLS 面外**（`void whereClause`）。主目录实测：premium 已物理含列；claims_detail + 6 派生域 latest.parquet 缺列（旧产物，federation 视图有兜底、PolicyFact/ClaimsDetail RLS-on 前须 backfill 补列）。完整分层证据详见 [新增省份落点checklist_2026-06-23.md](./新增省份落点checklist_2026-06-23.md) §3。

**新增省份落点**（codex P1 区分两类，防 overclaim）：派生映射唯一落点 = `fields.json` derivation.mapping；**但**新增省份还涉及 ≥8 处运行时白名单（preset-users `getAllBranchCodes` / branch-names / sql-federation-policy / branch-org-mapping / sync-vps / 压测白名单），**不可声称「唯一落点 = fields.json」**。全清单见 [新增省份落点checklist_2026-06-23.md](./新增省份落点checklist_2026-06-23.md)。

**验证**：本 entry 为文档定性，无代码改动；`bun run governance` 绿；RLS/loader 核对证据见 checklist §3（duckdb 逐域 + 代码双证）。

### 2026-06-24 · D6 收敛 — 0a 隔离层终局定为子目录，超越 D3（决策，附完整复盘）

backlog `多省 Phase B`（D3-vs-PhaseB 分叉）· 决策人 = 项目主人 · 证据依据 = [D3-vs-PhaseB 分叉复盘_2026-06-24.md](./D3-vs-PhaseB分叉复盘_2026-06-24.md)（git 时间线 + 六站点逐一核账）。

**🔴 D6（§1 新增·超越 D3）**：**0a 隔离层终局 = 分省子目录 `current/<省>/`（每省独立 sync），退役 #753「扁平 + `SX_` 前缀 + rsync filter」**。检测层（`branch_code` 从 `policy_no` 派生）已 Phase A 根治、方案无关，不受影响。

**为何超越 D3（不是推翻干净决策，是收敛已存在的分叉）**：
- **D3 否决子目录的核心理由已失效**：D3（06-19）反对子目录的唯一硬理由是「装载层 ~6 处扁平假设、blast radius 超 0a 预算」。但 Phase B B1（#773）已用**零物理迁移 + 双闸休眠**把装载层（`data-bootstrapper` crux 站点）做成子目录感知 + GATED fail-closed，**该 blast radius 已被填平**——这条去险路径是 06-19 ADR 未预见的。
- **方向用户 06-22 已批准**：《省份派生化+分省子目录方案（替代 #753 前缀方案）》(#760) 署名「决策人=项目主人」；本 ADR §11 2026-06-23「过渡定性」已明示「D3 的前缀是过渡方案、非终局」。D6 只是把这个**已批准、已定性**的方向收敛进权威 §1，让 §1 不再与 §11/规划文档矛盾。

**为何此前撞车（系统性教训）**：§11 2026-06-23 的计划是「前缀 NOW（本期维持 #753）/ 子目录 LATER（Phase B）」串行；但执行中 #783（前缀 promote）与 Phase B B1/B2/B3（子目录）**同一天并行落地**，两个 loop 会话互不知情，B3 退役 `buildRsyncBranchFilterArgs` 而 #783 仍引用 → sync 层撞车、B3 暂停 (#785)。根因 = dispatch 无架构/程序轴 + §1 非真 SSOT（详见复盘 §六）。

**D6 后的收敛动作**：① 本 entry + D6 + D3 超越标记（本次，docs-only）；② B3 落地（b3 分支 `950267c6` 复活，退役前缀 sync）；③ 退役/隔离 #783 前缀 promote（SX 未 promote，安全），B5 cutover 前重建子目录版 SX promote；④ loop 机制（dispatch 架构轴 + contract-token preflight）作独立 follow-up，本分叉为其 worked example。

**不变**：D1（单数据湖 + RLS 范式）/ D2 / D4 / D5 全部不动；检测层（branch_code 派生 + governance 闸）不动；71/78 端点 SQL 文本不动；SC `current/` 今日扁平字节安全（subdirOnly 才切）。

**验证**：本 entry 为决策收敛，docs-only 无代码改动；复盘 §五 六站点核账经 grep/代码逐一核实；`bun run governance` 绿。
