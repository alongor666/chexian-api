# 指标字典

> 从指标注册表自动生成，不要手动编辑
>
> 生成命令：`bun scripts/metric-registry/generate-metric-doc.ts`
>
> 注册表位置：`server/src/config/metric-registry/`

## 概览

| 分类 | 数量 |
|------|------|
| 基础指标 | 7 |
| 比率指标 | 9 |
| 成本指标 | 20 |
| 交叉销售指标 | 4 |
| 增长指标 | 3 |
| 维修资源指标 | 6 |
| 计划达成指标 | 1 |
| 业务结构指标 | 1 |
| 续保分析指标 | 6 |
| 销售队伍业绩指标 | 3 |
| **总计** | **60** |

---

## 基础指标

### 总保费 (`total_premium`)

- **版本**: 1.0.0
- **标签**: core, kpi, foundation
- **单位**: 元
- **公式**: 所有保单保费之和
  - 分子: SUM(premium)
- **展示**: 总保费 (premiumWan, 万元)

```sql
SUM(premium) as total_premium
```

**变更历史**:
- v1.0.0 (2026-03-27): 从 kpi.ts 迁移

---

### 保单件数 (`policy_count`)

- **版本**: 1.0.0
- **标签**: core, kpi, foundation
- **单位**: 件
- **公式**: 去重保单计数
  - 分子: COUNT(DISTINCT policy_no)
- **展示**: 保单件数 (count, 件)

```sql
COUNT(DISTINCT policy_no) as policy_count
```

**变更历史**:
- v1.0.0 (2026-03-27): 从 kpi.ts 迁移

---

### 机构数 (`org_count`)

- **版本**: 1.0.0
- **标签**: kpi, foundation
- **单位**: 个
- **公式**: 去重三级机构计数
  - 分子: COUNT(DISTINCT org_level_3)
- **展示**: 机构数 (count, 个)

```sql
COUNT(DISTINCT org_level_3) as org_count
```

**变更历史**:
- v1.0.0 (2026-03-27): 从 kpi.ts 迁移

---

### 业务员数 (`salesman_count`)

- **版本**: 1.0.0
- **标签**: kpi, foundation
- **单位**: 人
- **公式**: 去重业务员计数
  - 分子: COUNT(DISTINCT salesman_name)
- **展示**: 业务员数 (count, 人)

```sql
COUNT(DISTINCT salesman_name) as salesman_count
```

**变更历史**:
- v1.0.0 (2026-03-27): 从 kpi.ts 迁移

---

### 人均保费 (`per_capita_premium`)

- **版本**: 1.0.0
- **标签**: core, kpi, foundation
- **单位**: 元
- **公式**: 保费总额 / 业务员数
  - 分子: SUM(premium)
  - 分母: COUNT(DISTINCT salesman_name)
- **展示**: 人均保费 (premiumWan, 万元)
- **注意**: NULLIF 防除零

```sql
SUM(premium) / NULLIF(COUNT(DISTINCT salesman_name), 0) as per_capita_premium
```

**变更历史**:
- v1.0.0 (2026-03-27): 从 kpi.ts 迁移

---

### 车均保费 (`per_vehicle_premium`)

- **版本**: 1.0.0
- **标签**: core, kpi, foundation
- **单位**: 元
- **公式**: 保费总额 / 去重车架号数（含商业险+交强险）
  - 分子: SUM(premium)
  - 分母: COUNT(DISTINCT vehicle_frame_no)
- **展示**: 车均保费 (average, 元)
- **注意**: 分母为去重车架号数，车架号为空时回退至保单号。一台车可能有交强险+商业险两张保单，故车均保费 > 件均保费

```sql
SUM(premium) / NULLIF(COUNT(DISTINCT COALESCE(NULLIF(TRIM(CAST(vehicle_frame_no AS VARCHAR)), ''), policy_no)), 0) AS per_vehicle_premium
```

**变更历史**:
- v1.0.0 (2026-04-02): 新增车均保费指标，分母为去重车架号数

---

### 件均保费 (`avg_premium_per_policy`)

- **版本**: 1.0.0
- **标签**: core, kpi, foundation
- **单位**: 元
- **公式**: 保费总额 / 保单件数（按保单号去重）
  - 分子: SUM(premium)
  - 分母: COUNT(DISTINCT policy_no)
- **展示**: 件均保费 (average, 元)
- **注意**: NULLIF 防除零。分母按 policy_no 去重（一张保单计一件）；区别于车均（车架号去重，一台车含交强+商业两单时车均>件均）、人均（业务员）。

```sql
SUM(premium) / NULLIF(COUNT(DISTINCT policy_no), 0) AS avg_premium_per_policy
```

**变更历史**:
- v1.0.0 (2026-06-27): 新增件均保费指标（保费÷保单件数），消除"人均/件均/总保费"三义（K4 治理链）

---

## 比率指标

### 过户率 (`transfer_rate`)

- **版本**: 2.0.0
- **标签**: kpi, ratio
- **单位**: %
- **公式**: 过户保单数 / 总保单数
  - 分子: COUNT(DISTINCT 剔除批改后的保单键 WHERE is_transfer)
  - 分母: COUNT(DISTINCT 剔除批改后的保单键)
- **展示**: 过户占比 (percent, %)

```sql
COUNT(DISTINCT CASE WHEN NULLIF(TRIM(CAST(endorsement_no AS VARCHAR)), '') IS NULL AND is_transfer THEN COALESCE(NULLIF(TRIM(CAST(policy_no AS VARCHAR)), ''), NULLIF(TRIM(CAST(vehicle_frame_no AS VARCHAR)), '')) END) * 1.0 / NULLIF(COUNT(DISTINCT CASE WHEN NULLIF(TRIM(CAST(endorsement_no AS VARCHAR)), '') IS NULL THEN COALESCE(NULLIF(TRIM(CAST(policy_no AS VARCHAR)), ''), NULLIF(TRIM(CAST(vehicle_frame_no AS VARCHAR)), '')) END), 0) as transfer_rate
```

**变更历史**:
- v2.0.0 (2026-06-12): 计数口径统一为业绩面板口径：剔除批改(endorsement_no 非空) + policy_key 去重；原 COUNT(*) 含批改多行与交强/商业各一行，同名指标两套值（用户 2026-06-12 裁决以面板口径为准）
- v1.0.0 (2026-03-27): 从 kpi.ts 迁移

---

### 电销率 (`telesales_rate`)

- **版本**: 1.0.0
- **标签**: kpi, ratio
- **单位**: %
- **公式**: 电销保单数 / 总保单数
  - 分子: COUNT(CASE WHEN is_telemarketing THEN 1 END)
  - 分母: COUNT(*)
- **展示**: 电销占比 (percent, %)

```sql
COUNT(CASE WHEN is_telemarketing THEN 1 END) * 1.0 / NULLIF(COUNT(*), 0) as telesales_rate
```

**变更历史**:
- v1.0.0 (2026-03-27): 从 kpi.ts 迁移

---

### 续保率 (`renewal_rate`)

- **版本**: 2.0.0
- **标签**: core, kpi, ratio, renewal
- **单位**: %
- **公式**: 续保保单数 / 总保单数
  - 分子: COUNT(DISTINCT 剔除批改后的保单键 WHERE is_renewal)
  - 分母: COUNT(DISTINCT 剔除批改后的保单键)
- **展示**: 续保占比 (percent, %)
- **注意**: 此为 KPI 面板的快速续保占比，非续保分析板块的精确续保率（后者基于起保日期到期判断）

```sql
COUNT(DISTINCT CASE WHEN NULLIF(TRIM(CAST(endorsement_no AS VARCHAR)), '') IS NULL AND is_renewal THEN COALESCE(NULLIF(TRIM(CAST(policy_no AS VARCHAR)), ''), NULLIF(TRIM(CAST(vehicle_frame_no AS VARCHAR)), '')) END) * 1.0 / NULLIF(COUNT(DISTINCT CASE WHEN NULLIF(TRIM(CAST(endorsement_no AS VARCHAR)), '') IS NULL THEN COALESCE(NULLIF(TRIM(CAST(policy_no AS VARCHAR)), ''), NULLIF(TRIM(CAST(vehicle_frame_no AS VARCHAR)), '')) END), 0) as renewal_rate
```

**变更历史**:
- v2.0.0 (2026-06-12): 计数口径统一为业绩面板口径：剔除批改(endorsement_no 非空) + policy_key 去重；原 COUNT(*) 含批改多行与交强/商业各一行，同名指标两套值（用户 2026-06-12 裁决以面板口径为准）
- v1.0.0 (2026-03-27): 从 kpi.ts 迁移

---

### 商业险占比 (`commercial_rate`)

- **版本**: 1.0.0
- **标签**: core, kpi, ratio
- **单位**: %
- **公式**: 商业保险保费 / 总保费
  - 分子: SUM(CASE WHEN insurance_type = '商业保险' THEN premium ELSE 0 END)
  - 分母: SUM(premium)
- **展示**: 商业险占比 (percent, %)

```sql
SUM(CASE WHEN insurance_type = '商业保险' THEN premium ELSE 0 END) * 1.0 / NULLIF(SUM(premium), 0) as commercial_rate
```

**变更历史**:
- v1.0.0 (2026-03-27): 从 kpi.ts 迁移

---

### 新能源率 (`nev_rate`)

- **版本**: 2.0.0
- **标签**: kpi, ratio
- **单位**: %
- **公式**: 新能源车保单数 / 总保单数
  - 分子: COUNT(DISTINCT 剔除批改后的保单键 WHERE is_nev)
  - 分母: COUNT(DISTINCT 剔除批改后的保单键)
- **展示**: 新能源占比 (percent, %)

```sql
COUNT(DISTINCT CASE WHEN NULLIF(TRIM(CAST(endorsement_no AS VARCHAR)), '') IS NULL AND is_nev THEN COALESCE(NULLIF(TRIM(CAST(policy_no AS VARCHAR)), ''), NULLIF(TRIM(CAST(vehicle_frame_no AS VARCHAR)), '')) END) * 1.0 / NULLIF(COUNT(DISTINCT CASE WHEN NULLIF(TRIM(CAST(endorsement_no AS VARCHAR)), '') IS NULL THEN COALESCE(NULLIF(TRIM(CAST(policy_no AS VARCHAR)), ''), NULLIF(TRIM(CAST(vehicle_frame_no AS VARCHAR)), '')) END), 0) as nev_rate
```

**变更历史**:
- v2.0.0 (2026-06-12): 计数口径统一为业绩面板口径：剔除批改(endorsement_no 非空) + policy_key 去重；原 COUNT(*) 含批改多行与交强/商业各一行，同名指标两套值（用户 2026-06-12 裁决以面板口径为准）
- v1.0.0 (2026-03-27): 从 kpi.ts 迁移

---

### 新车率 (`new_car_rate`)

- **版本**: 2.0.0
- **标签**: kpi, ratio
- **单位**: %
- **公式**: 新车保单数 / 总保单数
  - 分子: COUNT(DISTINCT 剔除批改后的保单键 WHERE is_new_car)
  - 分母: COUNT(DISTINCT 剔除批改后的保单键)
- **展示**: 新车占比 (percent, %)

```sql
COUNT(DISTINCT CASE WHEN NULLIF(TRIM(CAST(endorsement_no AS VARCHAR)), '') IS NULL AND is_new_car THEN COALESCE(NULLIF(TRIM(CAST(policy_no AS VARCHAR)), ''), NULLIF(TRIM(CAST(vehicle_frame_no AS VARCHAR)), '')) END) * 1.0 / NULLIF(COUNT(DISTINCT CASE WHEN NULLIF(TRIM(CAST(endorsement_no AS VARCHAR)), '') IS NULL THEN COALESCE(NULLIF(TRIM(CAST(policy_no AS VARCHAR)), ''), NULLIF(TRIM(CAST(vehicle_frame_no AS VARCHAR)), '')) END), 0) as new_car_rate
```

**变更历史**:
- v2.0.0 (2026-06-12): 计数口径统一为业绩面板口径：剔除批改(endorsement_no 非空) + policy_key 去重；原 COUNT(*) 含批改多行与交强/商业各一行，同名指标两套值（用户 2026-06-12 裁决以面板口径为准）
- v1.0.0 (2026-03-27): 从 kpi.ts 迁移

---

### 优质业务率 (`quality_business_rate`)

- **版本**: 1.0.0
- **标签**: core, kpi, ratio
- **单位**: %
- **公式**: 优质业务保单数 / 总保单数
  - 分子: 非新能源客车+优质货车计数
  - 分母: COUNT(*)
- **展示**: 优质业务占比 (percent, %)
- **注意**: 优质业务定义：(1) 非新能源+非营业个人/企业/机关客车 (2) 货车+1吨以下或2-9吨

```sql
COUNT(CASE WHEN (
    (is_nev = false AND (
      customer_category LIKE '%非营业个人%'
      OR customer_category LIKE '%企业%'
      OR customer_category LIKE '%机关%'
    ))
    OR
    (customer_category LIKE '%货车%' AND tonnage_segment IN ('1吨以下', '2-9吨'))
  ) THEN 1 END) * 1.0 / NULLIF(COUNT(*), 0) as quality_business_rate
```

**变更历史**:
- v1.0.0 (2026-03-27): 从 kpi.ts 迁移

---

### 商业险投保率 (`commercial_insurance_rate`)

- **版本**: 1.0.0
- **标签**: kpi, ratio
- **单位**: %
- **公式**: 商业险件数 / 交强险件数
  - 分子: COUNT(CASE WHEN insurance_type LIKE '%商业%' THEN 1 END)
  - 分母: COUNT(CASE WHEN insurance_type = '交强险' THEN 1 END)
- **展示**: 商业险投保率 (percent, %)
- **注意**: 商业险投保率 = 商业险件数 / 交强险件数，衡量客户商业险投保意愿

```sql
COUNT(CASE WHEN insurance_type LIKE '%商业%' THEN 1 END) * 1.0 / NULLIF(COUNT(CASE WHEN insurance_type = '交强险' THEN 1 END), 0) as commercial_insurance_rate
```

**变更历史**:
- v1.0.0 (2026-03-27): 从 kpi.ts 迁移

---

### 承保率（报价→承保） (`underwriting_rate`)

- **版本**: 1.0.0
- **标签**: quote-conversion, ratio, underwriting
- **单位**: %
- **公式**: 承保件数 / 报价件数（单据级）
  - 分子: 已承保报价单数
  - 分母: 全部报价单数
- **展示**: 承保率 (percent, %)
- **注意**: 仅适用于 QuoteConversion VIEW（04_报价清单 Parquet），单据级统计。

```sql
ROUND(100.0 * COUNT(CASE WHEN is_underwritten = '承保' THEN 1 END) / NULLIF(COUNT(*), 0), 1) as underwriting_rate
```

**变更历史**:
- v1.0.0 (2026-04-18): 新增；从 quote-conversion.ts 硬编码公式 conversion_rate 归位到注册表。旧别名 conversion_rate 暂保留用于向后兼容。

---

## 成本指标

### 满期赔付率 (`earned_claim_ratio`)

- **版本**: 2.1.0
- **标签**: core, kpi, cost
- **单位**: %
- **公式**: 已报告赔款 / 满期保费（闰年感知）
  - 分子: SUM(reported_claims)
  - 分母: SUM(premium * earned_days / policy_term)
- **展示**: 赔付率 (percent, %)
- **注意**: 赔案口径：report_time < 观察截止（MAX(report_time)），已结案(settlement_time<观察点)取settled_amount，否则取reserve_amount。保单口径：按policy_no聚合净保费>0。分母闰年感知：policy_term = DATEDIFF(起期, 起期+1年) = 365或366；earned_days = MIN(已过天数, policy_term)，退保保单截止于退保日。不在 SQL 内 ROUND，避免与前端 display.decimals 双重舍入；前端按 decimals=1 单次舍入。

```sql
CASE
    WHEN SUM(premium * CAST(earned_days AS DOUBLE) / CAST(policy_term AS DOUBLE)) > 0
    THEN SUM(reported_claims) * 100.0 / SUM(premium * CAST(earned_days AS DOUBLE) / CAST(policy_term AS DOUBLE))
    ELSE NULL
  END AS earned_claim_ratio
```

**变更历史**:
- v1.0.0 (2026-03-27): 从 cost.ts 迁移
- v1.1.0 (2026-04-11): 口径修正：赔案锚点改为 report_time，已决/未决按 settlement_time 分类取值，保单净保费聚合排除完全退保，截止日期改为 MAX(report_time)
- v2.0.0 (2026-04-17): 铁律对齐：分母从 exposure_days/365 改为 earned_days/policy_term（闰年感知）；展示精度 2→1 位小数
- v2.1.0 (2026-07-04): 消除 SQL ROUND(2) 与前端 decimals=1 的双重舍入：移除 SQL 预舍入，前端单次舍入，消除双重舍入偏差

---

### 费用率 (`expense_ratio`)

- **版本**: 1.2.0
- **标签**: kpi, cost
- **单位**: %
- **公式**: 费用金额 / 签单保费（保单明细域 fee_amount，不混同综合费用率/综合成本率）
  - 分子: SUM(fee_amount)
  - 分母: SUM(premium)
- **展示**: 费用率 (percent, %)
- **注意**: 不在 SQL 内 ROUND，避免与前端 display.decimals 双重舍入；前端按 decimals=1 单次舍入。

```sql
CASE
    WHEN SUM(premium) > 0
    THEN SUM(COALESCE(fee_amount, 0)) * 100.0 / SUM(premium)
    ELSE NULL
  END AS expense_ratio
```

**变更历史**:
- v1.0.0 (2026-03-27): 从 cost.ts 迁移
- v1.1.0 (2026-04-17): 铁律对齐：展示精度 2→1 位小数
- v1.2.0 (2026-05-01): 消除 SQL ROUND(2) 与前端 decimals=1 的双重舍入；补充 tooltip 与 sql.notes 明确分母为签单保费、与综合费用率/综合成本率不同源

---

### 案均赔款 (`avg_claim_amount`)

- **版本**: 1.0.0
- **标签**: cost
- **单位**: 元
- **公式**: 已报告赔款 / 赔案件数
  - 分子: SUM(reported_claims)
  - 分母: SUM(claim_cases)
- **展示**: 案均赔款 (premiumWan, 万元)

```sql
CASE
    WHEN SUM(claim_cases) > 0
    THEN ROUND(SUM(reported_claims) / CAST(SUM(claim_cases) AS DOUBLE), 2)
    ELSE NULL
  END AS avg_claim_amount
```

**变更历史**:
- v1.0.0 (2026-03-27): 从 cost.ts 迁移

---

### 满期保费 (`earned_premium`)

- **版本**: 2.0.0
- **标签**: cost
- **单位**: 元
- **公式**: 保费 × 满期天数 / 保险期限天数（闰年感知）
  - 分子: SUM(premium * earned_days / policy_term)
- **展示**: 满期保费 (premiumWan, 万元)
- **注意**: 闰年感知：policy_term = DATEDIFF(起期, 起期+1年) = 365或366天。CTE 中预算 policy_term 和 earned_days

```sql
ROUND(SUM(premium * CAST(earned_days AS DOUBLE) / CAST(policy_term AS DOUBLE)), 2) AS earned_premium
```

**变更历史**:
- v1.0.0 (2026-03-27): 从 cost.ts 迁移

---

### 满期率 (`maturity_rate`)

- **版本**: 1.0.0
- **标签**: core, kpi, cost
- **单位**: %
- **公式**: 满期保费 / 同口径签单保费（闰年感知）
  - 分子: SUM(premium * earned_days / policy_term)
  - 分母: SUM(premium)
- **展示**: 满期率 (percent, %)
- **注意**: 与满期保费使用同一保单工作集和观察截止日；policy_term 为 365/366 天，禁止固定按 365 天折算。不在 SQL 内 ROUND，前端按 1 位小数单次舍入。

```sql
CASE
    WHEN SUM(premium) > 0
    THEN SUM(premium * CAST(earned_days AS DOUBLE) / CAST(policy_term AS DOUBLE)) * 100.0 / SUM(premium)
    ELSE NULL
  END AS maturity_rate
```

**变更历史**:
- v1.0.0 (2026-07-19): 新增 Dashboard 满期率 KPI，复用满期保费闰年感知口径

---

### 基准保费 (`baseline_premium`)

- **版本**: 1.0.0
- **标签**: cost, pricing, baseline
- **单位**: 元
- **公式**: 基准保费：商业险按商车自主定价系数归一，非商业险保持原保费
  - 分子: 商业险 premium / commercial_pricing_factor；非商业险 premium
- **展示**: 基准保费 (premiumWan, 万元)
- **注意**: 先计算基准保费，再进入满期口径。商业险自主系数缺失、为0或负数时输出 NULL，调用方应单独输出异常数据；交强险等非商业险不做自主系数折算。

```sql
CASE
    WHEN insurance_type = '商业保险' AND commercial_pricing_factor > 0
    THEN premium / NULLIF(commercial_pricing_factor, 0)
    WHEN insurance_type = '商业保险'
    THEN NULL
    ELSE premium
  END AS baseline_premium
```

**变更历史**:
- v1.0.0 (2026-05-12): 新增：商业险价格因子归一的基准保费，作为满期基准保费的前置层

---

### 满期基准保费 (`baseline_earned_premium`)

- **版本**: 1.0.0
- **标签**: cost, pricing, baseline
- **单位**: 元
- **公式**: 满期基准保费 = 基准保费 × 满期天数 / 保险期限天数（闰年感知）
  - 分子: SUM(baseline_premium * earned_days / policy_term)
- **展示**: 满期基准保费 (premiumWan, 万元)
- **注意**: 自包含表达式：内联 baseline_premium CASE 以避免别名引用（domain-testcases 测试单独消费）。对于已完全满期保单，earned_days = policy_term，满期基准保费等于基准保费。

```sql
ROUND(SUM(
    (CASE
      WHEN insurance_type = '商业保险' AND commercial_pricing_factor > 0
      THEN premium / NULLIF(commercial_pricing_factor, 0)
      WHEN insurance_type = '商业保险'
      THEN NULL
      ELSE premium
    END) * CAST(earned_days AS DOUBLE) / CAST(policy_term AS DOUBLE)
  ), 2) AS baseline_earned_premium
```

**变更历史**:
- v1.0.0 (2026-05-12): 新增：基准保费进入满期口径后的分母指标

---

### 满期基准赔付率 (`baseline_earned_claim_ratio`)

- **版本**: 1.0.0
- **标签**: cost, pricing, baseline
- **单位**: %
- **公式**: 满期基准赔付率 = 已报告赔款 / 满期基准保费
  - 分子: SUM(reported_claims)
  - 分母: SUM(baseline_earned_premium)
- **展示**: 满期基准赔付率 (percent, %)
- **注意**: 自包含表达式：内联满期基准保费分母以避免别名引用。分子分母使用一致的有效样本过滤（商业险无效自主系数记录在分子分母都排除）。

```sql
CASE
    WHEN SUM(
      (CASE
        WHEN insurance_type = '商业保险' AND commercial_pricing_factor > 0
        THEN premium / NULLIF(commercial_pricing_factor, 0)
        WHEN insurance_type = '商业保险'
        THEN NULL
        ELSE premium
      END) * CAST(earned_days AS DOUBLE) / CAST(policy_term AS DOUBLE)
    ) > 0
    THEN ROUND(SUM(
      CASE
        WHEN insurance_type = '商业保险' AND NOT (commercial_pricing_factor > 0)
        THEN NULL
        ELSE reported_claims
      END
    ) * 100.0 / SUM(
      (CASE
        WHEN insurance_type = '商业保险' AND commercial_pricing_factor > 0
        THEN premium / NULLIF(commercial_pricing_factor, 0)
        WHEN insurance_type = '商业保险'
        THEN NULL
        ELSE premium
      END) * CAST(earned_days AS DOUBLE) / CAST(policy_term AS DOUBLE)
    ), 2)
    ELSE NULL
  END AS baseline_earned_claim_ratio
```

**变更历史**:
- v1.0.0 (2026-05-12): 新增：基于满期基准保费的价格因子归一赔付率

---

### 变动成本率 (`variable_cost_ratio`)

- **版本**: 2.1.0
- **标签**: core, kpi, cost
- **单位**: %
- **公式**: 满期赔付率 + 费用率（两个分母不同；闰年感知）
  - 分子: 已报告赔款/满期保费 + 费用金额/签单保费
- **展示**: 变动成本率 (percent, %)
- **注意**: 赔付率分母=满期保费（premium × earned_days / policy_term，闰年感知），费用率分母=签单保费。可超100%（亏损）。不在 SQL 内 ROUND，避免与前端 display.decimals 双重舍入；前端按 decimals=1 单次舍入。

```sql
CASE
    WHEN SUM(premium * CAST(earned_days AS DOUBLE) / CAST(policy_term AS DOUBLE)) > 0 AND SUM(premium) > 0
    THEN
      SUM(reported_claims) * 100.0 / SUM(premium * CAST(earned_days AS DOUBLE) / CAST(policy_term AS DOUBLE)) +
      SUM(COALESCE(fee_amount, 0)) * 100.0 / SUM(premium)
    ELSE NULL
  END AS variable_cost_ratio
```

**变更历史**:
- v1.0.0 (2026-03-27): 新增，与 cost.ts:generateVariableCostQuery 一致
- v2.0.0 (2026-04-17): 铁律对齐：赔付分母 exposure_days/365 → earned_days/policy_term；展示精度 2→1 位小数
- v2.1.0 (2026-07-04): 消除 SQL ROUND(2) 与前端 decimals=1 的双重舍入：移除 SQL 预舍入，前端单次舍入，消除双重舍入偏差

---

### 满期出险率 (`earned_loss_frequency`)

- **版本**: 2.2.0
- **标签**: cost
- **单位**: %
- **公式**: (赔案件数/保单件数) × (保险期限天数/满期天数)。满期后=赔案/保单，未满期年化放大
  - 分子: SUM(claim_cases × policy_term / earned_days)
  - 分母: COUNT(DISTINCT policy_no)
- **展示**: 出险率 (percent, %)
- **注意**: 闰年感知：policy_term = DATEDIFF(起期, 起期+1年) = 365或366天。earned_days = MIN(已过天数, policy_term)。满期后 ratio=1，未满期 ratio>1。不在 SQL 内 ROUND，避免与前端 display.decimals 双重舍入；前端按 decimals=1 单次舍入。

```sql
CASE
    WHEN COUNT(DISTINCT policy_no) > 0 AND SUM(earned_days) > 0
    THEN SUM(claim_cases * 1.0 * policy_term / NULLIF(earned_days, 0))
      / COUNT(DISTINCT policy_no) * 100.0
    ELSE NULL
  END AS earned_loss_frequency
```

**变更历史**:
- v1.0.0 (2026-03-27): 从 cost.ts 迁移
- v2.0.0 (2026-03-31): 口径修正：保单级→年化公式，闰年感知(365/366)
- v2.1.0 (2026-04-17): 铁律对齐：展示精度 2→1 位小数
- v2.2.0 (2026-07-04): 消除 SQL ROUND(2) 与前端 decimals=1 的双重舍入：移除 SQL 预舍入，前端单次舍入，消除双重舍入偏差

---

### 满期边际贡献额 (`earned_margin_amount`)

- **版本**: 2.0.0
- **标签**: core, kpi, cost, margin
- **单位**: 元
- **公式**: 满期保费 × (1 - 已报告赔款/满期保费 - 费用金额/签单保费)（闰年感知）
  - 分子: earned_premium × (1 - earned_claim_ratio - expense_ratio)
- **展示**: 满期边际贡献额 (premiumWan, 万元)
- **注意**: 基于已赚保费的实际边际贡献，随满期天数增长而变化。已满期保单与 projected_margin_amount 相等。闰年感知分母

```sql
CASE
    WHEN SUM(premium * CAST(earned_days AS DOUBLE) / CAST(policy_term AS DOUBLE)) > 0 AND SUM(premium) > 0
    THEN ROUND(
      SUM(premium * CAST(earned_days AS DOUBLE) / CAST(policy_term AS DOUBLE)) * (
        1.0
        - SUM(reported_claims) / SUM(premium * CAST(earned_days AS DOUBLE) / CAST(policy_term AS DOUBLE))
        - SUM(COALESCE(fee_amount, 0)) / SUM(premium)
      ), 2
    )
    ELSE NULL
  END AS earned_margin_amount
```

**变更历史**:
- v1.0.0 (2026-03-31): 新增：时序对比核心指标，诊断脚本同步使用
- v2.0.0 (2026-04-17): 铁律对齐：赔付分母 exposure_days/365 → earned_days/policy_term

---

### 预估边际贡献额 (`projected_margin_amount`)

- **版本**: 2.0.0
- **标签**: core, kpi, cost, margin
- **单位**: 元
- **公式**: 签单保费 × (1 - 已报告赔款/满期保费 - 费用金额/签单保费)（闰年感知）
  - 分子: premium × (1 - earned_claim_ratio - expense_ratio)
- **展示**: 预估边际贡献额 (premiumWan, 万元)
- **注意**: 假设全部保费赚完后的预估边际贡献，用于预判最终盈亏。与 earned_margin_amount 共享变动成本率，仅保费基数不同。闰年感知分母

```sql
CASE
    WHEN SUM(premium * CAST(earned_days AS DOUBLE) / CAST(policy_term AS DOUBLE)) > 0 AND SUM(premium) > 0
    THEN ROUND(
      SUM(premium) * (
        1.0
        - SUM(reported_claims) / SUM(premium * CAST(earned_days AS DOUBLE) / CAST(policy_term AS DOUBLE))
        - SUM(COALESCE(fee_amount, 0)) / SUM(premium)
      ), 2
    )
    ELSE NULL
  END AS projected_margin_amount
```

**变更历史**:
- v1.0.0 (2026-03-31): 新增：时序对比核心指标，诊断脚本同步使用
- v2.0.0 (2026-04-17): 铁律对齐：赔付分母 exposure_days/365 → earned_days/policy_term

---

### 综合费用率 (`comprehensive_expense_ratio`)

- **版本**: 1.1.0
- **标签**: core, kpi, cost
- **单位**: %
- **公式**: (已报告赔款 + 费用金额) / 满期保费（闰年感知）
  - 分子: SUM(reported_claims) + SUM(fee_amount)
  - 分母: SUM(premium * earned_days / policy_term)
- **展示**: 综合费用率 (percent, %)
- **注意**: 与变动成本率的区别：分子费用金额基于已赚口径统一除满期保费，适合综合费用对标。闰年感知分母。不在 SQL 内 ROUND，避免与前端 display.decimals 双重舍入；前端按 decimals=1 单次舍入。

```sql
CASE
    WHEN SUM(premium * CAST(earned_days AS DOUBLE) / CAST(policy_term AS DOUBLE)) > 0
    THEN (SUM(reported_claims) + SUM(COALESCE(fee_amount, 0))) * 100.0
        / SUM(premium * CAST(earned_days AS DOUBLE) / CAST(policy_term AS DOUBLE))
    ELSE NULL
  END AS comprehensive_expense_ratio
```

**变更历史**:
- v1.0.0 (2026-04-17): 新增：综合分析补齐，分母闰年感知（earned_days/policy_term）
- v1.1.0 (2026-07-04): 消除 SQL ROUND(2) 与前端 decimals=1 的双重舍入：移除 SQL 预舍入，前端单次舍入，消除双重舍入偏差

---

### 固定成本额 (`fixed_cost_amount`)

- **版本**: 1.0.1
- **标签**: cost, fixed-cost
- **单位**: 元
- **公式**: 附加税费额 + 推动费额 + 管理费额（均为绝对值聚合）
  - 分子: SUM(保费×1.5%) + SUM(交强保费×0.15% + 商业保费×0.4%) + Σ(各机构保费×管理成本比例)
- **展示**: 固定成本额 (premiumWan, 万元)
- **注意**: L4 计算。固定成本参数来自 数据管理/config/fixed-cost-params.json。聚合时必须先算绝对值分子分母再除，禁止率值相加

```sql
-- L4 计算，由诊断脚本 diagnose_vehicle.py 通过 fixed_cost_config.py 动态生成 SQL
```

**变更历史**:
- v1.0.0 (2026-04-02): 新增：固定成本三分项绝对值，参数来自 fixed-cost-params.json
- v1.0.1 (2026-07-05): B274：附加税费率(1.5%)统一由 fixed-cost-params.json SSOT 派生（server/src/config/fixed-cost-params.ts 常量，earned-premium-detail.ts 修正离群硬编码 1.6%→1.5%）。税率动态，暂定值待精算口径

---

### 固定成本率 (`fixed_cost_ratio`)

- **版本**: 1.0.0
- **标签**: cost, fixed-cost
- **单位**: %
- **公式**: 固定成本额 / 满期保费
  - 分子: SUM(fixed_cost_amount)
  - 分母: SUM(earned_premium)
- **展示**: 固定成本率 (percent, %)
- **注意**: L4 计算。由诊断脚本 diagnose_vehicle.py 自动输出。率值必须从绝对值计算，禁止率值相加

```sql
-- L4 计算，fixed_cost_amount / earned_premium
```

**变更历史**:
- v1.0.0 (2026-04-02): 新增：固定成本占比

---

### 综合成本额 (`combined_cost_amount`)

- **版本**: 1.0.0
- **标签**: core, kpi, cost
- **单位**: 元
- **公式**: 变动成本额 + 固定成本额 = 已报告赔款 + 费用金额 + 固定成本额
  - 分子: SUM(reported_claims) + SUM(fee_amount) + SUM(fixed_cost)
- **展示**: 综合成本额 (premiumWan, 万元)
- **注意**: L4 计算。绝对值相加，诊断脚本自动输出

```sql
-- L4 计算
```

**变更历史**:
- v1.0.0 (2026-04-02): 新增：全口径成本绝对值

---

### 综合成本率 (`combined_cost_ratio`)

- **版本**: 1.1.0
- **标签**: core, kpi, cost
- **单位**: %
- **公式**: 综合成本额 / 满期保费（绝对值除法，非率值相加）
  - 分子: SUM(combined_cost_amount)
  - 分母: SUM(earned_premium)
- **展示**: 综合成本率 (percent, %)
- **注意**: L4 计算。≤100% 盈利，>100% 亏损。亮灯：≤99% 🟢 / 99-101% 🔵 / 101-105% 🟡 / >105% 🔴

```sql
-- L4 计算，combined_cost_amount / earned_premium
```

**变更历史**:
- v1.0.0 (2026-04-02): 新增：全口径成本率，含固定成本三分项（附加税费+推动费+管理费）
- v1.1.0 (2026-04-17): 铁律对齐：分母满期保费统一 earned_days/policy_term 闰年感知（依赖 earned_premium v2.0.0）

---

### 利润额 (`earned_profit_amount`)

- **版本**: 1.0.0
- **标签**: core, kpi, cost, profit
- **单位**: 元
- **公式**: 满期保费 - 综合成本额 = 边际贡献额 - 固定成本额
  - 分子: SUM(earned_premium) - SUM(combined_cost_amount)
- **展示**: 利润额 (premiumWan, 万元)
- **注意**: L4 计算。真实盈亏 = 边际贡献额 - 固定成本额。与边际贡献额并存：边际贡献额反映承保品质，利润额反映真实盈亏

```sql
-- L4 计算，earned_premium - combined_cost_amount
```

**变更历史**:
- v1.0.0 (2026-04-02): 新增：全口径利润，含固定成本（管理费+推动费+附加税费）

---

### 人伤案件占比 (`bi_case_ratio_pct`)

- **版本**: 1.0.0
- **标签**: cost, bodily-injury
- **单位**: %
- **公式**: 人伤案件数 / 总案件数（claims_detail 底表）
  - 分子: SUM(CAST(is_bodily_injury AS INT))
  - 分母: COUNT(*)
- **展示**: 人伤案占比 (percent, %)
- **注意**: 作用于 claims_detail 原始底表（非 policy 聚合 CTE）。is_bodily_injury 老案可能 NULL，COALESCE FALSE 兜底。整体实测约 10.7%（74.8 万案 / 8.01 万人伤）。

```sql
CASE
    WHEN COUNT(*) > 0
    THEN 100.0 * SUM(CAST(COALESCE(is_bodily_injury, FALSE) AS INT)) / COUNT(*)
    ELSE NULL
  END AS bi_case_ratio_pct
```

**变更历史**:
- v1.0.0 (2026-05-16): 由 diagnose-loss-development 引入；与 bi_amount_ratio_pct 配对呈现"频率轻/金额重"的人伤特性

---

### 人伤赔款占比 (`bi_amount_ratio_pct`)

- **版本**: 1.0.0
- **标签**: cost, bodily-injury
- **单位**: %
- **公式**: (人伤已决 + 人伤未决) / (总已决 + 总未决)（claims_detail 底表）
  - 分子: SUM(settled_bodily_amount) + SUM(reserve_bodily_amount)
  - 分母: SUM(settled_amount) + SUM(reserve_amount)
- **展示**: 人伤金额占比 (percent, %)
- **注意**: 作用于 claims_detail 原始底表。未决金额用 reserve_amount（项目标准口径，非 pending_amount，参 feedback_pending_vs_reserve_amount）。整体实测约 53.95%（人伤案件占比仅 10.7%，但占用 54% 赔款金额—严重性放大 5 倍）。

```sql
CASE
    WHEN (SUM(settled_amount) + SUM(reserve_amount)) > 0
    THEN 100.0 * (SUM(settled_bodily_amount) + SUM(reserve_bodily_amount)) / (SUM(settled_amount) + SUM(reserve_amount))
    ELSE NULL
  END AS bi_amount_ratio_pct
```

**变更历史**:
- v1.0.0 (2026-05-16): 由 diagnose-loss-development 引入；与 bi_case_ratio_pct 配对呈现"频率轻/金额重"的人伤特性

---

## 交叉销售指标

### 整体推介率 (`cross_sell_total_rate`)

- **版本**: 2.0.0
- **标签**: kpi, cross_sell
- **单位**: %
- **公式**: 驾意险推介件数 / 商业险出单件数（分子分母均限主全+交三，不含纯交强/单交）
  - 分子: SUM(driver_count) WHERE coverage_combination IN ('主全','交三')
  - 分母: SUM(auto_count) WHERE coverage_combination IN ('主全','交三')
- **展示**: 推介率 (percent, %)
- **注意**: 使用 CrossSellDailyAgg 聚合表中的预计算字段。红线（business-domain.md）：推介率分母为商业险出单件数（主全+交三），不含纯交强/单交；与 cross-sell.ts total_auto_count/total_driver_count 同口径。

```sql
CASE
    WHEN SUM(CASE WHEN coverage_combination IN ('主全', '交三') THEN auto_count ELSE 0 END) > 0
    THEN ROUND(
      SUM(CASE WHEN coverage_combination IN ('主全', '交三') THEN driver_count ELSE 0 END) * 100.0
      / SUM(CASE WHEN coverage_combination IN ('主全', '交三') THEN auto_count ELSE 0 END), 2)
    ELSE 0
  END AS total_rate
```

**变更历史**:
- v2.0.0 (2026-06-12): 分子分母限定 coverage_combination IN (主全,交三)：原对全险别求和（分母含单交，稀释整体推介率），违反推介率分母红线，与 SQL 实现 cross-sell.ts 不一致。
- v1.0.0 (2026-03-27): 从 cross-sell.ts 迁移

---

### 单交推介率 (`cross_sell_danjiao_rate`)

- **版本**: 1.0.0
- **标签**: cross_sell
- **单位**: %
- **公式**: 单交下驾意险件数 / 单交车险件数
  - 分子: SUM(danjiao_driver_count)
  - 分母: SUM(danjiao_auto_count)
- **展示**: 单交推介率 (percent, %)

```sql
CASE
    WHEN SUM(danjiao_auto_count) > 0
    THEN ROUND(SUM(danjiao_driver_count) * 100.0 / SUM(danjiao_auto_count), 2)
    ELSE 0
  END AS danjiao_rate
```

**变更历史**:
- v1.0.0 (2026-03-27): 从 cross-sell.ts 迁移

---

### 交三推介率 (`cross_sell_jiaosan_rate`)

- **版本**: 1.1.0
- **标签**: cross_sell
- **单位**: %
- **公式**: 交三下驾意险件数 / 交三车险件数
  - 分子: SUM(jiaosan_driver_count)
  - 分母: SUM(jiaosan_auto_count)
- **展示**: 交三推介率 (percent, %)

```sql
CASE
    WHEN SUM(jiaosan_auto_count) > 0
    THEN ROUND(SUM(jiaosan_driver_count) * 100.0 / SUM(jiaosan_auto_count), 2)
    ELSE 0
  END AS jiaosan_rate
```

**变更历史**:
- v1.1.0 (2026-07-07): 新增四级亮灯阈值（70/65/60，lower_worse）：原前端 crossSellRateStatus.ts 硬编码阈值上收注册表为事实源，前端保留镜像常量并由 tests/cross-sell-rate-status.test.ts 同步用例锁定。公式无变化。
- v1.0.0 (2026-03-27): 从 cross-sell.ts 迁移

---

### 主全推介率 (`cross_sell_zhuquan_rate`)

- **版本**: 1.1.0
- **标签**: cross_sell
- **单位**: %
- **公式**: 主全下驾意险件数 / 主全车险件数
  - 分子: SUM(zhuquan_driver_count)
  - 分母: SUM(zhuquan_auto_count)
- **展示**: 主全推介率 (percent, %)

```sql
CASE
    WHEN SUM(zhuquan_auto_count) > 0
    THEN ROUND(SUM(zhuquan_driver_count) * 100.0 / SUM(zhuquan_auto_count), 2)
    ELSE 0
  END AS zhuquan_rate
```

**变更历史**:
- v1.1.0 (2026-07-07): 新增四级亮灯阈值（80/75/70，lower_worse）：原前端 crossSellRateStatus.ts 硬编码阈值上收注册表为事实源，前端保留镜像常量并由 tests/cross-sell-rate-status.test.ts 同步用例锁定。公式无变化。
- v1.0.0 (2026-03-27): 从 cross-sell.ts 迁移

---

## 增长指标

### 同比增长率 (`growth_rate_yoy`)

- **版本**: 1.0.0
- **标签**: kpi, growth
- **单位**: %
- **公式**: (本期 - 去年同期) / 去年同期
  - 分子: current_value - previous_value
  - 分母: previous_value
- **展示**: 同比增长率 (percent, %)
- **注意**: current_value 和 previous_value 需要在外层 CTE 中预计算

```sql
CASE
    WHEN previous_value > 0
    THEN ROUND((current_value - previous_value) * 100.0 / previous_value, 2)
    ELSE NULL
  END AS growth_rate_yoy
```

**变更历史**:
- v1.0.0 (2026-03-27): 从 growth.ts 迁移

---

### 保费增长率 (`premium_growth_pct`)

- **版本**: 1.0.0
- **标签**: kpi, growth, alert, branch-ops
- **单位**: %
- **公式**: (本期签单保费 − 去年同期签单保费) × 100 ÷ 去年同期签单保费
  - 分子: current_value - previous_value
  - 分母: previous_value
- **展示**: 保费增长率 (percent, %)
- **注意**: current_value = SUM(premium) 当期；previous_value = 去年同期 SUM(premium)，需在外层 CTE 中预计算（与 growth_rate_yoy 共用 growth_data CTE 协议）。YTD 列对比去年同 YTD

```sql
CASE
    WHEN previous_value > 0
    THEN ROUND((current_value - previous_value) * 100.0 / previous_value, 2)
    ELSE NULL
  END AS premium_growth_pct
```

**变更历史**:
- v1.0.0 (2026-05-13): 新增：与诊断技能 alerts.py v1.7 阈值对齐（10/5/2，lower_worse）

---

### 环比增长率 (`growth_rate_mom`)

- **版本**: 1.0.0
- **标签**: kpi, growth
- **单位**: %
- **公式**: (本期 - 上期) / 上期
  - 分子: current_value - previous_value
  - 分母: previous_value
- **展示**: 环比增长率 (percent, %)
- **注意**: current_value 和 previous_value 需要在外层 CTE 中预计算（月度偏移）

```sql
CASE
    WHEN previous_value > 0
    THEN ROUND((current_value - previous_value) * 100.0 / previous_value, 2)
    ELSE NULL
  END AS growth_rate_mom
```

**变更历史**:
- v1.0.0 (2026-03-27): 从 growth.ts 迁移

---

## 维修资源指标

### 合作网点总数 (`repair_shop_total_count`)

- **版本**: 1.0.0
- **标签**: repair, foundation
- **单位**: 个
- **公式**: RepairDim 中排除非维修单位后的去重网点数
- **展示**: 合作网点数 (count, 个)
- **注意**: JOIN key 用前 8 位编码；排除"定损/自选/无"关键词

```sql
COUNT(DISTINCT CASE WHEN (
    repair_shop_name NOT LIKE '%定损%'
    AND repair_shop_name NOT LIKE '%自选%'
    AND repair_shop_name <> '无'
    AND repair_shop_name IS NOT NULL
  ) THEN SUBSTR(repair_shop_name, 1, 8) END) AS repair_shop_total_count
```

**变更历史**:
- v1.0.0 (2026-04-18): 初版：维修资源板块重设计

---

### 4S店占比 (`repair_4s_share`)

- **版本**: 1.0.0
- **标签**: repair, ratio
- **单位**: %
- **公式**: 4S 店网点数 / 有效合作网点数
  - 分子: COUNT(DISTINCT CASE WHEN is_4s_shop THEN shop_code END)
  - 分母: COUNT(DISTINCT shop_code)
- **展示**: 4S 占比 (percent, %)

```sql
COUNT(DISTINCT CASE WHEN is_4s_shop AND (
    repair_shop_name NOT LIKE '%定损%'
    AND repair_shop_name NOT LIKE '%自选%'
    AND repair_shop_name <> '无'
    AND repair_shop_name IS NOT NULL
  ) THEN SUBSTR(repair_shop_name, 1, 8) END) * 1.0 / NULLIF(COUNT(DISTINCT CASE WHEN (
    repair_shop_name NOT LIKE '%定损%'
    AND repair_shop_name NOT LIKE '%自选%'
    AND repair_shop_name <> '无'
    AND repair_shop_name IS NOT NULL
  ) THEN SUBSTR(repair_shop_name, 1, 8) END), 0) AS repair_4s_share
```

**变更历史**:
- v1.0.0 (2026-04-18): 初版

---

### 合作启用率 (`repair_cooperation_active_rate`)

- **版本**: 1.0.0
- **标签**: repair, ratio
- **单位**: %
- **公式**: 已合作（1生效中）网点数 / 有效合作网点数
  - 分子: COUNT(DISTINCT CASE WHEN CASE
  WHEN cooperation_status = '1生效中' THEN 'active'
  WHEN cooperation_status IN ('0暂停合作', '7已撤销', '8失效') THEN 'past'
  ELSE 'none'
END = 'active' THEN shop_code END)
  - 分母: COUNT(DISTINCT shop_code)
- **展示**: 合作启用率 (percent, %)

```sql
COUNT(DISTINCT CASE WHEN CASE
  WHEN cooperation_status = '1生效中' THEN 'active'
  WHEN cooperation_status IN ('0暂停合作', '7已撤销', '8失效') THEN 'past'
  ELSE 'none'
END = 'active' AND (
    repair_shop_name NOT LIKE '%定损%'
    AND repair_shop_name NOT LIKE '%自选%'
    AND repair_shop_name <> '无'
    AND repair_shop_name IS NOT NULL
  ) THEN SUBSTR(repair_shop_name, 1, 8) END) * 1.0 / NULLIF(COUNT(DISTINCT CASE WHEN (
    repair_shop_name NOT LIKE '%定损%'
    AND repair_shop_name NOT LIKE '%自选%'
    AND repair_shop_name <> '无'
    AND repair_shop_name IS NOT NULL
  ) THEN SUBSTR(repair_shop_name, 1, 8) END), 0) AS repair_cooperation_active_rate
```

**变更历史**:
- v1.0.0 (2026-04-18): 初版

---

### 修保比 (`repair_to_premium_ratio`)

- **版本**: 1.0.0
- **标签**: repair, ratio, depth
- **单位**: 倍
- **公式**: 维修产值（核损金额）/ 签单净保费；衡量合作深度
  - 分子: SUM(damage_assessment_amount)
  - 分母: SUM(net_premium)
- **展示**: 修保比 (coefficient, 倍)
- **注意**: 业务解读：<0.3 合作浅可挖掘 / 0.3-0.7 健康 / >0.7 定损偏重需推动保费

```sql
SUM(COALESCE(damage_assessment_amount, 0)) * 1.0 / NULLIF(SUM(COALESCE(net_premium, 0)), 0) AS repair_to_premium_ratio
```

**变更历史**:
- v1.0.0 (2026-04-18): 初版

---

### 维修产值合计 (`repair_damage_amount_total`)

- **版本**: 1.0.0
- **标签**: repair, foundation
- **单位**: 元
- **公式**: 所有网点核损金额合计（= 维修产值）
- **展示**: 维修产值 (premiumWan, 万元)

```sql
SUM(COALESCE(damage_assessment_amount, 0)) AS repair_damage_amount_total
```

**变更历史**:
- v1.0.0 (2026-04-18): 初版

---

### 网点签单净保费合计 (`repair_net_premium_total`)

- **版本**: 1.0.0
- **标签**: repair, foundation
- **单位**: 元
- **公式**: RepairDim 中所有网点签单净保费合计
- **展示**: 网点净保费 (premiumWan, 万元)

```sql
SUM(COALESCE(net_premium, 0)) AS repair_net_premium_total
```

**变更历史**:
- v1.0.0 (2026-04-18): 初版

---

## 计划达成指标

### 计划达成率 (`plan_completion_pct`)

- **版本**: 3.0.0
- **标签**: kpi, plan, alert, branch-ops
- **单位**: %
- **公式**: 计划覆盖范围内年初累计签单保费 × 100 ÷ (同范围年计划 × 时间进度)；时间进度 = 数据内最新签单日是当年第几天 ÷ 全年天数（闰年感知）
  - 分子: covered_scope_ytd_premium
  - 分母: plan_premium * time_progress
- **展示**: 计划达成率 (percent, %)
- **注意**: L4 计算。分子与分母的机构/团队/业务员覆盖范围必须完全一致；未配置范围返回 NULL，禁止以 0 或其他粒度计划回退。SC 沿用 achievement_cache 业务员计划汇总；SX 仅机构粒度从 PlanFact.level=organization 读取，其中“覆盖范围完全一致”是 v3 对 SX 新链路的强约束；SC 仍存在 PolicyFact 分子与 achievement_cache 业务员计划覆盖范围未逐机构闭合验证的历史偏差，本版本明确保留其现网结果，不宣称已完成 SC 同源迁移。分公司整体及团队/业务员粒度在无权威计划时返回 NULL。covered_scope_ytd_premium = 年初（当年 1 月 1 日）至时间窗口末、且只属于计划覆盖范围的累计 SUM(premium)；time_progress = EXTRACT(doy FROM 数据内最新签单日) ÷ 全年天数（闰年感知，禁止硬编码 365）。进度锚点必须用数据内最新签单日（非服务器当前日期）；带时间筛选时返回「年初至筛选末日的累计达成率」。100% 即按时间进度均匀达成。月度/任意子周期计划取官方派生口径 = 年计划 × 子周期占比（月度即年计划 ÷ 12，B290 用户 2026-06-22 拍板；非真实逐月计划，详见业务规则字典 §「计划与时间进度口径」）；本指标标准口径用连续时间进度，离散月度仅为口头解释时的派生近似。注册到 L4_METRIC_IDS，集成测试跳过。

```sql
-- L4 计算，由 SQL 生成器动态拼接：ytd_premium * 100.0 / (plan_premium * time_progress)
```

**变更历史**:
- v3.0.0 (2026-07-21): SX 计划覆盖范围升级：分子与分母必须完全同源同范围；未配置范围返回 NULL，禁止回退业务员/团队计划。SX 仅机构粒度消费 PlanFact 机构计划，分公司整体在缺少全口径权威计划时不计算达成率；SC 保持原 achievement_cache 口径，其分子/计划覆盖范围闭合属于已知历史偏差，留待后续独立迁移。
- v2.1.0 (2026-06-22): B290 时间口径语义层 v0.1：标注 timeWindow=cutoff-based（达成率锚定数据内最新签单日的时间进度，数值随观察截止日变化，跨窗口比较须对齐 cutoff，不可与自由窗口口径混用）；确立月度计划官方派生口径 = 年计划 ÷ 12（用户 2026-06-22 拍板，非真实逐月计划）。仅补元数据与口径文档，L4 公式语义不变。
- v2.0.0 (2026-06-11): 占位符升级为正式标准口径（用户 2026-06-11 拍板，B-146cce）：分子明确为年初累计签单保费；时间进度锚点统一为数据内最新签单日（废除报告中心的服务器当前日期锚）；全年天数闰年感知（废除保费看板硬编码 365）；经营分析废除「年计划÷周期数」均分语义，下钻表/业务员表达成率列改为本口径（分摊漏失缺陷随之消灭）。三条接口同参数同值。
- v1.0.0 (2026-05-13): 新增：与诊断技能 alerts.py v1.7 阈值对齐（110/100/95，lower_worse）

---

## 业务结构指标

### 家自车占比 (`household_share_pct`)

- **版本**: 1.0.0
- **标签**: kpi, structure, alert, branch-ops
- **单位**: %
- **公式**: 客户类别='非营业个人客车' 的保单数 × 100 ÷ 全部保单数
  - 分子: COUNT(*) FILTER (WHERE customer_category = '非营业个人客车')
  - 分母: COUNT(*)
- **展示**: 家自车占比 (percent, %)
- **注意**: 结构性指标，反映业务质量底色。「非营业个人客车」即家自车，其赔付与续保表现普遍优于其他车型。规范文本见 src/shared/config/customer-categories.ts

```sql
ROUND(
    COUNT(*) FILTER (WHERE customer_category = '非营业个人客车') * 100.0
    / NULLIF(COUNT(*), 0),
    2
  ) AS household_share_pct
```

**变更历史**:
- v1.0.0 (2026-05-13): 新增：与诊断技能 alerts.py v1.7 阈值对齐（70/65/60，lower_worse）

---

## 续保分析指标

### 应续件数 (`renewal_due_count`)

- **版本**: 1.0.0
- **标签**: renewal, count
- **单位**: 件
- **公式**: 续保窗口内应续保的车辆件数（按车架号去重）
  - 分子: COUNT(DISTINCT vehicle_frame_no)
- **展示**: 应续件数 (count, 件)
- **注意**: L4 计算。数据源为 renewal_tracker 派生域（非 policy 主表）。续保窗口内按车架号去重

```sql
-- L4 计算，应续件数 A = COUNT(DISTINCT vehicle_frame_no)，由续保 SQL 生成器 renewal-tracker.ts（GROUPING SETS）实现
```

**变更历史**:
- v1.0.0 (2026-06-19): 新增：续保应续件数 A（VIN 去重），L4 占位符；口径取 renewal-tracker.ts SSOT

---

### 报价件数 (`renewal_quoted_count`)

- **版本**: 1.0.0
- **标签**: renewal, count
- **单位**: 件
- **公式**: 应续车中截至观察时点已有有效报价的件数（报价为真实时点事件，按 cutoff 切片）
  - 分子: COUNT(DISTINCT vehicle_frame_no WHERE is_quoted AND first_quote_time ≤ cutoff)
- **展示**: 报价件数 (count, 件)
- **注意**: L4 计算。报价为真实时点事件，按观察截止日 cutoff 切片。数据源为 renewal_tracker 派生域

```sql
-- L4 计算，报价件数 B = 应续中 first_quote_time ≤ cutoff 的 VIN 去重计数，由续保 SQL 生成器 renewal-tracker.ts 实现
```

**变更历史**:
- v1.0.0 (2026-06-19): 新增：续保报价件数 B（first_quote_time ≤ cutoff），L4 占位符；口径取 renewal-tracker.ts SSOT

---

### 已续件数 (`renewal_renewed_count`)

- **版本**: 1.0.0
- **标签**: renewal, count
- **单位**: 件
- **公式**: 应续车中已签单成交续保的件数（匹配到续保单号）
  - 分子: COUNT(DISTINCT vehicle_frame_no WHERE is_renewed)
- **展示**: 已续件数 (count, 件)
- **注意**: L4 计算。is_renewed 匹配到续保单号即已签单成交；renewed_date 是续保起保日（=原保单到期次日）非签单时点，不按 cutoff 过滤（未到期已签单仍属已续）

```sql
-- L4 计算，已续件数 C = 应续中 is_renewed 的 VIN 去重计数，由续保 SQL 生成器 renewal-tracker.ts 实现
```

**变更历史**:
- v1.0.0 (2026-06-19): 新增：续保已续件数 C（is_renewed），L4 占位符；口径取 renewal-tracker.ts SSOT

---

### 未报价件数 (`renewal_unquoted_count`)

- **版本**: 1.1.0
- **标签**: renewal, count
- **单位**: 件
- **公式**: 应续车中至今无任何有效报价的件数 = 应续件数 − 已报价件数
  - 分子: COUNT(DISTINCT vehicle_frame_no)（应续） − 已报价件数
- **展示**: 未报价件数 (count, 件)
- **注意**: L4 计算。数据源为 renewal_tracker 派生域（非 policy 主表）。续保窗口内按车架号去重

```sql
-- L4 计算，应续件数 − 已报价件数（A − B），由续保 SQL 生成器 renewal-tracker.ts 与诊断脚本 diagnose_renewal_branch.py 实现
```

**变更历史**:
- v1.1.0 (2026-06-19): 归类调整：foundation → renewal 续保分析域（口径不变；与 A/B/C/E/影响度 同域可一并 cx metrics --category renewal 枚举）
- v1.0.0 (2026-06-07): 新增：续保未报价件数（应续 − 已报价），L4 占位符

---

### 流失件数 (`renewal_lost_count`)

- **版本**: 1.1.0
- **标签**: renewal, count
- **单位**: 件
- **公式**: 应续车中尚未续保的件数（含未报价 + 已报价未成交）= 应续件数 − 已续保件数
  - 分子: COUNT(DISTINCT vehicle_frame_no)（应续） − 已续保件数
- **展示**: 流失件数 (count, 件)
- **注意**: L4 计算。数据源为 renewal_tracker 派生域。⚠️ 仅在已到期窗口表示真实流失；未到期窗口为「待续件数」（尚未到续保动作时点）

```sql
-- L4 计算，应续件数 − 已续保件数（A − C），由续保 SQL 生成器 renewal-tracker.ts 与诊断脚本 diagnose_renewal_branch.py 实现
```

**变更历史**:
- v1.1.0 (2026-06-19): 归类调整：foundation → renewal 续保分析域（口径不变）
- v1.0.0 (2026-06-07): 新增：续保流失件数（应续 − 已续保），L4 占位符

---

### 续保影响度 (`renewal_impact_rate`)

- **版本**: 1.1.0
- **标签**: renewal, ratio
- **单位**: %
- **公式**: 该分类流失导致整体续保缺口扩大的占比；可加和，各分类之和 = 整体续保缺口（1 − 续保率）
  - 分子: SUM(流失件数)（按分类聚合）
  - 分母: 合计应续件数（窗口内全部分类的应续件数合计）
- **展示**: 续保影响度 (percent, %)
- **注意**: L4 计算。遵循「先聚合后计算」：先按分类（机构/团队/业务员）聚合各件数，再以该次分类的合计应续件数为分母，什么分类就按什么合计。⚠️ 仅在已到期窗口表示真实流失影响度（越高越坏）

```sql
-- L4 计算，流失件数 ÷ 合计应续件数（分母为窗口合计应续，需窗口聚合 SUM(...) OVER()），由诊断脚本 diagnose_renewal_branch.py 实现
```

**变更历史**:
- v1.1.0 (2026-06-19): 归类调整：ratio → renewal 续保分析域（口径不变）
- v1.0.0 (2026-06-07): 新增：续保影响度（流失 ÷ 合计应续，可加和缺口分解），L4 占位符

---

## 销售队伍业绩指标

### 标保 (`standard_premium`)

- **版本**: 1.0.0
- **标签**: sales-team, premium, standardized
- **单位**: 元
- **公式**: 销售队伍明细实收保费乘最终折标系数后的金额合计
  - 分子: SUM(标保)
- **展示**: 标保 (premiumWan, 万元)
- **注意**: L4 独立域：数据源为 SalesTeamPerformanceFact；不得在 PolicyFact pivot/cube 中执行

```sql
-- L4 独立域计算：ROUND(SUM("标保"), 2) AS standard_premium，由 sales-team-performance.ts 实现
```

**变更历史**:
- v1.0.0 (2026-07-15): 新增销售队伍业绩域标保指标；口径取 sales_team_rules.sql

---

### 实收保费 (`received_premium`)

- **版本**: 1.0.0
- **标签**: sales-team, premium, received
- **单位**: 元
- **公式**: 销售队伍业绩明细的实收保费金额合计
  - 分子: SUM(实收保费)
- **展示**: 实收保费 (premiumWan, 万元)
- **注意**: L4 独立域：数据源为 SalesTeamPerformanceFact；不得与 PolicyFact 的签单保费混用

```sql
-- L4 独立域计算：ROUND(SUM("实收保费"), 2) AS received_premium，由 sales-team-performance.ts 实现
```

**变更历史**:
- v1.0.0 (2026-07-15): 新增销售队伍业绩域实收保费指标

---

### 销售业绩明细行数 (`sales_team_row_count`)

- **版本**: 1.0.0
- **标签**: sales-team, count, row-count
- **单位**: 行
- **公式**: 销售队伍业绩明细行数；同一保单可有多险种或多次实收，不是保单去重件数
  - 分子: COUNT(*)
- **展示**: 明细行数 (count, 行)
- **注意**: L4 独立域：严禁复用 PolicyFact policy_count（COUNT DISTINCT 保单号）解释本指标

```sql
-- L4 独立域计算：COUNT(*) AS sales_team_row_count，由 sales-team-performance.ts 实现
```

**变更历史**:
- v1.0.0 (2026-07-15): 新增无歧义明细行数，替代本域误导性的 policy_count 别名

---

