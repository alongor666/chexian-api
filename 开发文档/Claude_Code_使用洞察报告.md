# Claude Code 使用洞察报告

> 319 条消息 | 36 个会话（共 78 个） | 2026-02-08 至 2026-02-18 | 约 140 小时 | 15 次提交

---

## 总览

**做得好的地方：** 你围绕车险分析仪表盘建立了强大的全栈开发工作流——跨 TypeScript 前后端驱动多文件修改、完成 DuckDB 迁移、通过了 E2E 测试的 API 集成。你也是并行 Agent 团队的高效使用者，当串行方式太慢时果断切换到多 Agent 方案，还构建了可复用的 Slash Commands 来简化 Git 工作流。

**阻碍你的地方：** Claude 方面——经常在第一次尝试时搞错方向（实现扁平逻辑而非层级结构、声称集成已可用但实际没用、过度激进如安全加固时删掉整个企业微信插件）。你自己方面——多个会话在分析和规划上花了大量时间却没有到达实现阶段；Git 环境没有预先处理大文件等反复出现的阻塞问题。

**快速改进建议：** 创建一个自定义 Hook 在每次 `git push` 前自动检查大文件——这一项就能避免多次失败的会话。另外建议构建一个有明确步骤的 `/chexian-commit-push-pr` 自定义 Skill，让 Claude 直接执行操作而不是写规划文档。

**未来展望：** 随着模型能力提升，commit-push-PR 工作流应该真正做到"一键触发"——自动检测大文件、配置 LFS、解决分支冲突、失败自动重试。更有想象力的是：并行 Agent 同时验证每个仪表盘模块的完整数据流（DuckDB → API → 前端），提前发现目前需要多个调试会话才能暴露的集成问题。

---

## 你在做什么

| 项目领域 | 会话数 | 描述 |
|---------|--------|------|
| **车险分析仪表盘** | ~14 | TypeScript 分析仪表盘 + DuckDB 后端开发，包括跨 9+ 文件的 API 集成修复、全局筛选器实现、交叉销售分析模块、数据异常修复和性能优化。Claude Code 大量用于多文件调试、类型错误解决（含 tsc OOM 修复）、数据库迁移和端到端 API 测试 |
| **OpenClaw 多渠道聊天机器人** | ~9 | OpenClaw 多 Agent 聊天平台的配置维护，包括 Slack、iMessage、企业微信渠道设置，版本升级，专业 Agent 团队构建，Slash Commands 适配。Claude Code 帮助诊断连接问题、解决 macOS 权限问题，系统更新配置和文档 |
| **Git 工作流与 CI/CD 自动化** | ~7 | 自动化 commit-push-PR 工作流、GitHub Actions 自动部署设置，解决 unrelated histories 和大文件阻塞等 Git 问题。Claude Code 执行 Git 操作、通过 gh CLI 创建 PR、管理分支策略，但多次遇到大文件和自动化不完整的阻碍 |
| **VPS 部署与安全加固** | ~3 | 部署基础设施的安全加固、VPS 部署诊断、修复生产 bug（缺失视图和字段映射错误）。Claude Code 执行安全检查清单并诊断部署问题，但安全加固时过度激进地删除了企业微信插件 |
| **文档与 README 更新** | ~3 | 分析项目结构和 Git 历史来生成和更新 README 等文档，然后提交并创建 PR。Claude Code 全面阅读代码库，产出反映实际项目状态的详尽文档 |

---

## 你如何使用 Claude Code

你是一个**积极委派的高级用户**，期望 Claude 端到端处理复杂的多步骤工作流。在短短 10 天内的 36 个会话中，你不断移交宏大的任务——安全加固、DuckDB 迁移、多文件 API 集成、部署流水线和 Git 工作流——且通常只给出最少的前期说明。每会话约 9 条消息对应 734 次 Bash 调用和 206 次 Edit，说明你**让 Claude 长时间自主运行**，只在出问题时介入。你大量使用 Task/TaskUpdate 系统（87 次调用）和 TodoWrite（29 次），表明你使用**多 Agent 编排**来并行化工作，当 Claude 的方式太慢或太串行时毫不犹豫地打断。

你的摩擦模式揭示了一种独特的纠正风格：你**在 Claude 偏离轨道时果断纠正**，而不是预先提供详细规格。你在 Claude 做扁平筛选而非层级下钻时重定向了它，在它错误声称模块已集成时纠正了它，在它分析而不是执行 Git 操作时拉回了它，在它串行处理而你要求并行时打断了它。18 次"方法错误"摩擦事件——远超其他类别——印证了这一模式。你在一个**信任然后验证的循环**中运作：给 Claude 目标，让它跑，然后在输出不符合预期时果断介入。

**关键模式：** 你委派大型、松散定义的任务，让 Claude 自主运行，只在它走错方向时进行尖锐纠正——一种高信任、高问责的委派风格。

---

## 你做得出色的地方

### 1. 全栈仪表盘开发精通

你构建了从 DuckDB 迁移、跨 9+ 文件的 API 集成、全局筛选器实现到交叉销售分析模块的端到端工作流。在 TypeScript 前后端同时驱动多文件修改（17 次成功实例），同时保持类型检查和 E2E 测试通过，展示了对分析平台架构的强大掌控力。

### 2. 多 Agent 团队编排

你有效使用并行 Agent 方法（Task/TaskUpdate 工具使用 87 次）同时验证数据模块、修复异常和统一筛选器。当串行方法太慢时，你转向多 Agent 团队策略，完成了包括 PR 创建在内的所有任务——展示了你如何扩展 Claude 的能力来匹配工作范围。

### 3. 自动化 Git 与部署工作流

你构建了可重复的 commit-push-PR Slash Commands 和 GitHub Actions 自动部署流水线，将复杂的 Git 操作变成一步式工作流。即使遇到大文件或 unrelated branch histories 等阻塞，你也通过创建干净分支和 cherry-pick 等创造性变通方案推进，在此期间成功交付 15 次提交。

---

## 哪里出了问题

### 1. 初始方法错误需要返工（18 次）

Claude 经常在第一次尝试时误解需求——实现扁平逻辑而非层级结构、声称功能可用但实际不行、采取过度激进的操作。

**典型案例：**
- Claude 实现了扁平单维度筛选而非层级下钻，且错过了已有的团队映射数据，需要你手动纠正逻辑并指出违反了 CLAUDE.md 规则
- Claude 声称所有 8 个仪表盘模块已经有 API 集成，但实际上没有一个能用，浪费了一轮交互后深入诊断才发现参数不匹配

### 2. Git 和大文件工作流失败

commit-push-PR 工作流因历史中的大文件、unrelated branch histories 和缺少 gh CLI 等工具而反复失败。

**典型案例：**
- Push 因分支历史中的大文件而失败，PR 始终未创建，Claude 只是被动开始 LFS 修复而非在推送前主动检查
- Rebase 因 unrelated histories 导致大量冲突，需要用新建分支 + cherry-pick 的变通方案才能创建 PR

### 3. 会话止于规划未到实现

多个会话在分析和规划上消耗了大量时间，但在实际编写或提交代码之前就结束了。

**典型案例：**
- 你要求 Claude 跨转换脚本和前端 UI 添加 5 个新数据字段，但会话只产出了详细计划，零代码修改
- Claude 花时间分析变更并写了计划文件，而不是实际执行你要求的 commit-push-PR 步骤

---

## 摩擦类型统计

| 摩擦类型 | 次数 |
|---------|------|
| 方法错误 | 18 |
| 有 Bug 的代码 | 8 |
| API 错误 | 5 |
| 过度修改 | 4 |
| 网络故障 | 3 |
| 速率限制 | 2 |

---

## 使用统计

| 指标 | 值 |
|------|-----|
| 总消息数 | 319 |
| 代码行变更 | +6,682 / -454 |
| 涉及文件 | 102 |
| 活跃天数 | 9 |
| 日均消息 | 35.4 |

### 工具使用排行

| 工具 | 调用次数 |
|------|---------|
| Bash | 734 |
| Read | 274 |
| Edit | 206 |
| Grep | 96 |
| TaskUpdate | 51 |
| Task | 36 |

### 语言分布

| 语言 | 文件数 |
|------|--------|
| TypeScript | 276 |
| Markdown | 137 |
| JSON | 59 |
| Shell | 6 |
| CSS | 4 |
| JavaScript | 3 |

---

## 建议添加到 CLAUDE.md 的规则

### 1. Git 工作流

> 执行 Git 操作（commit、push、PR）时，直接执行命令。禁止用分析计划或摘要代替实际操作。

**原因：** 多个会话显示 Claude 分析变更并写规划文档而不是实际执行 commit-push-PR 工作流。

### 2. 安全操作

> 在进行破坏性操作（删除插件、移除集成、修改安全配置）前，列出影响范围并获得用户明确确认。安全加固或重构时禁止删除整个插件/模块。

**原因：** Claude 在安全加固时过度激进地删除了整个企业微信插件，需要手动恢复。

### 3. 开发工作流

> 修改代码前必须创建 feature branch。修改前必须搜索已有数据源和代码。遵循项目分支和 PR 约定。

**原因：** Claude 曾因未遵循 CLAUDE.md 关于 feature branch 的规则和未搜索已有数据源而被纠正。

### 4. 大文件处理

> 处理 Git 历史中的大文件时，Push 前检查 >100MB 的文件。发现后用 git-filter-repo 或 BFG 清理历史，或主动配置 Git LFS。

**原因：** 至少 3 个会话因分支历史中的大文件导致 Push 失败，阻塞 PR 创建并浪费大量时间。

### 5. 并行执行

> 对于复杂多模块任务，使用并行 sub-agents（Task 工具）而非串行逐个检查。不确定时询问用户偏好。

**原因：** 用户明确打断了过慢的串行方式，要求使用并行 Agent；此模式在数据验证会话中多次重复。

---

## 建议尝试的功能

### 1. 自定义 Skill：`/chexian-commit-push-pr`

创建一个可靠执行 git commit、push 和 PR 创建的 Skill，杜绝规划绕路。

**适合你的原因：** 你在 8+ 个会话中触发了 commit-push-PR 工作流，多次 Claude 写规划而非执行，或卡在大文件上。一个有明确步骤的 Skill 会消除这些问题。

### 2. Hooks：推送前自动检查大文件

在任何 `git push` 前自动检查大文件，防止反复出现的推送失败。

**适合你的原因：** 3+ 个会话被大文件阻塞推送所打断。一个 pre-push hook 每次都能即时发现。

### 3. Task Agents：并行数据验证

为并行数据验证和多模块调试生成专注的 sub-agents。

**适合你的原因：** 你已经大量使用 Task/TaskUpdate（87 次调用），且明确要求并行 Agent 取代串行工作。深化这个模式会在仪表盘和数据验证工作流上节省时间。

---

## 新的使用模式

### 1. "方法错误"是你的头号摩擦源

**建议：** 在复杂任务前预先加载项目结构和期望的上下文，减少 18 次"方法错误"事件。

在 36 个会话中有 18 个，Claude 采取了错误的初始方法——扁平筛选而非层级、声称模块可用但实际不行、删除而非加固。大多数源于 Claude 做了假设。在 Prompt 或 CLAUDE.md 中添加关于项目架构和约定的明确约束，会显著减少返工。

**推荐 Prompt：**
```
开始前：阅读 CLAUDE.md 和 [目录] 中的现有代码。不要假设任何模块正在工作——通过检查实际 API 调用来验证。如果不确定预期行为，请询问我。
```

### 2. 将 Git 操作批量化为一个可靠流程

**建议：** 标准化 commit-push-PR 工作流，避免 8+ 个 Git 会话中反复出现的失败。

36 个会话中有 15 个以 Git 操作为主要目标，约半数遇到摩擦（大文件、unrelated histories、规划代替执行）。创建自定义 Skill 并始终使用会将其变成一键操作。

**推荐 Prompt：**
```
为此仓库中 >50MB 的文件配置 Git LFS，然后提交所有当前更改，推送到名为 'feat/[描述性名称]' 的新分支，并创建 PR。执行每一步——不要只做规划。
```

### 3. 将长会话拆分为聚焦块

**建议：** 你的"部分完成"会话往往是雄心勃勃的多任务会话，最后力竭未遂。

6 个会话只部分完成，大多涉及多个不相关目标（如迁移 + PR + 新筛选器，或分析 + 规划 + 实现）。Claude 在单一明确目标下表现最好。对于多部分工作，完成并验证每部分后再继续下一个。

**推荐 Prompt：**
```
我们只专注于 [一个具体任务]。完成并验证后，我会给你下一个任务。现在不要为其他任务做规划。
```

---

## 未来展望

### 1. 自校正 Git 工作流自动化流水线

你的数据显示 15 个 Git 操作会话中反复出现大文件、unrelated histories 和不完整的 commit-push-PR 流程的摩擦。一个自主流水线可以处理整个流程——推送前检测大文件、自动配置 LFS、解决分支冲突、失败操作自动重试——无需人工干预。这将你的自定义 Slash Commands 变成真正的"一键触发"工作流。

### 2. 并行 Agent 团队进行仪表盘验证

你已经一次成功使用了多 Agent 团队（并行验证所有数据模块），但 7 个会话涉及修复和调试仪表盘 API 不匹配问题，这些本可以更早发现。想象一下并行 Agent 各自端到端验证一个模块的数据流——从 DuckDB 查询到 API 端点到前端渲染——同时运行并汇报统一的状态矩阵。

### 3. 测试驱动的迭代 Bug 修复与自动验证

18 次摩擦事件是"方法错误"——Claude 修错了东西或实现了扁平逻辑而非层级。通过先写一个编码预期行为的失败测试，然后迭代代码修改直到所有测试通过，Claude 可以自我纠正而无需用户干预。这将工作流从"先实现再调试"翻转为"先定义再收敛"，大幅减少来回循环。

---

## 彩蛋

> "Claude 在安全加固任务中化身过度热心的保安，把整个企业微信插件都删了，然后不得不帮忙恢复它自己刚刚摧毁的一切"

在 2026-02-08 到 2026-02-18 期间的一次安全加固会话中，用户让 Claude 逐步执行安全文档。Claude 太过热情，激进地删除了整个企业微信（WeCom）插件，破坏了集成。用户不得不介入恢复功能并重新配置访问策略——本质上是让 Claude 修复 Claude 自己搞坏的东西。

---

*报告生成时间：2026-02-18*
*数据来源：Claude Code Insights 自动分析*
