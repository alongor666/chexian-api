# Code Review Report - 2026-02-18

## Findings (ordered by severity)

### 1. High: Host key verification was weakened to TOFU in deployment workflow
- **File**: `.github/workflows/deploy.yml:51`
- **Issue**: Change replaced pre-pinned `known_hosts` secret with runtime `ssh-keyscan` against the target IP. This trusts whatever key is presented at deploy time, which can allow MITM during CI-to-VPS SSH bootstrap.
- **Risk**: compromised deployment path (artifact upload + remote command execution).
- **Recommendation**: Keep host key pinning via secret (or verify scanned fingerprint against an expected fingerprint stored in secret before SSH/SCP).

### 2. Medium: Deployment documentation is now inconsistent with workflow behavior
- **File**: `CLAUDE.md:876` vs `.github/workflows/deploy.yml:44-51`
- **Issue**: Docs still require `VPS_KNOWN_HOSTS` secret, but workflow no longer uses it.
- **Risk**: Operator confusion, stale secret management, and harder incident response when troubleshooting deploy auth failures.
- **Recommendation**: Align docs and workflow immediately (either restore secret usage in workflow, or remove the secret from docs and document fingerprint verification strategy).

### 3. Low: Untracked large-data LFS pointers present in working tree
- **Files**: 
  - `数据管理/meta_data/车险签单报价数据20260214.xlsx` (~140MB)
  - `数据管理/meta_data/续保类型匹配至2026年4月.xlsx` (~101MB)
  - `chexian-deploy.tar.gz` (~22MB)
- **Issue**: These are LFS pointer files referencing large objects.
- **Risk**: Accidental commit without corresponding LFS object availability can break push/CI for collaborators.
- **Recommendation**: Confirm whether these are intended to be versioned now; if not, keep them out of commit scope.

## Open Questions

1. Was removal of `VPS_KNOWN_HOSTS` intentional as a permanent security tradeoff, or temporary to bypass secret setup issues?
2. Should deploy security policy require strict host fingerprint pinning for production CI paths?
3. Are the three untracked LFS pointers intended for this PR/commit, or local-only artifacts?

## Change Summary

- **Modified tracked files reviewed**: `.github/workflows/deploy.yml`, `CLAUDE.md`
- **Main functional impact**: CI deployment SSH trust setup; docs changes are mostly governance text but currently drift from deploy workflow config.

---
*Generated by OpenAI Codex (gpt-5.3-codex) on 2026-02-18*
