# Code Review Report - 2026-02-15

## 自动化代码审查报告

**审查日期**: 2026-02-15  
**审查分支**: review/codex-review-2026-02-15  
**审查工具**: OpenAI Codex v0.98.0

---

## 发现问题（按严重程度排序）

### 🔴 高优先级问题

#### 1. **高：`sanitizeFilename` 安全回退 + 测试失败**
- **位置**: `server/src/utils/security.ts:47`
- **问题**: `sanitizeFilename` 从白名单切换到黑名单，现在允许之前被阻止的字符（`'`, `;`, `` ` ``, `$()`, 空格, 全角斜杠变体）
- **影响**: 直接破坏现有安全测试（`server/src/utils/__tests__/security.test.ts:68`, `server/src/utils/__tests__/security.test.ts:241`）
- **验证**: 运行 `bun run test server/src/utils/__tests__/security.test.ts` -> **6 个测试失败**（特殊字符注入 + Unicode 斜杠绕过案例）

#### 2. **高：敏感操作数据出现在仓库变更中**
- **位置**: 
  - `logs/audit.log:1` - 包含真实审计记录（用户名/用户ID, 路径, 时间戳）
  - `vps.md:16`, `vps.md:47`, `vps.md:55`, `vps.md:92`, `vps.md:105` - 暴露具体基础设施细节（公网IP, SSH方法, root登录配置, 端口绑定）
  - `chexian-deploy.tar.gz` - 新增工件
- **风险**: 如果超出严格私有访问范围共享，可能导致内部基础设施暴露和操作数据泄露

### 🟡 中等优先级问题

#### 3. **中：审计日志文件权限要求已记录但未强制执行**
- **位置**: `server/src/middleware/audit.ts`
- **问题**: 
  - 第48行说明审计日志应该是 `600` 权限
  - 实现仅确保目录模式 `700`（第85行）并使用 `appendFile`（第89行）而没有明确的 `chmod`
  - 实际文件权限取决于进程 umask，可能不符合声明的安全要求

#### 4. **中：部署脚本宣传默认凭据**
- **位置**: `deploy/vps-deploy.sh:395`
- **问题**: 部署后打印 `admin / admin123`
- **影响**: 即使作为提示，这也使不安全的默认值正常化，如果密码轮换被遗漏，可能导致生产环境暴露

### 🟢 低优先级问题

#### 5. **低：审计中间件范围的行为/注释不匹配**
- **位置**: `server/src/app.ts`
- **问题**: 
  - 注释说仅限生产环境（第45行），但中间件无条件挂载（第49行）
  - 不是功能性破坏，但会导致非生产环境的审计行为，可能意外产生本地审计日志

---

## 其他观察

✅ **审计中间件集成方法合理**：
- 非阻塞写入
- 响应后计时
- API路径过滤

⚠️ **部署文档/脚本全面但混合了高度敏感的环境细节**：
- 应视为受限工件
- 需要严格的访问控制

---

## 验证执行

- ✅ 运行针对性测试：`bun run test server/src/utils/__tests__/security.test.ts`（失败：6个测试）
- ❌ 未运行完整仓库测试套件

---

## 建议行动

### 立即行动
1. **回退 `sanitizeFilename` 更改**或修复黑名单实现以通过所有安全测试
2. **从仓库变更中移除敏感数据**：
   - 添加 `logs/audit.log` 到 `.gitignore`
   - 添加 `vps.md` 到 `.gitignore` 或移除敏感信息
   - 移除 `chexian-deploy.tar.gz`

### 短期行动
3. **强制执行审计日志文件权限**：在文件创建后添加显式 `chmod 600`
4. **更新部署脚本**：移除默认凭据显示，改为提示用户设置强密码

### 长期行动
5. **统一审计中间件行为与文档**：根据环境变量有条件地挂载中间件
6. **审查所有部署工件**：确保不包含生产凭据或敏感基础设施信息

---

## 总结

本次审查发现了 **2个高优先级安全问题**、**2个中等优先级问题** 和 **1个低优先级问题**。最关键的是 `sanitizeFilename` 的安全回退和敏感操作数据的暴露。建议在合并此PR之前解决高优先级问题。